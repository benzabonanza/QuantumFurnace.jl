# Inactive staging archive

Archived on 2026-08-04. These files were already inactive or non-included at
the Task 2 green baseline (6,867/6,867 default tests). They are frozen,
unmaintained historical references and are not loaded or tested by the active
package. The `src/` and `test/` subtrees preserve their former relative
locations to disambiguate files with the same name.
