---
phase: 06-dead-code-pruning
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/bohr_domain.jl
  - src/coherent.jl
  - src/trotter_domain.jl
  - src/qi_tools.jl
  - src/errors.jl
  - src/structs.jl
  - src/ofts.jl
autonomous: true

must_haves:
  truths:
    - "All exported and internal functions are reachable from the public API or test suite"
    - "LindbladianJumpCaches, LiouvLiouv structs no longer exist in the codebase"
    - "Non-mutating oft wrapper function no longer exists in the codebase"
    - "qi_tools functions are preserved (pedagogical/user-facing value per locked decision)"
    - "linearmaps and log_sobolev functions are preserved (future dependency per locked decision)"
    - "time_oft! and trotter_oft! are preserved (per locked decision)"
    - "All 224 existing tests pass with no regressions"
  artifacts:
    - path: "src/structs.jl"
      provides: "Structs file with LindbladianJumpCaches, LiouvLiouv, and LindbladWorkspace removed"
    - path: "src/ofts.jl"
      provides: "OFTs file with non-mutating oft() removed, time_oft! and trotter_oft! preserved"
    - path: "src/bohr_domain.jl"
      provides: "Bohr domain with ~8 unused functions removed"
    - path: "src/coherent.jl"
      provides: "Coherent module with ~9 unused functions removed"
    - path: "src/trotter_domain.jl"
      provides: "Trotter domain with ~4 unused functions removed"
    - path: "src/errors.jl"
      provides: "Errors with empty stubs and broken compute_errors removed"
    - path: "src/qi_tools.jl"
      provides: "QI tools with are_we_tp removed, all useful functions preserved"
  key_links:
    - from: "src/QuantumFurnace.jl"
      to: "src/structs.jl"
      via: "include + export"
      pattern: "No references to deleted types remain in exports"
    - from: "src/ofts.jl"
      to: "src/jump_workers.jl"
      via: "oft! called from jump_contribution!"
      pattern: "oft! still works for EnergyDomain"
---

<objective>
Remove ~35 unused functions and 3 dead structs (LindbladianJumpCaches, LiouvLiouv, non-mutating `oft`).

Purpose: Satisfy PRUNE-02 and PRUNE-03 -- eliminate unreachable code and dead type definitions. Keep functions on the explicit preserve list.
Output: Source files containing only reachable, live functions and types.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dead-code-pruning/06-CONTEXT.md
@.planning/phases/06-dead-code-pruning/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead structs and non-mutating oft wrapper</name>
  <files>
    src/structs.jl
    src/ofts.jl
  </files>
  <action>
**PRUNE-03: Remove dead structs and non-mutating `oft` wrapper.**

**structs.jl removals:**
1. Remove `LindbladianJumpCaches` struct (lines 23-34) -- constructor and all fields. No tombstone comment (per locked decision).
2. Remove `LiouvLiouv` struct (lines 234-239) -- constructor and all fields. No tombstone comment.
3. Remove `LindbladWorkspace` struct (lines 287-296) -- used only by the commented-out `apply_lindbladian!` / `apply_lindbladian_dagger!` which were removed in Plan 01. This struct has no remaining callers.

Before removing each struct, verify no active code references it:
- Search for `LindbladianJumpCaches` across all `src/` and `test/` files -- should only appear in structs.jl definition and QuantumFurnace.jl exports
- Search for `LiouvLiouv` across all `src/` and `test/` files -- should only appear in structs.jl definition
- Search for `LindbladWorkspace` (NOT `LindbladianWorkspace`) across all `src/` and `test/` files -- should only appear in structs.jl definition and QuantumFurnace.jl exports

**ofts.jl removal:**
1. Remove the non-mutating `oft` function (lines 1-4 of ofts.jl) -- the allocating wrapper `oft(jump, energy, hamiltonian, sigma)` that returns a new matrix. Keep `oft!` (the in-place version at lines 6-10).
2. PRESERVE `time_oft!` and `trotter_oft!` -- these are on the explicit keep list for NUFFT validation.
3. PRESERVE `OFTCaches` struct in structs.jl -- it is used by `time_oft!` and `trotter_oft!` which are kept.

Verify no active code calls the non-mutating `oft()` (as opposed to `oft!()`). The only callers should be in the dead functions already removed in Plan 01 or the errors.jl functions being removed in Task 2.
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to confirm module loads. Search for any remaining references to removed types/functions.
  </verify>
  <done>
LindbladianJumpCaches, LiouvLiouv, LindbladWorkspace structs are deleted. Non-mutating `oft` wrapper is deleted. OFTCaches, time_oft!, trotter_oft! are preserved. Module loads cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused active functions</name>
  <files>
    src/bohr_domain.jl
    src/coherent.jl
    src/trotter_domain.jl
    src/qi_tools.jl
    src/errors.jl
  </files>
  <action>
**PRUNE-02: Remove unreachable active functions.**

**Explicit keep list (from CONTEXT.md locked decisions -- do NOT remove):**
- `time_oft!()` and `trotter_oft!()` in ofts.jl
- ALL `qi_tools.jl` functions (pedagogical/user-facing value) EXCEPT `are_we_tp` which references undefined globals
- ALL `linearmaps_liouv.jl` functions (future dependency)
- ALL `log_sobolev.jl` functions (future dependency)
- Functions relevant to v1.1 phases 7-11 (preserved even if currently unused)

**Functions to remove per file:**

**bohr_domain.jl** -- Remove these functions that are unreachable from public API or tests:
1. `coherent_bohr_gauss(hamiltonian, jump, beta)` -- old Gaussian-only B computation superseded by `coherent_bohr(ham, jump, config)`. Verify not called anywhere.
2. `transition_bohr_gauss_vectorized(jumps, hamiltonian, beta)` -- old vectorized Liouvillian assembly. Verify not called.
3. `transition_bohr_gauss_gibbsed_vectorized(jumps, hamiltonian, beta)` -- old Gibbs-weighted version. Verify not called.
4. `thermalize_bohr_gauss_vectorized(jumps, hamiltonian, initial_dm, delta, mixing_time, beta)` -- old Gaussian-only thermalization. References undefined `with_coherent`, `vectorize_liouvillian_coherent` (non-!) and `vectorize_liouvillian_diss` (non-!). Verify not called.
5. `B_nu_gauss(nu, nu_2, hamiltonian, jump, beta)` -- old per-frequency B component. Verify not called.
6. `R_nu_gauss(nu, nu_2, hamiltonian, jump, beta)` -- old per-frequency R component. Calls 2-arg `create_alpha_gauss` and `create_f_gauss` which don't match current signatures. Verify not called.
7. `check_alpha_skew_symmetry(alpha, nu_1, nu_2, beta)` -- debug utility. Verify not called.
8. `find_all_nu1s_to_nu2(nu_2, nu, unique_freqs)` -- unused helper. Verify not called.

**coherent.jl** -- Remove these functions:
1. `coherent_term_time(jump, hamiltonian, f_minus, f_plus, t0)` -- old f-based B computation, superseded by `B_time`. Verify not called.
2. `coherent_term_trotter(jump, trotter, f_minus, f_plus)` -- old f-based trotter B, superseded by `B_trotter`. Verify not called.
3. `coherent_term_time_metro_exact(jump, hamiltonian, time_labels, beta)` -- old exact Metro coherent term. Verify not called.
4. `coherent_term_timedomain_integrated_gauss(jump, hamiltonian, beta, time_domain)` -- old Gaussian integral. Verify not called.
5. `coherent_term_time_integrated_metro(jump, hamiltonian, eta, beta, time_domain)` -- old Metro integral. Verify not called.
6. `coherent_term_time_integrated_eh(jump, hamiltonian, beta, a, b, time_domain)` -- old eh integral. Verify not called.
7. `coherent_term_time_integrated_eh_b(jump, hamiltonian, beta, a, b, time_domain)` -- old b-parameterized integral. Verify not called.
8. `check_B_gauss(coherent_term, jump, hamiltonian, beta)` -- debug checker referencing `trace_norm_nh`. Verify not called.
9. `check_B_metro(coherent_term, jump, hamiltonian, eta, beta)` -- debug checker referencing undefined `SpinBasis`, `tracenorm_nh`. Verify not called.

**trotter_domain.jl** -- Remove:
1. `trotterize(hamiltonian, T, num_trotter_steps)` -- 1st order Trotter, uses `Progress`/`@showprogress`. Superseded by `trotterize2`. Verify not called.
2. `trotter_diag(hamiltonian, T, num_trotter_steps)` -- 1st order with diag. Calls `trotterize`. Verify not called.
3. `trotter2_diag(hamiltonian, T, num_trotter_steps)` -- 2nd order with diag. Used for standalone evolution, not by any simulation path. Verify not called.
4. `trotter2_t0_multiple(int_multiple, trott_t0_eigvals)` -- convenience wrapper. Verify not called.

**qi_tools.jl** -- Remove:
1. `are_we_tp(liouv)` -- references undefined `hamiltonian` and `num_qubits` globals. Clearly dead.

**errors.jl** -- Remove:
1. `compute_errors(hamiltonian, config; trotter)` -- calls `compute_energy_quadrature_error` which references undefined `num_qubits`, `create_jumpop`, and the non-mutating `oft` (now removed). Broken and unreachable.
2. `compute_quadrature_error(integrand, labels, args...)` -- only called by `compute_energy_quadrature_error`. Remove together.
3. `compute_energy_quadrature_error(config, hamiltonian, energy_labels; trotter)` -- references undefined `num_qubits`, `create_jumpop`, `oft`. Broken.
4. `compute_time_oft_quadrature_error()` -- empty stub.
5. `compute_trotter_oft_quadrature_error()` -- empty stub.
6. `compute_time_B_quadrature_error()` -- empty stub.
7. `compute_trotter_B_quadrature_error()` -- empty stub.

After removing all functions from errors.jl, the file will be empty. Keep the file (empty or with just a module-level comment) since Phase 10 API cleanup may add proper error types. Alternatively, just leave the file as an empty placeholder.

**For each function removal:** Before deleting, search all `src/` and `test/` files for calls to the function name. Only remove if zero active callers are found.
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to confirm module loads. Run `julia --project test/runtests.jl` to verify all 224 tests pass.
  </verify>
  <done>
All ~35 unused functions removed. Dead structs removed. errors.jl is now a placeholder. Module loads and all 224 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update exports and run final verification</name>
  <files>
    src/QuantumFurnace.jl
  </files>
  <action>
Update the module's export list to remove references to deleted types and functions.

**Exports to remove from src/QuantumFurnace.jl:**
- `LindbladWorkspace` -- struct removed (line 26, in the first export block)
- Any other exports that now reference deleted symbols

**Exports to keep (even though they reference code on the keep list):**
- `OFTCaches` -- still used by time_oft!/trotter_oft!
- `NUFFTPrefactors` -- still active
- `LSIFramework` -- log_sobolev is preserved

Do NOT remove exports of functions that still exist. Be careful to distinguish `LindbladWorkspace` (deleted) from `LindbladianWorkspace` (still alive and used).

After updating exports, do a full verification:
1. `julia --project -e 'using QuantumFurnace; println("Module loaded")'` -- must succeed
2. `julia --project test/runtests.jl` -- 224/224 tests pass
3. Quick sanity: `julia --project -e 'using QuantumFurnace; println(methods(oft!))'` -- oft! exists with the in-place signature
  </action>
  <verify>
Run `julia --project test/runtests.jl` -- all 224 tests pass. Module loads cleanly with no undefined export warnings.
  </verify>
  <done>
Export list is clean -- no references to deleted types/functions. All 224 tests pass. Phase 6 requirements PRUNE-01, PRUNE-02, PRUNE-03 are fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. `julia --project test/runtests.jl` -- 224/224 tests pass
2. `grep -r "LindbladianJumpCaches" src/` -- zero matches
3. `grep -r "LiouvLiouv" src/` -- zero matches
4. `grep -rn "^function oft(" src/` -- zero matches (non-mutating removed)
5. `grep -rn "^function oft!(" src/` -- one match (in-place version preserved)
6. `grep -rn "time_oft!" src/ofts.jl` -- present (preserved per keep list)
7. `grep -rn "trotter_oft!" src/ofts.jl` -- present (preserved per keep list)
8. `grep -r "LindbladWorkspace" src/` -- zero matches (deleted, NOT LindbladianWorkspace)
9. All functions in qi_tools.jl (except are_we_tp) still present
10. linearmaps_liouv.jl untouched
11. log_sobolev.jl untouched
</verification>

<success_criteria>
PRUNE-02 and PRUNE-03 are satisfied: all ~35 unused functions are removed, dead structs (LindbladianJumpCaches, LiouvLiouv) are deleted, non-mutating `oft` wrapper is removed, exports are updated. All 224 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dead-code-pruning/06-02-SUMMARY.md`
</output>
