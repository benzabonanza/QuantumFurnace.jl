---
phase: 06-dead-code-pruning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qi_tools.jl
  - src/energy_domain.jl
  - src/trotter_domain.jl
  - src/coherent.jl
  - src/trajectories.jl
  - src/jump_workers.jl
  - src/errors.jl
  - src/misc_tools.jl
  - src/ofts.jl
  - src/bohr_domain.jl
autonomous: true

must_haves:
  truths:
    - "No commented-out code blocks remain in any source file under src/"
    - "All 224 existing tests pass with no regressions"
    - "TODO/FIXME/HACK comments are preserved (only dead code blocks removed)"
    - "Explanatory comments describing decisions or rationale are preserved"
  artifacts:
    - path: "src/qi_tools.jl"
      provides: "Quantum information utilities with ~46 lines of commented test/benchmark code removed"
    - path: "src/energy_domain.jl"
      provides: "Energy domain with ~137 lines of old commented functions removed"
    - path: "src/trotter_domain.jl"
      provides: "Trotter domain with ~165 lines of commented test scripts removed"
    - path: "src/coherent.jl"
      provides: "Coherent terms with ~172 lines of commented test/obsolete code removed"
    - path: "src/trajectories.jl"
      provides: "Trajectories with ~165 lines of commented old implementations removed"
    - path: "src/jump_workers.jl"
      provides: "Jump workers with ~112 lines of commented old implementations removed"
    - path: "src/errors.jl"
      provides: "Errors with ~42 lines of commented stubs removed"
    - path: "src/misc_tools.jl"
      provides: "Misc tools with ~36 lines of commented old validate_config removed"
    - path: "src/ofts.jl"
      provides: "OFTs with ~18 lines of commented code removed"
    - path: "src/bohr_domain.jl"
      provides: "Bohr domain with ~36 lines of commented code removed"
  key_links:
    - from: "test/runtests.jl"
      to: "src/*.jl"
      via: "Pkg.test()"
      pattern: "224 tests pass"
---

<objective>
Remove all commented-out code blocks (~930 lines) across 9+ source files.

Purpose: Satisfy PRUNE-01 -- eliminate dead commented code that adds noise and maintenance burden. Git history preserves everything.
Output: Clean source files with only live code and explanatory comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dead-code-pruning/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove commented-out code blocks from all source files</name>
  <files>
    src/qi_tools.jl
    src/energy_domain.jl
    src/trotter_domain.jl
    src/coherent.jl
    src/trajectories.jl
    src/jump_workers.jl
    src/errors.jl
    src/misc_tools.jl
    src/ofts.jl
    src/bohr_domain.jl
  </files>
  <action>
Remove all commented-out code blocks from each file. These are multi-line blocks of `#`-prefixed code that were once active -- NOT documentation comments, NOT TODO/FIXME/HACK annotations, NOT explanatory comments about design decisions.

**Preservation policy (from CONTEXT.md locked decisions):**
- PRESERVE all TODO/FIXME/HACK comments (e.g., `#TODO: Reintroduce sigmas here` at coherent.jl:477, `#TODO: test it; set BLAS threads...` at jump_workers.jl:522)
- PRESERVE explanatory comments that describe WHY something was disabled or a design decision
- PRESERVE normal code comments (docstrings, inline explanations of active code)
- REMOVE only commented-out code blocks (old implementations, test scripts, debug code, benchmark code)

**File-by-file removal guide:**

1. **qi_tools.jl** -- Remove lines 224-269 (old test/benchmark code comparing with QuantumOptics)
2. **energy_domain.jl** -- Remove lines 170-306 (8 old commented functions: `transition_gauss_vectorized`, `transition_bohr_gauss_from_energy_vectorized`, 4 `create_alpha_*` variants, 2 `integrate_gamma_*`)
3. **trotter_domain.jl** -- Remove lines 223-388 (old test scripts: Pauli exponentiation tests, Hamiltonian time evolution tests, Trotter parameter sweeps, Fourier label tests)
4. **coherent.jl** -- Remove lines 443-475 (old f_minus/f_plus/f_plus_metro commented functions) AND lines 549-721 (old test/benchmark code). Keep the TODO at line 477 and all active code.
5. **trajectories.jl** -- Remove lines 739-903 (old `evolve_and_measure_along_trajectory`, `measure!`, `KrausFramework`, `krausframework`, `construct_gksl_lindbladian`, old `evolve_along_trajectory`, old `step_along_the_trajectory!`)
6. **jump_workers.jl** -- Remove lines 503-568 (old `verify_completeness`, old `precompute_B`, old `apply_lindbladian!`, old `apply_lindbladian_dagger!`). Keep the TODO at line 522.
7. **errors.jl** -- Remove lines 8-16 (commented `if trotter === nothing` block inside `compute_errors`) and lines 52-92 (old quadrature error test code)
8. **misc_tools.jl** -- Remove lines 51-87 (old `validate_config!` implementation)
9. **ofts.jl** -- Remove lines 118-139 (old `time_oft_integrated` and Trotter OFT check code)
10. **bohr_domain.jl** -- Remove lines 330-366 (old `exact_mask`, `approx_mask`, `transition_bohr_metro_gibbsed` commented functions)

Also clean up separator comments that guarded removed blocks (e.g., `#* ----------------------------------------------------------` lines that now sit at file end with nothing after them).
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to confirm the module still loads. Then run `julia --project test/runtests.jl` to verify all 224 tests pass.
  </verify>
  <done>
All ~930 lines of commented-out code blocks are removed from 10 source files. TODO/FIXME/HACK comments and explanatory comments are preserved. Module loads and all 224 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no commented code blocks remain and run full test suite</name>
  <files>
    src/qi_tools.jl
    src/energy_domain.jl
    src/trotter_domain.jl
    src/coherent.jl
    src/trajectories.jl
    src/jump_workers.jl
    src/errors.jl
    src/misc_tools.jl
    src/ofts.jl
    src/bohr_domain.jl
  </files>
  <action>
Scan all source files for any remaining commented-out code blocks that were missed. Use a heuristic: look for 3+ consecutive lines starting with `#` that contain code patterns (function calls, assignments, loops, variable definitions) rather than documentation.

Exclude from this check:
- Block docstrings
- TODO/FIXME/HACK single-line comments
- Explanatory comments (sentences in English)
- Comment separator lines (`#* ---...`)

If any remaining commented code blocks are found, remove them.

Then run the full test suite one final time to confirm 224/224 pass.
  </action>
  <verify>
Run `julia --project test/runtests.jl` -- all 224 tests pass. Manually scan output of `grep -n "^#" src/*.jl` for any remaining multi-line commented code blocks.
  </verify>
  <done>
Zero commented-out code blocks remain in any source file. All 224 tests pass.
  </done>
</task>

</tasks>

<verification>
1. `julia --project test/runtests.jl` -- 224/224 tests pass
2. No multi-line commented-out code blocks remain in any `src/*.jl` file
3. TODO/FIXME/HACK comments are intact
4. Explanatory comments are intact
5. Total lines removed is approximately ~930
</verification>

<success_criteria>
PRUNE-01 is satisfied: all ~930 lines of commented-out code across 9+ files are removed. The codebase contains only live code and documentation/annotation comments. All 224 tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-dead-code-pruning/06-01-SUMMARY.md`
</output>
