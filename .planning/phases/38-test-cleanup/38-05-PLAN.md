---
phase: 38-test-cleanup
plan: 05
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - test/test_krylov_matvec.jl
  - test/test_krylov_eigsolve.jl
  - test/test_krylov_crossvalidation.jl
autonomous: true

must_haves:
  truths:
    - "Every numerical @test in the 3 Krylov test files has @info with label, value, threshold"
    - "Matvec round-trip tests use loop-summary @info with max_error across random inputs"
    - "Allocation tests in test_krylov_matvec.jl have @info showing bytes and threshold"
    - "Cross-validation tests show Krylov-vs-dense eigenvalue errors and gap comparisons"
    - "Printf-formatted tables in test_krylov_crossvalidation.jl are preserved (not converted to @info)"
    - "All tests pass with no threshold regressions"
  artifacts:
    - path: "test/test_krylov_matvec.jl"
      provides: "@info for matvec round-trips and allocation checks"
      contains: "@info"
    - path: "test/test_krylov_eigsolve.jl"
      provides: "@info for eigsolve accuracy and guard rail tests"
      contains: "@info"
    - path: "test/test_krylov_crossvalidation.jl"
      provides: "@info for cross-validation eigenvalue comparisons"
      contains: "@info"
  key_links:
    - from: "test/test_helpers.jl"
      to: "test/test_krylov_matvec.jl"
      via: "TOL_EXACT for matvec round-trip precision"
      pattern: "TOL_EXACT"
    - from: "test/test_krylov_crossvalidation.jl"
      to: "test/test_helpers.jl"
      via: "make_config with num_qubits=6 for large system tests"
      pattern: "make_config.*num_qubits.*6"
---

<objective>
Add @info output after every numerical @test and review/document thresholds in the 3 Krylov test files: test_krylov_matvec.jl (398 lines, 23 testsets), test_krylov_eigsolve.jl (210 lines), test_krylov_crossvalidation.jl (505 lines).

Purpose: The Krylov tests are the most numerically demanding -- matvec round-trips test matrix-free Lindbladian application against dense reference, eigsolve tests verify iterative eigenvalue accuracy, and cross-validation compares Krylov eigenvalues against exact dense diagonalization. Making their numerical output visible is critical for detecting subtle regressions.
Output: 3 updated test files with @info after numerical assertions and threshold rationale comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-cleanup/38-RESEARCH.md
@.planning/phases/38-test-cleanup/38-01-SUMMARY.md
@test/test_helpers.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @info and threshold rationale to test_krylov_matvec.jl</name>
  <files>test/test_krylov_matvec.jl</files>
  <action>
Read test_krylov_matvec.jl (398 lines, ~26 @tests across 23 testsets) thoroughly. This file tests the matrix-free Krylov matvec (apply_lindbladian!) against dense Liouvillian multiplication.

**Test structure in this file:**
- 16+ round-trip tests: for each domain x construction x coherent combination, applies matvec to random density matrices and checks `norm(v_dense - vec(L_rho)) < threshold`
- These are typically in a loop over 10 random inputs
- Allocation tests: `@test allocs == 0` for hot-path functions

**@info strategy:**

For **round-trip loop tests** (most of this file):
- Use loop-summary pattern: track `max_err = 0.0`, update `max_err = max(max_err, err)` each iteration
- After the loop: `@info "Matvec round-trip (EnergyDomain, KMS)" max_error=max_err n_samples=10 threshold=1e-12`
- This avoids 10x noise per testset

For **allocation tests**:
- `@info "apply_lindbladian! allocations (EnergyDomain)" allocs_bytes=allocs threshold=0`

For **adjoint duality tests** (tr(X' * L(Y)) == tr(L*(X)' * Y)):
- These test a mathematical identity. Threshold should be machine precision * DIM^2.
- `@info "Adjoint duality (EnergyDomain)" max_error=max_err threshold=1e-10`

**Threshold review:**
- **Round-trip norm** (`< 1e-12`): Matvec is algebraically exact (same computation, different code path). Error is pure FP accumulation. Theory: O(n_jumps * DIM^2 * eps) ~ 12 * 256 * 1e-16 ~ 3e-13. Threshold 1e-12 gives 3x margin. Appropriate. Can tighten to 5e-13 if tests pass, but safer to keep 1e-12.
- **Allocation** (`== 0`): Correct -- hot path must be zero-allocation.
- **Adjoint duality**: Similar FP accumulation argument. Current threshold appropriate.
- Add rationale comment to every threshold.

Do NOT restructure the test loops. Only add tracking variables, @info, and rationale comments.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_krylov_matvec.jl` should be >= 15 (one per testset with numerical checks + allocation tests).
  </verify>
  <done>All numerical @tests in test_krylov_matvec.jl have @info (loop-summary for round-trips, individual for allocations). All thresholds have inline rationale comments. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add @info and threshold rationale to test_krylov_eigsolve.jl and test_krylov_crossvalidation.jl</name>
  <files>
test/test_krylov_eigsolve.jl
test/test_krylov_crossvalidation.jl
  </files>
  <action>
**test_krylov_eigsolve.jl (210 lines, ~21 @tests):**

This file tests KrylovKit-based eigensolve accuracy and guard rails.

For each numerical @test:
1. Add @info AFTER with label, value, threshold
2. Add inline rationale

Key test patterns:
- **Eigenvalue accuracy**: Krylov eigenvalue vs dense reference. Threshold depends on KrylovKit tolerance setting. Theory: Krylov eigenvalue error bounded by KrylovKit tol (typically 1e-10 to 1e-12). Threshold should be a small multiple of that tol.
- **Eigenvector accuracy**: residual norm ||L*v - lambda*v||. Same KrylovKit tol reasoning.
- **Spectral gap agreement**: Krylov gap vs dense gap. Error is difference of two eigenvalue estimates.
- **Guard rails**: tests for edge cases (small system, converged check). Some are structural.

Threshold review:
- Krylov tolerances: document the KrylovKit tol used and the safety factor. Example: "KrylovKit tol=1e-10, threshold 1e-8 gives 100x margin for iterative convergence variability"
- Eigenvalue ordering: exact property, no threshold needed (structural check)

**test_krylov_crossvalidation.jl (505 lines, ~4-8 numerical @tests + Printf tables):**

This file contains XVAL-01 through XVAL-04 cross-validation tests.

**CRITICAL (LOCKED DECISION from research pitfall 6):** This file uses Printf-formatted tables for diagnostic comparison output. DO NOT convert these Printf tables to @info. They serve a different purpose (tabular comparison data with alignment). The @info additions are ONLY for the numerical @test assertions.

For each numerical @test:
1. Add @info AFTER with label, value, threshold
2. Add inline rationale

Key test patterns:
- **Krylov vs dense eigenvalue comparison**: `@test abs(krylov_gap - dense_gap) < threshold`. Theory: Krylov error bounded by requested tolerance * condition number. Document both.
- **Relative gap agreement**: `@test abs(krylov_gap - dense_gap) / dense_gap < rel_threshold`.

The n=6 cross-validation tests (already gated behind QUANTUMFURNACE_FULL_TESTS) also need @info for their numerical assertions. These run on larger systems (DIM=64) where thresholds may need different scaling. Document: "n=6 system (DIM=64), Krylov tol=X, threshold scaled for larger system"

Threshold review:
- Cross-validation thresholds typically derive from KrylovKit convergence tolerance. Document the relationship.
- For gated n=6 tests: larger system means more FP accumulation. Threshold may need to be looser. Document rationale.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_krylov_eigsolve.jl` should be >= 6.
`grep -c '@info' test/test_krylov_crossvalidation.jl` should be >= 4.
Verify Printf tables preserved: `grep -c 'Printf\|@printf\|println.*|' test/test_krylov_crossvalidation.jl` should be unchanged from before.
  </verify>
  <done>All numerical @tests in both Krylov test files have @info. Printf tables in test_krylov_crossvalidation.jl are preserved. All thresholds have inline rationale. All tests pass.</done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` passes with zero failures
2. Every numerical @test in all 3 files is followed by @info
3. Loop-based round-trip tests use summary @info (max_error pattern)
4. Printf tables in test_krylov_crossvalidation.jl are unchanged
5. All thresholds have inline rationale comments
6. Allocation tests have @info showing bytes
</verification>

<success_criteria>
- test_krylov_matvec.jl: 15+ @info additions (round-trip summaries + allocations)
- test_krylov_eigsolve.jl: 6+ @info additions (eigenvalue/eigenvector accuracy)
- test_krylov_crossvalidation.jl: 4+ @info additions (Krylov vs dense), Printf tables preserved
- All thresholds documented with theory-based rationale (FP accumulation, KrylovKit tol, system dimension)
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-cleanup/38-05-SUMMARY.md`
</output>
