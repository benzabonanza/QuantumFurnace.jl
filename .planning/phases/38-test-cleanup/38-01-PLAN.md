---
phase: 38-test-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test_helpers.jl
  - test/runtests.jl
  - test/reference/generate_references.jl
  - test/test_allocation.jl
  - test/test_compilation.jl
  - test/test_convergence.jl
  - test/test_cptp.jl
  - test/test_diagnostics.jl
  - test/test_dm_detailed_balance.jl
  - test/test_dm_scaling.jl
  - test/test_gns_trajectory.jl
  - test/test_krylov_crossvalidation.jl
  - test/test_krylov_eigsolve.jl
  - test/test_krylov_matvec.jl
  - test/test_observable_trajectories.jl
  - test/test_regression.jl
  - test/test_results.jl
  - test/test_threading.jl
  - test/test_trajectory_fixes.jl
  - test/test_workspace_independence.jl
  - test/staging/test_gap_estimation.jl
  - test/trajectory_validation/run_trajectory_validation.jl
  - test/trajectory_validation/run_convergence_tests.jl
autonomous: true

must_haves:
  truths:
    - "make_config(Lindbladian(), EnergyDomain()) produces a valid Config identical to the old make_liouv_config(EnergyDomain())"
    - "make_config(Thermalize(), TimeDomain(); num_qubits=3, construction=GNS()) produces a valid Config identical to the old make_small_thermalize_config_gns(TimeDomain())"
    - "make_test_system(; num_qubits=3) returns the same system as the old make_small_test_system()"
    - "N3_HAM, N3_JUMPS, N3_GIBBS, N3_DIM, N3_TROTTER, N3_TROTTER_JUMPS constants exist and equal the old SMALL_* values"
    - "ALL_DOMAINS constant contains all 4 domain singletons"
    - "test/old_tests/ directory is deleted"
    - "trajectory_validation tests are included in runtests.jl behind QUANTUMFURNACE_FULL_TESTS gate"
    - "All tests pass with zero regressions after the consolidation"
  artifacts:
    - path: "test/test_helpers.jl"
      provides: "Unified make_config, make_test_system, N3_* globals, ALL_DOMAINS"
      contains: "function make_config"
    - path: "test/runtests.jl"
      provides: "Updated test runner with trajectory_validation integration"
      contains: "QUANTUMFURNACE_FULL_TESTS"
  key_links:
    - from: "test/test_helpers.jl"
      to: "all test files"
      via: "make_config replaces 8 old factory functions"
      pattern: "make_config\\("
    - from: "test/test_helpers.jl"
      to: "all test files"
      via: "N3_ replaces SMALL_ globals"
      pattern: "N3_HAM|N3_JUMPS|N3_GIBBS|N3_DIM"
---

<objective>
Consolidate test infrastructure: unify 8 config factory functions into single `make_config`, unify 2 system factories into single `make_test_system`, rename SMALL_* globals to N3_*, add ALL_DOMAINS constant, delete old_tests/, integrate trajectory_validation into runtests.jl, and review reference generator.

Purpose: Eliminate duplicate setup patterns and dead test code, creating a single parametrized foundation that all subsequent @info and threshold work builds on.
Output: Consolidated test_helpers.jl, cleaned runtests.jl, deleted old_tests/, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-cleanup/38-RESEARCH.md
@test/test_helpers.jl
@test/runtests.jl
@test/reference/generate_references.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate test_helpers.jl factories and globals</name>
  <files>test/test_helpers.jl</files>
  <action>
Rewrite test_helpers.jl to consolidate all factories and rename globals:

1. **Unify make_test_system**: Replace both `make_test_system()` and `make_small_test_system()` with a single function:
   ```julia
   function make_test_system(; num_qubits=NUM_QUBITS, trotter::Union{Nothing, TrottTrott}=nothing)
   ```
   The body parametrizes by num_qubits (load ham path, set jump_sites=1:num_qubits, etc.). Keep the existing logic, just parametrize. Delete `make_small_test_system`.

2. **Rename SMALL_* to N3_***: All precomputed 3-qubit globals:
   - `SMALL_SYSTEM` -> `N3_SYSTEM`
   - `SMALL_HAM` -> `N3_HAM`
   - `SMALL_JUMPS` -> `N3_JUMPS`
   - `SMALL_GIBBS` -> `N3_GIBBS`
   - `SMALL_DIM` -> `N3_DIM`
   - `SMALL_TROTTER` -> `N3_TROTTER`
   - `SMALL_TROTTER_JUMPS` -> `N3_TROTTER_JUMPS`
   Update the definitions to use the new unified `make_test_system(; num_qubits=3)`. Also rename `make_small_test_trotter()` to just use `TrottTrott(N3_HAM, T0, NUM_TROTTER_STEPS_PER_T0)` inline or a local helper.

3. **Unify make_config**: Replace all 8 factory functions (make_liouv_config, make_liouv_config_gns, make_thermalize_config, make_small_liouv_config, make_small_liouv_config_gns, make_small_thermalize_config, make_small_thermalize_config_gns, plus the duplicate) with a single:
   ```julia
   function make_config(sim, domain;
       num_qubits=NUM_QUBITS,
       construction=KMS(),
       delta=TEST_DELTA,
       mixing_time=1.0,
   )
   ```
   The body should construct Config(...) with the standard locked parameters (BETA, SIGMA, a=BETA/30.0, b=0.4, NUM_ENERGY_BITS, W0, T0, NUM_TROTTER_STEPS_PER_T0). For Thermalize sim type, include mixing_time and delta kwargs. For Lindbladian, do NOT pass mixing_time/delta. Use conditional splatting:
   ```julia
   therm_kw = sim isa Thermalize ? (; mixing_time=mixing_time, delta=delta) : (;)
   Config(sim=sim, domain=domain, construction=construction, num_qubits=num_qubits, ..., therm_kw...)
   ```
   Delete all 8 old factory functions.

4. **Add ALL_DOMAINS constant**:
   ```julia
   const ALL_DOMAINS = [EnergyDomain(), TimeDomain(), TrotterDomain(), BohrDomain()]
   ```

5. Keep the `_load_test_hamiltonian` function unchanged. Keep all tolerance constants (TOL_EXACT, TOL_QUADRATURE, TOL_DELTA) unchanged. Keep physical parameter constants unchanged.

IMPORTANT: Do NOT change any thresholds or add @info in this task -- that is for subsequent plans. This task is purely structural consolidation.
  </action>
  <verify>
Run `julia --project -e 'include("test/test_helpers.jl"); println("make_config: ", typeof(make_config(Lindbladian(), EnergyDomain()))); println("make_config GNS 3q: ", typeof(make_config(Lindbladian(), TrotterDomain(); num_qubits=3, construction=GNS()))); println("make_config Therm: ", typeof(make_config(Thermalize(), EnergyDomain()))); println("N3_HAM: ", typeof(N3_HAM)); println("ALL_DOMAINS: ", ALL_DOMAINS)'`
Verify: no errors, correct types returned, N3_HAM is HamHam, ALL_DOMAINS has 4 elements.
  </verify>
  <done>test_helpers.jl has exactly 1 make_config function, 1 make_test_system function, N3_* globals, ALL_DOMAINS constant, and zero old factory functions remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all test files to new factories and N3_* globals</name>
  <files>
test/test_allocation.jl
test/test_compilation.jl
test/test_convergence.jl
test/test_cptp.jl
test/test_diagnostics.jl
test/test_dm_detailed_balance.jl
test/test_dm_scaling.jl
test/test_gns_trajectory.jl
test/test_krylov_crossvalidation.jl
test/test_krylov_eigsolve.jl
test/test_krylov_matvec.jl
test/test_observable_trajectories.jl
test/test_regression.jl
test/test_results.jl
test/test_threading.jl
test/test_trajectory_fixes.jl
test/test_workspace_independence.jl
test/staging/test_gap_estimation.jl
test/trajectory_validation/run_trajectory_validation.jl
test/trajectory_validation/run_convergence_tests.jl
test/reference/generate_references.jl
  </files>
  <action>
Migrate ALL test files from old factory functions and SMALL_* globals to the new unified API. This is a mechanical search-replace with per-file verification.

**Factory migration rules** (apply to every file):
- `make_liouv_config(domain)` -> `make_config(Lindbladian(), domain)`
- `make_liouv_config(domain; construction=X)` -> `make_config(Lindbladian(), domain; construction=X)`
- `make_liouv_config_gns(domain)` -> `make_config(Lindbladian(), domain; construction=GNS())`
- `make_thermalize_config(domain; delta=d, mixing_time=m)` -> `make_config(Thermalize(), domain; delta=d, mixing_time=m)`
- `make_thermalize_config(domain; construction=X, delta=d, mixing_time=m)` -> `make_config(Thermalize(), domain; construction=X, delta=d, mixing_time=m)`
- `make_small_liouv_config(domain)` -> `make_config(Lindbladian(), domain; num_qubits=3, construction=GNS())`
- `make_small_liouv_config(domain; construction=X)` -> `make_config(Lindbladian(), domain; num_qubits=3, construction=X)`
- `make_small_liouv_config_gns(domain)` -> `make_config(Lindbladian(), domain; num_qubits=3, construction=GNS())`
- `make_small_thermalize_config(domain; ...)` -> `make_config(Thermalize(), domain; num_qubits=3, ...)`
  NOTE: make_small_thermalize_config defaults to construction=GNS() -- preserve this default
- `make_small_thermalize_config_gns(domain; ...)` -> `make_config(Thermalize(), domain; num_qubits=3, construction=GNS(), ...)`

**Global rename rules** (apply to every file):
- `SMALL_SYSTEM` -> `N3_SYSTEM`
- `SMALL_HAM` -> `N3_HAM`
- `SMALL_JUMPS` -> `N3_JUMPS`
- `SMALL_GIBBS` -> `N3_GIBBS`
- `SMALL_DIM` -> `N3_DIM`
- `SMALL_TROTTER` -> `N3_TROTTER`
- `SMALL_TROTTER_JUMPS` -> `N3_TROTTER_JUMPS`

**Special files:**
- `test_dm_detailed_balance.jl`: Has an inline `Config(...)` construction. Replace with `make_config(Lindbladian(), BohrDomain(); num_qubits=3, construction=KMS())`.
- `test_krylov_crossvalidation.jl`: Has local `make_n6_liouv_config`, `make_n6_thermalize_config`, `make_n6_test_system` functions. Replace calls with `make_config(...; num_qubits=6)` and `make_test_system(; num_qubits=6)`. Delete the local factory functions. Keep the n=6 Trotter object creation local (TrottTrott call specific to n=6 Hamiltonian).
- `test/reference/generate_references.jl`: Uses `make_small_liouv_config` and `SMALL_*` constants. Migrate to new names.
- `test/staging/test_gap_estimation.jl`: Uses SMALL_* and old factories. Migrate to new names.
- `test/trajectory_validation/run_trajectory_validation.jl` and `run_convergence_tests.jl`: Use old factories and SMALL_*. Migrate.

Do this file-by-file. Read each file, apply transformations, write back. Do NOT change any test logic, thresholds, or add @info -- purely rename/replace.
  </action>
  <verify>
Run `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e 'using Pkg; Pkg.test()'` -- all tests must pass.
Also verify no old names remain: `grep -r 'make_liouv_config\|make_thermalize_config\|make_small_\|SMALL_HAM\|SMALL_JUMPS\|SMALL_GIBBS\|SMALL_DIM\|SMALL_TROTTER\|SMALL_SYSTEM\|make_n6_' test/ --include='*.jl'` should return zero matches (except inside string literals or comments referencing the migration).
  </verify>
  <done>All 22 test files use make_config() and N3_* globals exclusively. Zero references to old factory names or SMALL_* globals remain. Full test suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Delete old_tests, integrate trajectory_validation, update runtests.jl and reference generator</name>
  <files>
test/old_tests/B_test.jl
test/old_tests/ham_test.jl
test/old_tests/kossakowski_test.jl
test/old_tests/log_sobolev_test.jl
test/old_tests/time_tests.jl
test/old_tests/trajectory_test.jl
test/old_tests/trott_test.jl
test/runtests.jl
test/reference/generate_references.jl
  </files>
  <action>
1. **Delete test/old_tests/ directory entirely**: Remove all 7 files (B_test.jl, ham_test.jl, kossakowski_test.jl, log_sobolev_test.jl, time_tests.jl, trajectory_test.jl, trott_test.jl) and the directory itself. Use `rm -rf test/old_tests/`.

2. **Integrate trajectory_validation into runtests.jl**: Add at the end of the `@testset "QuantumFurnace.jl"` block, AFTER all existing includes:
   ```julia
   # Trajectory validation (slow: ~minutes, gated behind env variable)
   if get(ENV, "QUANTUMFURNACE_FULL_TESTS", "false") == "true"
       include("trajectory_validation/run_trajectory_validation.jl")
       include("trajectory_validation/run_convergence_tests.jl")
   else
       @info "Skipping trajectory validation tests (set QUANTUMFURNACE_FULL_TESTS=true to run)"
   end
   ```
   Note: run_convergence_tests.jl already has its own internal QUANTUMFURNACE_FULL_TESTS gate. That is fine -- the outer gate in runtests.jl prevents loading these files at all during normal testing, saving compilation time. The inner gate in run_convergence_tests.jl is for when it is run standalone.

3. **Review reference generator**: After Task 2 migrates generate_references.jl to new names, verify it still works by reading the file and confirming:
   - It uses `make_config(Lindbladian(), domain; num_qubits=3, construction=...)` instead of `make_small_liouv_config`
   - It uses `N3_TROTTER`, `N3_TROTTER_JUMPS`, `N3_JUMPS`, `N3_HAM`, `N3_DIM` instead of SMALL_*
   - The `construct_lindbladian` API call still matches current signatures (it should -- Phase 37 did not change this)
   - Do NOT regenerate the BSON files (they are frozen references)
  </action>
  <verify>
`ls test/old_tests/ 2>&1` should show "No such file or directory".
`grep -c 'trajectory_validation' test/runtests.jl` should be >= 1.
`grep -c 'QUANTUMFURNACE_FULL_TESTS' test/runtests.jl` should be >= 1.
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass (trajectory validation skipped with @info message).
  </verify>
  <done>test/old_tests/ deleted. trajectory_validation integrated into runtests.jl behind QUANTUMFURNACE_FULL_TESTS gate. Reference generator uses new API names. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` passes with zero failures
2. `grep -rn 'make_liouv_config\|make_small_\|SMALL_HAM\|SMALL_JUMPS\|SMALL_GIBBS\|SMALL_DIM\|SMALL_TROTTER\b\|SMALL_SYSTEM\|make_n6_' test/ --include='*.jl'` returns zero matches
3. `grep -c 'function make_config' test/test_helpers.jl` returns 1
4. `grep -c 'function make_test_system' test/test_helpers.jl` returns 1
5. `test -d test/old_tests && echo "FAIL: old_tests exists" || echo "PASS: old_tests deleted"`
6. `grep 'QUANTUMFURNACE_FULL_TESTS' test/runtests.jl` shows trajectory validation gate
</verification>

<success_criteria>
- Single `make_config(sim, domain; kwargs...)` factory replaces all 8 old factories
- Single `make_test_system(; num_qubits=N)` factory replaces both old system factories
- SMALL_* renamed to N3_* across all 22 test files (157 references migrated)
- ALL_DOMAINS constant available in test_helpers.jl
- test/old_tests/ directory deleted (7 dead files removed)
- trajectory_validation integrated into runtests.jl behind FULL test gate
- n=6 factories in test_krylov_crossvalidation.jl merged into unified make_config/make_test_system
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-cleanup/38-01-SUMMARY.md`
</output>
