---
phase: 38-test-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - test/test_dm_scaling.jl
  - test/test_gns_trajectory.jl
  - test/test_observable_trajectories.jl
  - test/test_results.jl
autonomous: true

must_haves:
  truths:
    - "Every numerical @test in these 4 files is followed by @info showing label, computed value, and threshold"
    - "Scaling ratio tests (O(delta^2), O(delta)) have @info showing ratio, expected value, and bounds"
    - "Statistical trajectory tests have @info showing trace distance and the 1/sqrt(N) rationale"
    - "test_results.jl has no @info additions (all round-trip serialization tests are exact equality)"
    - "All tests pass with no threshold regressions"
  artifacts:
    - path: "test/test_dm_scaling.jl"
      provides: "@info for Euler/OFT/NUFFT scaling tests with ratio diagnostics"
      contains: "@info"
    - path: "test/test_gns_trajectory.jl"
      provides: "@info for GNS trajectory trace distance and convergence tests"
      contains: "@info"
  key_links:
    - from: "test/test_helpers.jl"
      to: "these test files"
      via: "TOL_DELTA, TEST_DELTA tolerance constants"
      pattern: "TOL_DELTA|TEST_DELTA"
---

<objective>
Add @info output after every numerical @test and review/document thresholds in 4 test files: test_dm_scaling, test_gns_trajectory, test_observable_trajectories, test_results.

Purpose: These files contain important scaling tests (O(delta^2) Euler convergence, OFT/NUFFT accuracy) and trajectory statistics that benefit from visible numerical output. The @info additions make test output self-documenting.
Output: 4 updated test files with @info after numerical assertions and threshold rationale comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-cleanup/38-RESEARCH.md
@.planning/phases/38-test-cleanup/38-01-SUMMARY.md
@test/test_helpers.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @info and threshold rationale to test_dm_scaling and test_gns_trajectory</name>
  <files>
test/test_dm_scaling.jl
test/test_gns_trajectory.jl
  </files>
  <action>
**test_dm_scaling.jl (268 lines, ~12 numerical tests):**

This file contains DMTST-03 through DMTST-06b -- scaling tests that verify O(delta^2) single-step Euler error, O(delta) accumulated error, coherent term (B) cross-domain consistency, and OFT cross-domain consistency.

For each numerical @test:
1. Add @info AFTER the assertion with label, computed value, and threshold
2. Add inline rationale comment explaining the threshold

Key test patterns in this file:
- **Ratio tests** (`@test 3.0 <= ratio <= 5.0`): These verify O(delta^2) convergence where exact ratio is 4.0. Bounds allow for sub-leading terms. @info should show: `@info "DMTST-03: Euler single-step ratio" ratio=ratio expected=4.0 lower=3.0 upper=5.0`
- **Trace distance tests** (`@test dist < threshold`): Show the distance and threshold.
- **Accumulated error ratios** (`@test 1.5 <= ratio <= 2.5`): O(delta) accumulated, exact ratio 2.0.
- **B/OFT consistency** (cross-domain norm comparisons): document which domains should match exactly vs approximately.

Review thresholds:
- Ratio bounds [3.0, 5.0] for O(delta^2): Theory says exact 4.0, bounds are +/-25%. This is reasonable for finite delta with sub-leading O(delta^3) terms. Keep unless tests show tighter is safe.
- Ratio bounds [1.5, 2.5] for O(delta): Theory says exact 2.0. Same reasoning. Keep.
- Any println() diagnostic output in this file should be converted to @info with keyword arguments per the locked decision. But KEEP Printf-formatted tables if they exist (pitfall 6 from research).

**test_gns_trajectory.jl (122 lines, ~14 numerical tests, 4 @info already present):**

This file contains GNS-01 (trace distance to Gibbs after DM evolution) and GNS-02 (trajectory vs DM cross-validation) tests.

For each numerical @test without an existing @info:
1. Add @info AFTER the assertion
2. Add inline rationale comment

Key patterns:
- **Trace distance to Gibbs** after DM evolution: GNS gives approximate detailed balance, so threshold is looser than KMS. Document: "GNS approximate DB: error scales with system-dependent approximation quality"
- **Trajectory vs DM** distance: statistical noise from trajectory averaging. Document: "N_traj trajectories, expected error ~ 1/sqrt(N_traj), threshold gives Mx margin"
- Already has 4 @info -- preserve those, add missing ones.

Threshold review for GNS tests:
- GNS trace distance to Gibbs: depends on the smoothing parameter choices (a, b, sigma). These are NOT machine precision. Theory: GNS detailed balance error is O(smoothing), system-dependent. Current thresholds likely empirical -- keep but document as such.
- Trajectory vs DM: statistical, 1/sqrt(N) scaling. Document the N_traj used and the margin.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_dm_scaling.jl` should be >= 6.
`grep -c '@info' test/test_gns_trajectory.jl` should be >= 6.
  </verify>
  <done>All numerical @tests in test_dm_scaling.jl and test_gns_trajectory.jl have @info after them. All thresholds have inline rationale comments. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add @info and threshold rationale to test_observable_trajectories and test_results</name>
  <files>
test/test_observable_trajectories.jl
test/test_results.jl
  </files>
  <action>
**test_observable_trajectories.jl (85 lines, ~18 @tests but most structural):**

This file tests the observable-only trajectory runner. Many tests are structural (type checks, field existence, array dimensions).

For each @test:
- **Numerical** (isapprox with atol, < threshold): Add @info + rationale
- **Structural** (@test x isa Type, @test length(x) == N, @test hasfield, dimension checks): Skip @info

Key numerical tests likely include:
- Observable convergence: trajectory-averaged observable vs reference. Statistical noise, 1/sqrt(N) scaling.
- Any trace preservation checks.

Review thresholds:
- Statistical thresholds: document N_traj and margin
- Exact preservation: machine precision + accumulation

**test_results.jl (215 lines, ~10 @tests):**

This file tests Result type serialization round-trips. Per the research inventory, these are ALL exact round-trip tests (atol=0 or exact equality). The research says "~0 @info needed" for this file.

Review each @test:
- `isapprox(loaded.field, original.field; atol=0)` -- exact round-trip, structural. Skip @info.
- `@test loaded.config == original.config` -- exact equality, structural. Skip @info.
- If ANY test uses a non-zero atol or a < threshold, add @info + rationale.
- Otherwise, this file gets zero @info additions (per locked decision: skip @info for structural/exact checks).

Document in a comment at the top of the file: "# All tests in this file are exact round-trip checks; no @info needed per test output policy"
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_observable_trajectories.jl` should be >= 1 (at least for statistical checks).
Review test_results.jl: confirm no @info was added inappropriately for exact equality checks.
  </verify>
  <done>All numerical @tests in test_observable_trajectories.jl have @info. test_results.jl correctly has no @info additions (all exact round-trips). All thresholds have inline rationale comments. All tests pass.</done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` passes with zero failures
2. Every numerical @test in all 4 files is followed by @info (manual review of diff)
3. Every threshold has an inline comment (no bare numeric literals without rationale)
4. No @info for exact equality or structural checks
5. Scaling ratio @info shows ratio, expected value, and bounds
</verification>

<success_criteria>
- test_dm_scaling.jl: all ratio tests and trace distance tests have @info with computed values
- test_gns_trajectory.jl: all numerical tests have @info (building on 4 existing)
- test_observable_trajectories.jl: statistical tests have @info, structural tests do not
- test_results.jl: zero @info additions (correct -- all exact round-trips)
- Every threshold has inline rationale comment
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-cleanup/38-03-SUMMARY.md`
</output>
