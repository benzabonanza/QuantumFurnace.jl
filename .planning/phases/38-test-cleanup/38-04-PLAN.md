---
phase: 38-test-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - test/test_diagnostics.jl
  - test/test_convergence.jl
autonomous: true

must_haves:
  truths:
    - "Every numerical @test in test_diagnostics.jl (525 lines) has @info with label, value, threshold"
    - "Every numerical @test in test_convergence.jl (763 lines) has @info with label, value, threshold"
    - "Structural/type checks in both files have no @info"
    - "All thresholds have inline rationale comments explaining theory-based or empirical justification"
    - "All tests pass with no threshold regressions"
  artifacts:
    - path: "test/test_diagnostics.jl"
      provides: "@info after numerical DIAG-01 through DIAG-06 tests"
      contains: "@info"
    - path: "test/test_convergence.jl"
      provides: "@info after numerical convergence tests (CONV series)"
      contains: "@info"
  key_links:
    - from: "test/test_helpers.jl"
      to: "test/test_diagnostics.jl"
      via: "N3_HAM, N3_JUMPS, N3_GIBBS for exact diagnostics"
      pattern: "N3_HAM|N3_JUMPS|N3_GIBBS"
---

<objective>
Add @info output after every numerical @test and review/document thresholds in the two longest test files: test_diagnostics.jl (525 lines) and test_convergence.jl (763 lines).

Purpose: These are the most complex test files in the suite. test_diagnostics.jl has ~47 @tests (many structural, ~15 numerical) testing exact eigenvalue computations, spectral gaps, and Gibbs state diagnostics. test_convergence.jl has ~54 @tests (~30 numerical) testing convergence tracking, batch statistics, and adaptive sampling. Both benefit enormously from visible numerical output.
Output: 2 updated test files with comprehensive @info and threshold documentation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-cleanup/38-RESEARCH.md
@.planning/phases/38-test-cleanup/38-01-SUMMARY.md
@test/test_helpers.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @info and threshold rationale to test_diagnostics.jl</name>
  <files>test/test_diagnostics.jl</files>
  <action>
Read test_diagnostics.jl (525 lines) thoroughly. This file contains DIAG-01 through DIAG-06 exact diagnostic tests.

**Classification of tests in this file:**
- **Structural** (skip @info): type checks (`isa`), field existence, boolean flags, string comparisons, dimension checks
- **Numerical** (add @info): eigenvalue comparisons, spectral gap values, trace distance to Gibbs, norm comparisons, isapprox with atol

**For each numerical @test:**
1. Add `@info "DIAG-XX: description" value=computed threshold=used_threshold` AFTER the @test
2. Add inline rationale comment

**Threshold categories in this file:**
- **Eigenvalue accuracy** (exact diagonalization of small 3-qubit system): Should be machine precision. Theory: eigen() on DIM=8 matrix, error O(DIM * eps) ~ 8e-15. Threshold 1e-12 gives ample margin. Can tighten to 1e-13 if tests pass.
- **Trace distance to Gibbs** (fixed point comparison): KMS construction = exact, machine precision. GNS construction = approximate, system-dependent.
- **Spectral gap** (second eigenvalue of Lindbladian): Should match dense eigenvalue to machine precision for small systems.
- **Norm of matrices** (Lindbladian matrix elements, coherent term): Theory-dependent. Document what each norm represents physically.

**Loop-based tests:**
- If tests iterate over domains or jump operators, use the loop-summary @info pattern: track max_err, emit one @info after loop.

**Important nuance for diagnostics:**
- DIAG tests compute exact references (dense eigendecomposition) and compare against. These are effectively round-trip tests between two computation methods. The threshold should reflect floating-point accumulation in both methods, NOT an approximation error. Document this distinction.

Review all ~47 @tests, add @info to ~15 numerical ones, add rationale to all thresholds. Do NOT change test logic or assertion structure.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_diagnostics.jl` should be >= 10.
Verify no @info appears for structural checks: manual review that @info only follows numerical @test lines.
  </verify>
  <done>All ~15 numerical @tests in test_diagnostics.jl have @info after them. All ~47 thresholds have inline rationale comments. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add @info and threshold rationale to test_convergence.jl</name>
  <files>test/test_convergence.jl</files>
  <action>
Read test_convergence.jl (763 lines) thoroughly. This is the largest test file, containing CONV-series convergence tracking tests.

**Classification of tests in this file:**
- **Structural** (skip @info): type checks, field existence, array length checks, boolean convergence flags, haskey checks, Dict structure checks
- **Numerical** (add @info): batch statistics (mean, std), convergence rates, trace distance comparisons, isapprox checks

**For each numerical @test:**
1. Add `@info "CONV-XX: description" value=computed threshold=used_threshold` AFTER the @test
2. Add inline rationale comment

**Threshold categories in this file:**
- **Batch statistics accuracy** (mean of batched runs): Statistical, scales as 1/sqrt(N_batches). Document N and margin.
- **Convergence rate** (exponential decay rate estimation): Fitting accuracy, system-dependent. Document what "converged" means in terms of the rate.
- **Trace distance** (converged state vs reference): Combination of statistical noise and systematic error. Document both contributions.
- **Timing bounds** (adaptive sampling, not-too-slow checks): These are wall-clock timing checks. They are inherently noisy. Document: "timing-based, system-dependent, loose bounds intentional"

**Special patterns in this file:**
- This file likely has gated sections behind QUANTUMFURNACE_FULL_TESTS. Only add @info to the always-run sections unless the gated sections also have numerical @tests.
- Long convergence tracking sections may have many structural checks (checking ConvergenceData fields, batch_sizes arrays, etc.) -- skip @info for all of these.
- If the file uses println() for diagnostic output, convert numerical diagnostics to @info. Keep any tabular println output as-is (pitfall 6).

**Threshold tightening considerations:**
- Convergence tests are inherently statistical. Do NOT aggressively tighten statistical thresholds.
- Theory-based tightening applies only to deterministic sub-tests (e.g., DM evolution part of convergence).
- For statistical tests: document the expected error, N_traj, and margin factor.

Review all ~54 @tests, add @info to ~30 numerical ones, add rationale to all thresholds.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_convergence.jl` should be >= 10.
  </verify>
  <done>All ~30 numerical @tests in test_convergence.jl have @info after them. All thresholds have inline rationale comments. All tests pass.</done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` passes with zero failures
2. Every numerical @test in both files is followed by @info
3. Every threshold has an inline rationale comment
4. Structural/type checks have no @info
5. Loop-based tests use summary @info pattern
</verification>

<success_criteria>
- test_diagnostics.jl: ~15 numerical tests have @info, ~32 structural tests do not
- test_convergence.jl: ~30 numerical tests have @info, ~24 structural tests do not
- All thresholds documented with theory-based or empirical rationale
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-cleanup/38-04-SUMMARY.md`
</output>
