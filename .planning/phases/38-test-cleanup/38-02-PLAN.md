---
phase: 38-test-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - test/test_compilation.jl
  - test/test_cptp.jl
  - test/test_dm_detailed_balance.jl
  - test/test_trajectory_fixes.jl
  - test/test_regression.jl
  - test/test_allocation.jl
  - test/test_workspace_independence.jl
  - test/test_threading.jl
autonomous: true

must_haves:
  truths:
    - "Every numerical @test in these 8 files is followed by @info showing label, computed value, and threshold"
    - "Structural/type checks (isa, haskey, boolean equality) have no @info"
    - "Every threshold has an inline comment explaining its rationale (theory-based or empirical with scaling note)"
    - "All tests pass with no threshold regressions"
  artifacts:
    - path: "test/test_allocation.jl"
      provides: "@info after every allocation @test, threshold rationale comments"
      contains: "@info"
    - path: "test/test_cptp.jl"
      provides: "Loop-summary @info pattern for CPTP completeness checks"
      contains: "@info"
    - path: "test/test_dm_detailed_balance.jl"
      provides: "@info for trace distance and domain hierarchy tests"
      contains: "@info"
  key_links:
    - from: "test/test_helpers.jl"
      to: "these 8 test files"
      via: "TOL_EXACT, TOL_QUADRATURE, TOL_DELTA tolerance constants"
      pattern: "TOL_EXACT|TOL_QUADRATURE|TOL_DELTA"
---

<objective>
Add @info output after every numerical @test and review/document thresholds in 8 shorter test files: test_compilation, test_cptp, test_dm_detailed_balance, test_trajectory_fixes, test_regression, test_allocation, test_workspace_independence, test_threading.

Purpose: Make test output self-documenting -- every numerical check shows what was measured, the computed value, and the threshold it was compared against. Thresholds get inline rationale so future maintainers understand why each value was chosen.
Output: 8 updated test files with @info after numerical assertions and threshold rationale comments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-cleanup/38-RESEARCH.md
@.planning/phases/38-test-cleanup/38-01-SUMMARY.md
@test/test_helpers.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @info and threshold rationale to test_compilation, test_cptp, test_dm_detailed_balance, test_trajectory_fixes</name>
  <files>
test/test_compilation.jl
test/test_cptp.jl
test/test_dm_detailed_balance.jl
test/test_trajectory_fixes.jl
  </files>
  <action>
For each of these 4 files, read the file, identify every numerical @test, and apply the following pattern:

**@info placement rule (LOCKED DECISION):**
- Place @info AFTER the @test assertion, never before
- Pattern: `@info "label" value=computed_value threshold=threshold_used`
- Only for numerical comparisons (trace distance, norm, allocation, ratio, isapprox with atol/rtol)
- Skip @info for structural checks (@test x isa Type, @test haskey(...), @test x == y for booleans, @test_throws)

**Loop-based tests (LOCKED DECISION from research pitfall 5):**
- For tests inside loops (e.g., CPTP completeness across jumps), compute max/summary stats and emit ONE @info AFTER the loop
- Example: track `max_err` across loop iterations, emit `@info "CPTP completeness" n_jumps=N max_error=max_err threshold_atol=1e-10` after the loop

**Threshold review for each file:**

test_compilation.jl (~3 numerical tests):
- Size checks (DIM, DIM) -- structural, skip @info
- Field value checks (delta == TEST_DELTA) -- exact equality, skip @info
- Any isapprox or < threshold -- add @info + rationale comment

test_cptp.jl (~3 numerical tests, in loops):
- CPTP completeness: `isapprox(completeness, identity; atol=1e-10)` -- algebraic identity, 1e-10 allows FP accumulation across DIM^2 entries. Use loop-summary pattern with max_err.
- Review: 1e-10 is appropriate for algebraic identity (K0'K0 + delta*R + U'U = I). Theory: error scales with DIM^2 * eps ~ 16^2 * 1e-16 ~ 3e-13, so 1e-10 gives 300x margin. Keep or tighten to 1e-11 if empirically justified. Add rationale comment.

test_dm_detailed_balance.jl (~4 numerical tests, 2 @info already exist):
- DMTST-01: trace_distance < threshold for Gibbs fixed point. KMS construction: Gibbs is exact fixed point (machine precision), threshold should be O(DIM * eps). GNS construction: approximate detailed balance, threshold must be looser.
- DMTST-02: domain hierarchy distances. Already has @info. Review thresholds for domain ordering checks.
- Any new numerical tests added since research -- check and add @info.
- Document rationale: "KMS: exact DB, machine precision" vs "GNS: approximate DB, looser bound"

test_trajectory_fixes.jl (~4-6 numerical tests):
- TFIX tests: trace distance, norm comparisons. Review each threshold. These are single-step trajectory checks -- error should be O(delta) or O(delta^2) depending on the specific check.
- Add @info after each numerical assertion.
- Add rationale: reference the error scaling (single-step Euler, CPTP completeness, etc.)

**Threshold tightening approach (LOCKED DECISION):**
- Theory-based first: O(delta), O(delta^2), machine epsilon * DIM, known error scaling
- If empirical: note "empirical, validated at N=3,4 qubit systems"
- Every threshold gets an inline comment explaining WHY that value
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
Verify @info count: `grep -c '@info' test/test_cptp.jl` should be >= 1, `grep -c '@info' test/test_trajectory_fixes.jl` should be >= 2.
Verify no @info before @test: `grep -B1 '@test ' test/test_cptp.jl test/test_dm_detailed_balance.jl test/test_trajectory_fixes.jl` -- @info should not appear in the line immediately before @test.
  </verify>
  <done>All numerical @tests in these 4 files have @info after them. All thresholds have inline rationale comments. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add @info and threshold rationale to test_regression, test_allocation, test_workspace_independence, test_threading</name>
  <files>
test/test_regression.jl
test/test_allocation.jl
test/test_workspace_independence.jl
test/test_threading.jl
  </files>
  <action>
For each of these 4 files, read the file, identify every numerical @test, and apply the @info + threshold rationale pattern.

**test_regression.jl (~4 numerical tests):**
- TINF-02: DM regression vs frozen BSON reference. `isapprox(rho_fresh, rho_ref; atol=1e-10)`. Rationale: deterministic DM evolution (exp(delta*L)) matches to machine precision across platforms. 1e-10 gives margin for FP accumulation in matrix exponential.
- Trajectory regression: `isapprox(rho_traj, rho_dm; atol=0.05)`. Rationale: 1000 trajectories, expected statistical error ~ 1/sqrt(1000) * norm_factor ~ 0.03, threshold 0.05 gives ~1.6x margin.
- Add @info after each: `@info "TINF-02: DM regression" max_error=... threshold_atol=...`

**test_allocation.jl (~7 numerical tests):**
- Allocation checks: `@test allocs == 0` or `@test allocs <= BUDGET`. These ARE numerical. Add @info.
- Pattern: `@info "apply_lindbladian! allocations (EnergyDomain)" allocs_bytes=allocs threshold=0`
- Budget-based checks: document the MATVEC_ALLOC_BUDGET rationale (Union{Nothing,Function} boxing ~300 bytes).
- Review: any allocation test that checks `== 0` for a hot path is correct. Any that checks a budget -- verify the budget formula is still accurate.

**test_workspace_independence.jl (~4-7 numerical tests):**
- Workspace isolation: typically bitwise equality checks (ws1.field == ws2.field) after independent construction. These are structural if exact equality. Skip @info for exact equality.
- Any isapprox or threshold-based check -- add @info + rationale.
- Review which checks are genuinely numerical (approximate) vs structural (exact).

**test_threading.jl (~3 numerical tests + skip messages):**
- Threading determinism: `result1.rho_mean == result2.rho_mean` -- bitwise exact, skip @info.
- Speedup test: `@test t_threaded < t_serial` -- timing comparison, add @info showing both times.
- BLAS thread restoration: structural check, skip.
- Already has ~5 @info skip messages. Leave those. Add @info only for NEW numerical assertions.
- Note: speedup tests are inherently noisy. Document: "timing comparison, system-dependent, no fixed threshold"

**Same threshold approach as Task 1:**
- Theory-based tightening first
- Inline rationale comment for every threshold
- @info AFTER @test, keyword arguments format
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass.
`grep -c '@info' test/test_allocation.jl` should be >= 5.
`grep -c '@info' test/test_regression.jl` should be >= 2.
  </verify>
  <done>All numerical @tests in these 4 files have @info after them. All thresholds have inline rationale comments. All tests pass.</done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` passes with zero failures
2. Every numerical @test in all 8 files is followed by @info (manual review of diff)
3. Every threshold has an inline comment (grep for bare numeric literals without comments)
4. No @info appears for structural/type checks
5. Loop-based numerical tests use summary @info pattern (not per-iteration)
</verification>

<success_criteria>
- All 8 files have @info after every numerical @test assertion
- Zero @info for structural/type/boolean checks
- Every threshold comparison has an inline rationale comment
- Loop-based tests use max/summary @info pattern
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-cleanup/38-02-SUMMARY.md`
</output>
