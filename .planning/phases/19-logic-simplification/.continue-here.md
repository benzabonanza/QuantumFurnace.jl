---
phase: 19-logic-simplification
task: 0
total_tasks: 8
status: planned
last_updated: 2026-02-16T14:10:51.051Z
---

<current_state>
Phase 19 planning is COMPLETE. All 3 plans created, researched, verified (passed after 1 revision iteration). No execution has started yet.
</current_state>

<completed_work>

- Research: 19-RESEARCH.md written (call chain analysis, jump basis 6 call sites, result struct hierarchy)
- Plan 19-01: Jump basis fix (wave 1, 3 tasks, 13 files) -- domain-aware JumpOp construction, remove internal transforms
- Plan 19-02: Call chain flattening (wave 2, 2 tasks, 2 files) -- inline _evolve_along_trajectory!, lift framework building
- Plan 19-03: Result struct simplification (wave 3, 3 tasks, 10 files) -- rename structs, embed ConvergenceData
- Verification: Passed on iteration 2 (blocker in 19-01 fixed: test helpers need trotter-basis JumpOps for TrotterDomain tests)
- Context: 19-CONTEXT.md gathered from /gsd:discuss-phase (locked decisions on all 3 targets)
</completed_work>

<remaining_work>

- Execute Phase 19: `/gsd:execute-phase 19`
  - Wave 1: Plan 19-01 (jump basis fix)
  - Wave 2: Plan 19-02 (call chain flattening)
  - Wave 3: Plan 19-03 (result struct simplification)
- All 3 waves are sequential (each depends on the previous)
</remaining_work>

<decisions_made>

- Jump basis: Fix at JumpOp construction source, not downstream transforms (user decision)
- Test helpers: Add optional `trotter` kwarg to make_test_system/make_small_test_system, create SMALL_TROTTER_JUMPS/TEST_TROTTER_JUMPS constants (revision fix for blocker)
- Call chain: Keep step_along_trajectory! as-is (hot path), inline _evolve_along_trajectory!, extract framework building
- Result structs: HotSpectralResults -> LindbladianResult (4 fields), HotAlgorithmResults -> DMSimulationResult (3 fields), embed ConvergenceData in TrajectoryResult
- Convergence runners: Return single TrajectoryResult instead of (TrajectoryResult, ConvergenceData) tuple
- Keep times/measurements_mean in TrajectoryResult (test_threading.jl uses them)
- Keep transform_jumps_to_basis as public API export, just remove internal callers
</decisions_made>

<blockers>
None
</blockers>

<context>
Phase 19 is a pure simplification phase -- no new capabilities. Three targets: jump basis transforms (6 call sites), call chain depth (5->3 levels), result struct hierarchy (rename + slim + embed convergence). All 539 existing tests must pass. The plans are sequential: basis fix first (enables cleaner code), then call chain (changes how results flow), then structs (changes what is returned). v1.2 milestone is complete; this is post-milestone cleanup.
</context>

<next_action>
Start with: `/gsd:execute-phase 19` to execute all 3 plans in wave order.
Clear context first (`/clear`) for fresh window.
</next_action>
