---
phase: 31-scaling-benchmarks
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - results/krylov_scaling_report.md
  - simulations/main_krylov_benchmark.jl
autonomous: true

must_haves:
  truths:
    - "Full benchmark executes successfully for n=3,4,5,6 on both EnergyDomain and TrotterDomain KMS"
    - "Power-law scaling fit produces b in [3.5, 4.5] for both domains (hard assertion passes)"
    - "Markdown report exists at results/krylov_scaling_report.md with timing tables, scaling fits, and extrapolation"
    - "Report contains predicted wall-clock and memory for n=10 and n=12 with feasibility notes"
    - "Memory data from @allocated is recorded for each system size in the report"
  artifacts:
    - path: "results/krylov_scaling_report.md"
      provides: "Krylov scaling benchmark report with timing, memory, scaling fits, and extrapolation"
      min_lines: 30
  key_links:
    - from: "simulations/main_krylov_benchmark.jl"
      to: "results/krylov_scaling_report.md"
      via: "script execution produces report"
      pattern: "krylov_scaling_report"
---

<objective>
Execute the complete Krylov scaling benchmark and verify the output report contains valid timing data, correct scaling fits, and useful extrapolation.

Purpose: Produce the empirical scaling data that establishes the 4^n law and provides resource estimates for n=10,12 production runs. This completes Phase 31 and v1.5.

Output: `results/krylov_scaling_report.md` -- generated benchmark report
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-scaling-benchmarks/31-01-SUMMARY.md
@.planning/phases/31-scaling-benchmarks/31-RESEARCH.md
@simulations/main_krylov_benchmark.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute the full benchmark and capture output</name>
  <files>results/krylov_scaling_report.md</files>
  <action>
Run the complete benchmark script from the project root:

```bash
julia --project=@. simulations/main_krylov_benchmark.jl
```

**Expected behavior:**
- Script prints header, then iterates through n=3,4,5,6 for EnergyDomain, printing timing rows
- At n=6, the krylovdim probe runs first (dense 4096x4096 eigen, then Krylov comparison)
- After EnergyDomain, scaling fit is printed with the b value
- If n=7 is predicted to be under 15 minutes, a single n=7 run executes
- TrotterDomain benchmarks follow the same pattern
- Report is written to `results/krylov_scaling_report.md`

**Timeout considerations:**
- n=3,4,5 should each take seconds
- n=6 may take 1-5 minutes (including krylovdim probe with dense eigen)
- n=7 (if attempted) may take 5-15 minutes
- Total expected runtime: 10-30 minutes for both domains
- Set a generous timeout (use the Bash tool with `timeout 600000` for 10 minutes, or run in background if needed)

**If the script fails:**
- Read the error message carefully
- Common issues: OOM at n=6/7 (reduce krylovdim), convergence failure (increase maxiter), path issues
- Fix the script and re-run
- If n=7 causes OOM or timeout, ensure the script's conditional logic skips it gracefully

**If the scaling assertion fails (b outside [3.5, 4.5]):**
- This is a genuine finding -- do NOT weaken the assertion
- Investigate: print the individual timing data points to identify outliers
- Check if n=3 timing is anomalously low (JIT warmup issue) or n=6 anomalously high (krylovdim retry overhead)
- Fix the root cause (e.g., improve warmup, adjust krylovdim) and re-run

**Capture stdout** to verify the benchmark ran correctly.
  </action>
  <verify>
1. `results/krylov_scaling_report.md` exists
2. The report contains timing data for at least n=3,4,5,6
3. The report contains scaling fit parameters (a, b) for both EnergyDomain and TrotterDomain
4. b is in [3.5, 4.5] (the assertion passed)
5. The report contains extrapolation for n=10 and n=12
6. No errors in stdout/stderr (warnings about memory are acceptable)

```bash
cat results/krylov_scaling_report.md
```
  </verify>
  <done>
Full benchmark completed successfully. Report at `results/krylov_scaling_report.md` contains timing tables for n=3-6 (both domains), scaling fits with b in [3.5, 4.5], memory data, and extrapolation to n=10,12.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify report quality and fix any issues</name>
  <files>results/krylov_scaling_report.md, simulations/main_krylov_benchmark.jl</files>
  <action>
Review the generated report for completeness and correctness:

1. **Timing data sanity:** Verify that median times increase roughly 4x per qubit added (n to n+1). If the ratio is wildly different (e.g., 2x or 16x), investigate.

2. **Memory data sanity:** Verify that @allocated values grow with n. Compare against the pre-flight estimate `krylovdim * 4^n * 16 * 1.5`. Note any discrepancy in the report's memory guard calibration section.

3. **Scaling fit quality:** The fit should have b close to 4.0. If b is at the edge of [3.5, 4.5], note this but do not change the assertion bounds.

4. **Extrapolation reasonableness:**
   - n=10 EnergyDomain: should predict minutes to hours (krylov vectors ~838 MB with kd=50)
   - n=12 EnergyDomain: should predict hours to days (krylov vectors ~13.4 GB with kd=50)
   - TrotterDomain n=10: should note NUFFT prefactors make it infeasible (~34 GB)

5. **Report formatting:** Ensure markdown tables render correctly, numbers are formatted consistently, and sections are clearly separated.

6. **If any issues found:** Fix the benchmark script and re-run, or manually adjust the report if the issue is purely cosmetic.

7. **Print a brief summary** of the key results to stdout for the user's reference:
   - EnergyDomain scaling: b = ?
   - TrotterDomain scaling: b = ?
   - n=10 EnergyDomain predicted time: ?
   - n=12 EnergyDomain predicted time: ?
   - n=7 attempted? Result?
  </action>
  <verify>
1. Report has sections: Metadata, EnergyDomain KMS, TrotterDomain KMS, Extrapolation, Memory Guard Calibration
2. Tables have consistent formatting with n, dim^2, krylovdim, time, alloc columns
3. Scaling fits report a and b values
4. Extrapolation section has n=10 and n=12 predictions
5. Feasibility notes correctly identify TrotterDomain NUFFT memory constraint

```bash
# Check report structure
grep -c "^##" results/krylov_scaling_report.md  # should have multiple sections
grep "b =" results/krylov_scaling_report.md || grep "b=" results/krylov_scaling_report.md  # scaling base present
```
  </verify>
  <done>
Report is complete, well-formatted, and contains all required sections: timing tables for both domains at n=3-6, scaling fits with b values, memory measurements, n=10/12 extrapolation with feasibility notes, and memory guard calibration data.
  </done>
</task>

</tasks>

<verification>
1. `results/krylov_scaling_report.md` exists with >30 lines
2. Report contains timing data for n=3,4,5,6 in both EnergyDomain and TrotterDomain sections
3. Scaling base b is in [3.5, 4.5] for both domains (script did not error)
4. Report contains extrapolation predictions for n=10 and n=12
5. Report includes memory measurements at each n
6. Report has feasibility notes identifying TrotterDomain NUFFT constraint at n=10+
</verification>

<success_criteria>
- Complete benchmark report exists at `results/krylov_scaling_report.md`
- Scaling law confirmed: b approximately 4.0, within [3.5, 4.5]
- Extrapolation provides wall-clock and memory estimates for n=10 and n=12
- Report clearly identifies feasibility constraints for both domains
</success_criteria>

<output>
After completion, create `.planning/phases/31-scaling-benchmarks/31-02-SUMMARY.md`
</output>
