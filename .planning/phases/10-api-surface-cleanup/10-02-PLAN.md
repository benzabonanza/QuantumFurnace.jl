---
phase: 10-api-surface-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/hamiltonian.jl
  - src/trotter_domain.jl
  - src/qi_tools.jl
  - src/time_domain.jl
  - src/nufft.jl
  - src/ofts.jl
  - src/energy_domain.jl
  - src/bohr_domain.jl
  - src/coherent.jl
  - src/jump_workers.jl
  - src/trajectories.jl
  - src/furnace_utensils.jl
  - src/furnace.jl
  - src/misc_tools.jl
  - src/log_sobolev.jl
  - test/test_dm_scaling.jl
autonomous: true

must_haves:
  truths:
    - "All internal (non-exported) functions have names starting with underscore (_)"
    - "All exported functions retain their original names (no underscore prefix)"
    - "Types (structs) retain their original names (no underscore prefix per locked decision)"
    - "All call sites updated consistently -- no undefined function references"
    - "Test files using qualified access (QuantumFurnace.func_name) updated to use new _-prefixed names"
    - "All 224 existing tests pass with no regressions"
  artifacts:
    - path: "src/qi_tools.jl"
      provides: "Internal functions with _ prefix: _kron!, _vectorize_liouv_diss_and_add!, _vectorize_liouvillian_coherent!"
      contains: "function _kron!"
    - path: "src/furnace_utensils.jl"
      provides: "Internal functions with _ prefix: _precompute_labels, _precompute_data, _select_b_plus_calculator"
      contains: "function _precompute_data"
    - path: "src/coherent.jl"
      provides: "Internal functions with _ prefix: _precompute_coherent_total_B, etc."
      contains: "function _precompute_coherent_total_B"
  key_links:
    - from: "src/furnace.jl"
      to: "src/furnace_utensils.jl"
      via: "Calls _precompute_data instead of precompute_data"
      pattern: "_precompute_data"
    - from: "src/jump_workers.jl"
      to: "src/qi_tools.jl"
      via: "Calls _vectorize_liouv_diss_and_add! instead of vectorize_liouv_diss_and_add!"
      pattern: "_vectorize_liouv_diss_and_add!"
    - from: "test/test_dm_scaling.jl"
      to: "src/energy_domain.jl"
      via: "Qualified access updated to _create_energy_labels"
      pattern: "QuantumFurnace\\._create_energy_labels"
---

<objective>
Apply underscore prefix convention to all internal (non-exported) functions across the codebase, signaling "private by convention" while preserving qualified access for power users.

Purpose: Per locked decision, all internal functions get `_` prefix to clearly separate public API from implementation details. Types (structs) keep their original names since they appear in method signatures and dispatch. This is a large mechanical rename that must be done consistently.

Output: All internal function names prefixed with `_`, all call sites updated, all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-api-surface-cleanup/10-CONTEXT.md
@.planning/phases/10-api-surface-cleanup/10-01-SUMMARY.md
@src/QuantumFurnace.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply _ prefix to all internal functions in source files</name>
  <files>src/hamiltonian.jl, src/trotter_domain.jl, src/qi_tools.jl, src/time_domain.jl, src/nufft.jl, src/ofts.jl, src/energy_domain.jl, src/bohr_domain.jl, src/coherent.jl, src/jump_workers.jl, src/trajectories.jl, src/furnace_utensils.jl, src/furnace.jl, src/misc_tools.jl, src/log_sobolev.jl</files>
  <action>
Rename all internal (non-exported) functions to use `_` prefix. This is a mechanical rename: for each function below, rename its definition AND all call sites within the `src/` directory.

**IMPORTANT RULES:**
- Types (structs, abstract types) do NOT get `_` prefix -- per locked decision
- Constructors for types (e.g., `KrausScratch(...)`, `OFTCaches(...)`, `LSIFramework(...)`, `TrajectoryWorkspace(...)`) do NOT get `_` prefix -- they are constructor methods of their type
- Functions that are already `_`-prefixed need NO change
- Exported functions need NO change (refer to the export block in QuantumFurnace.jl from Plan 01)

**Complete rename list, organized by file:**

**src/hamiltonian.jl:**
- `construct_base_ham` -> `_construct_base_ham` (called in hamiltonian.jl lines 71, 121, 228, 293-internal)
- `construct_disordering_terms` -> `_construct_disordering_terms` (called in hamiltonian.jl line 122 and trotter_domain.jl line 243)
- `rescaling_and_shift_factors` -> `_rescaling_and_shift_factors` (called in hamiltonian.jl lines 73, 125 and hamiltonian.jl line 246)

**src/trotter_domain.jl:**
- `trotter_bohr_freqs` -> `_trotter_bohr_freqs` (called only in trotter_domain.jl line 26)
- `trotterize2` -> `_trotterize2` (called only in trotter_domain.jl line 24)
- `does_term_differ_at_both_sites` -> `_does_term_differ_at_both_sites` (called only in trotter_domain.jl line 146)
- `compute_U_group` -> `_compute_U_group` (called in trotter_domain.jl lines 65, 68, 73, 171)

**src/qi_tools.jl:**
- `kron!` -> `_kron!` (called in qi_tools.jl lines 87, 90, 91, 106, 109, 110, 122, 123)
- `vectorize_liouv_diss_and_add!` -> `_vectorize_liouv_diss_and_add!` (called in jump_workers.jl lines 42, 74, 77, 84, 119, 129, and both method definitions in qi_tools.jl)
- `vectorize_liouvillian_coherent!` -> `_vectorize_liouvillian_coherent!` (called in jump_workers.jl lines 26, 61, 105 and furnace.jl line 74)

**src/time_domain.jl:**
- `truncate_time_labels_for_oft` -> `_truncate_time_labels_for_oft` (called in furnace_utensils.jl line 87 and test/test_dm_scaling.jl)

**src/nufft.jl:**
- `prepare_oft_nufft_prefactors` -> `_prepare_oft_nufft_prefactors` (called in furnace_utensils.jl line 103)
- `prefactor_view` -> `_prefactor_view` (called in jump_workers.jl lines 115, 127 and trajectories.jl lines 542, 575 and test/test_dm_scaling.jl)

**src/ofts.jl:**
- `time_oft!` -> `_time_oft!` (called in test/test_dm_scaling.jl -- NOT called from source code)
- `trotter_oft!` -> `_trotter_oft!` (called in test/test_dm_scaling.jl -- NOT called from source code)

**src/energy_domain.jl:**
- `pick_transition` -> `_pick_transition` (called in energy_domain.jl lines 3-6, furnace_utensils.jl lines 22, 37, 73, 89 -- 4 dispatch lines must all be renamed)
- `create_energy_labels` -> `_create_energy_labels` (called in furnace_utensils.jl lines 2, 8 and test/test_dm_scaling.jl)
- `truncate_energy_labels` -> `_truncate_energy_labels` (called in furnace_utensils.jl lines 3, 9)

**src/bohr_domain.jl:**
- `pick_f` -> `_pick_f` (called only in bohr_domain.jl lines 8, 29)
- `pick_alpha` -> `_pick_alpha` (called in furnace_utensils.jl lines 19, 34 -- 4 dispatch lines must all be renamed)

**src/coherent.jl:**
- `precompute_coherent_total_B` -> `_precompute_coherent_total_B` (called in coherent.jl line 83 via trajectories.jl build, and furnace.jl line 72)
- `precompute_coherent_unitary_terms` -> `_precompute_coherent_unitary_terms` (called in furnace.jl line 135)
- `precompute_coherent_terms` -> `_precompute_coherent_terms` (NOT called from source -- was exported for external use, now internal)
- `compute_b_minus` -> `_compute_b_minus` (called in furnace_utensils.jl line 94)
- `compute_b_plus` -> `_compute_b_plus` (called in furnace_utensils.jl line 126)
- `compute_b_plus_metro` -> `_compute_b_plus_metro` (called in furnace_utensils.jl line 133)
- `compute_b_plus_smooth` -> `_compute_b_plus_smooth` (called in furnace_utensils.jl line 130)
- `compute_truncated_func` -> `_compute_truncated_func` (called in furnace_utensils.jl lines 94-96)
- `get_truncated_indices` -> `_get_truncated_indices` (called in coherent.jl line 310 -- both variants)
- `convolute` -> `_convolute` (called in coherent.jl line 284)

**src/jump_workers.jl:**
- `jump_contribution!` -> `_jump_contribution!` (called in furnace.jl lines 88, trajectories.jl -- but wait, all 6 dispatch variants)
- `apply_coherent_unitary!` -> `_apply_coherent_unitary!` (called in jump_workers.jl lines 236, 329, 408)
- `finalize_kraus_step!` -> `_finalize_kraus_step!` (called in jump_workers.jl lines 308, 387, 450)

**src/trajectories.jl:**
- `precompute_R` -> `_precompute_R` (called in trajectories.jl line 96 -- both variants)

**src/furnace_utensils.jl:**
- `precompute_labels` -> `_precompute_labels` (called in furnace_utensils.jl lines 21, 36, 71, 86 -- both variants)
- `precompute_data` -> `_precompute_data` (called in furnace.jl lines 58, 123, trajectories.jl line 360 -- all 4 variants)
- `select_b_plus_calculator` -> `_select_b_plus_calculator` (called in furnace_utensils.jl line 95)

**src/misc_tools.jl:**
- `generate_filename` -> `_generate_filename` (both variants)
- `riemann_sum` -> `_riemann_sum` (all 3 variants -- only called internally if at all; check callsites)
- `print_press` -> `_print_press` (called in furnace.jl lines 9, 111 and trajectories.jl line 345 -- both variants)

**src/log_sobolev.jl:**
- `sandwich!` -> `_sandwich!` (called in log_sobolev.jl lines 56, 90, 125)

**Approach:**
For each function in the list above:
1. Rename the function definition (the `function name(...)` line)
2. Find ALL call sites within `src/` and rename them
3. Use `replace_all` or systematic search-and-replace to ensure nothing is missed

Be very careful with `kron!` -- Julia's Base has a `kron` function but no `kron!`. Our `kron!` is module-local and only called within qi_tools.jl itself. The rename to `_kron!` is safe.

Be careful with `pick_transition` -- it has 4 one-liner dispatch definitions AND the actual call sites.

**Note on `riemann_sum`:** Check if it's called anywhere in source. If not, it can still be renamed for convention consistency.
  </action>
  <verify>
After all renames, run: `julia --project -e 'using QuantumFurnace'` to verify the module loads without errors.
Then run: `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests must pass.

If tests fail, the error messages will indicate undefined function references that were missed during renaming.
  </verify>
  <done>
All internal functions across all source files have `_` prefix. All call sites within `src/` are updated consistently. Module loads without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test files for _ prefixed internal function names</name>
  <files>test/test_dm_scaling.jl</files>
  <action>
Update test files that use qualified access (`QuantumFurnace.func_name`) for functions that were renamed to `_func_name`.

**test/test_dm_scaling.jl** has these qualified-access call sites that need updating:
- `QuantumFurnace.create_energy_labels` -> `QuantumFurnace._create_energy_labels` (lines 152, 203)
- `QuantumFurnace.truncate_time_labels_for_oft` -> `QuantumFurnace._truncate_time_labels_for_oft` (lines 154, 205)
- `QuantumFurnace.time_oft!` -> `QuantumFurnace._time_oft!` (lines 159, 222)
- `QuantumFurnace.trotter_oft!` -> `QuantumFurnace._trotter_oft!` (lines 165, 241)
- `QuantumFurnace.prefactor_view` -> `QuantumFurnace._prefactor_view` (lines 216, 234)

**Note:** `test/test_dm_detailed_balance.jl` and trajectory validation files used `QuantumFurnace.trace_distance_h` which was already handled in Plan 01 Task 2 (unqualified, since it's now exported). No changes needed here.

Verify no other test files reference renamed functions by searching for `QuantumFurnace\.` in the test directory and checking each hit.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests must pass.
  </verify>
  <done>
All test files correctly reference `_`-prefixed internal functions via qualified access. All 224 tests pass.
  </done>
</task>

</tasks>

<verification>
1. Every non-exported function in `src/` starts with `_` (except constructors/type methods)
2. Every exported function retains its original name
3. All type names (structs) retain their original names
4. All call sites within `src/` use the new `_`-prefixed names
5. Test files using `QuantumFurnace.func` use new `_`-prefixed names
6. `julia --project -e 'using QuantumFurnace'` loads without errors
7. All 224 existing tests pass with `Pkg.test()`
8. Aqua.jl quality checks pass (no undefined exports)
</verification>

<success_criteria>
- All internal functions prefixed with `_`
- All exported functions unchanged
- All types unchanged
- All call sites consistently updated
- All 224 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-surface-cleanup/10-02-SUMMARY.md`
</output>
