---
phase: 10-api-surface-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/hamiltonian.jl
  - src/trotter_domain.jl
  - src/qi_tools.jl
  - src/time_domain.jl
  - src/nufft.jl
  - src/ofts.jl
  - src/energy_domain.jl
  - src/bohr_domain.jl
  - src/coherent.jl
  - src/jump_workers.jl
  - src/trajectories.jl
  - src/furnace_utensils.jl
  - src/misc_tools.jl
  - src/log_sobolev.jl
autonomous: true

must_haves:
  truths:
    - "All internal function DEFINITIONS have names starting with underscore (_) -- every `function name(...)` line for non-exported functions is renamed"
    - "All intra-file call sites (calls to functions defined in the SAME file) use the new _-prefixed names"
    - "All exported functions retain their original names (no underscore prefix)"
    - "Types (structs) retain their original names (no underscore prefix per locked decision)"
    - "Public API functions (trace_distance_h, create_f, trotterize, etc.) are accessible via `using QuantumFurnace` without qualification"
    - "`julia --project -e 'using QuantumFurnace'` loads without errors (module compiles despite cross-file calls being temporarily broken until Plan 10-03)"
  artifacts:
    - path: "src/qi_tools.jl"
      provides: "Internal functions with _ prefix: _kron!, _vectorize_liouv_diss_and_add!, _vectorize_liouvillian_coherent!"
      contains: "function _kron!"
    - path: "src/hamiltonian.jl"
      provides: "Internal functions with _ prefix: _construct_base_ham, _construct_disordering_terms, _rescaling_and_shift_factors"
      contains: "function _construct_base_ham"
    - path: "src/coherent.jl"
      provides: "Internal functions with _ prefix: _precompute_coherent_total_B, _convolute, _get_truncated_indices, etc."
      contains: "function _precompute_coherent_total_B"
    - path: "src/furnace_utensils.jl"
      provides: "Internal functions with _ prefix: _precompute_labels, _precompute_data, _select_b_plus_calculator"
      contains: "function _precompute_data"
  key_links:
    - from: "src/qi_tools.jl"
      to: "src/qi_tools.jl"
      via: "Intra-file: _kron! called within qi_tools.jl itself"
      pattern: "_kron!"
    - from: "src/coherent.jl"
      to: "src/coherent.jl"
      via: "Intra-file: _get_truncated_indices, _convolute called within coherent.jl"
      pattern: "_convolute"
    - from: "src/jump_workers.jl"
      to: "src/jump_workers.jl"
      via: "Intra-file: _apply_coherent_unitary!, _finalize_kraus_step! called within jump_workers.jl"
      pattern: "_finalize_kraus_step!"
---

<objective>
Rename all internal (non-exported) function DEFINITIONS to use `_` prefix, and update all INTRA-FILE call sites (calls within the same file where the function is defined).

Purpose: Per locked decision, all internal functions get `_` prefix to clearly separate public API from implementation details. This plan handles the bulk of the rename work -- every function definition and same-file call site. Cross-file call sites are deferred to Plan 10-03 to keep scope manageable.

Output: All internal function definitions prefixed with `_`, all intra-file call sites updated. Cross-file calls will be temporarily broken (calling old names) until Plan 10-03 runs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-api-surface-cleanup/10-CONTEXT.md
@.planning/phases/10-api-surface-cleanup/10-01-SUMMARY.md
@src/QuantumFurnace.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename internal function definitions and intra-file call sites in self-contained files</name>
  <files>src/hamiltonian.jl, src/trotter_domain.jl, src/qi_tools.jl, src/time_domain.jl, src/nufft.jl, src/ofts.jl, src/energy_domain.jl, src/bohr_domain.jl, src/coherent.jl, src/log_sobolev.jl</files>
  <action>
For each file below, rename the function DEFINITION (`function name(...)` or `name(...) = ...`) AND any call sites WITHIN THE SAME FILE. Do NOT update calls in other files -- that is Plan 10-03.

**IMPORTANT RULES:**
- Types (structs, abstract types) do NOT get `_` prefix -- per locked decision
- Constructors for types (e.g., `KrausScratch(...)`, `OFTCaches(...)`) do NOT get `_` prefix
- Exported functions need NO change (refer to export block in QuantumFurnace.jl from Plan 01)

**src/hamiltonian.jl** (all calls are intra-file):
- `construct_base_ham` -> `_construct_base_ham` (definition + intra-file calls at lines 71, 121, 228, 293)
- `construct_disordering_terms` -> `_construct_disordering_terms` (definition + intra-file call at line 122; cross-file call in trotter_domain.jl handled in Plan 10-03)
- `rescaling_and_shift_factors` -> `_rescaling_and_shift_factors` (definition + intra-file calls at lines 73, 125, 246)

**src/trotter_domain.jl** (all calls are intra-file):
- `trotter_bohr_freqs` -> `_trotter_bohr_freqs` (definition + intra-file call at line 26)
- `trotterize2` -> `_trotterize2` (definition + intra-file call at line 24)
- `does_term_differ_at_both_sites` -> `_does_term_differ_at_both_sites` (definition + intra-file call at line 146)
- `compute_U_group` -> `_compute_U_group` (definition + intra-file calls at lines 65, 68, 73, 171)

**src/qi_tools.jl** (kron! is purely intra-file; the other two have cross-file callers):
- `kron!` -> `_kron!` (definition + intra-file calls at lines 87, 90, 91, 106, 109, 110, 122, 123)
- `vectorize_liouv_diss_and_add!` -> `_vectorize_liouv_diss_and_add!` (definition only -- both method definitions; cross-file calls in jump_workers.jl handled in Plan 10-03)
- `vectorize_liouvillian_coherent!` -> `_vectorize_liouvillian_coherent!` (definition only; cross-file calls in jump_workers.jl and furnace.jl handled in Plan 10-03)

**src/time_domain.jl:**
- `truncate_time_labels_for_oft` -> `_truncate_time_labels_for_oft` (definition only; cross-file call in furnace_utensils.jl and test handled in Plan 10-03)

**src/nufft.jl:**
- `prepare_oft_nufft_prefactors` -> `_prepare_oft_nufft_prefactors` (definition only; cross-file call in furnace_utensils.jl handled in Plan 10-03)
- `prefactor_view` -> `_prefactor_view` (definition only; cross-file calls in jump_workers.jl, trajectories.jl, and test handled in Plan 10-03)

**src/ofts.jl:**
- `time_oft!` -> `_time_oft!` (definition only; called only from test/test_dm_scaling.jl handled in Plan 10-03)
- `trotter_oft!` -> `_trotter_oft!` (definition only; called only from test/test_dm_scaling.jl handled in Plan 10-03)

**src/energy_domain.jl:**
- `pick_transition` -> `_pick_transition` (ALL dispatch definitions, lines 3-6; cross-file calls in furnace_utensils.jl handled in Plan 10-03)
- `create_energy_labels` -> `_create_energy_labels` (definition; cross-file calls in furnace_utensils.jl and test handled in Plan 10-03)
- `truncate_energy_labels` -> `_truncate_energy_labels` (definition; cross-file calls in furnace_utensils.jl handled in Plan 10-03)

**src/bohr_domain.jl:**
- `pick_f` -> `_pick_f` (definition + intra-file calls at lines 8, 29)
- `pick_alpha` -> `_pick_alpha` (ALL dispatch definitions; cross-file calls in furnace_utensils.jl handled in Plan 10-03)

**src/coherent.jl:**
- `precompute_coherent_total_B` -> `_precompute_coherent_total_B` (definition; cross-file calls in furnace.jl handled in Plan 10-03)
- `precompute_coherent_unitary_terms` -> `_precompute_coherent_unitary_terms` (definition; cross-file call in furnace.jl handled in Plan 10-03)
- `precompute_coherent_terms` -> `_precompute_coherent_terms` (definition only; not called from source)
- `compute_b_minus` -> `_compute_b_minus` (definition; cross-file call in furnace_utensils.jl handled in Plan 10-03)
- `compute_b_plus` -> `_compute_b_plus` (definition; cross-file call in furnace_utensils.jl handled in Plan 10-03)
- `compute_b_plus_metro` -> `_compute_b_plus_metro` (definition; cross-file call in furnace_utensils.jl handled in Plan 10-03)
- `compute_b_plus_smooth` -> `_compute_b_plus_smooth` (definition; cross-file call in furnace_utensils.jl handled in Plan 10-03)
- `compute_truncated_func` -> `_compute_truncated_func` (definition; cross-file calls in furnace_utensils.jl handled in Plan 10-03)
- `get_truncated_indices` -> `_get_truncated_indices` (definition + intra-file call at line 310 -- both variants)
- `convolute` -> `_convolute` (definition + intra-file call at line 284)

**src/log_sobolev.jl** (all calls are intra-file):
- `sandwich!` -> `_sandwich!` (definition + intra-file calls at lines 56, 90, 125)

**Approach:**
1. Work through each file systematically
2. For each function: rename the `function name(` definition AND any intra-file calls
3. For functions with only cross-file callers, rename ONLY the definition
4. Use `replace_all` for safety when a name appears multiple times in the same file

Be very careful with `kron!` -- Julia's Base has a `kron` function but no `kron!`. Our `kron!` is module-local and only called within qi_tools.jl itself. The rename to `_kron!` is safe.

Be careful with `pick_transition` -- it has 4 one-liner dispatch definitions that must ALL be renamed.
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to verify the module loads without errors. Since Julia uses late binding, cross-file calls to old names will not error at load time -- they will only error when actually called. Module loading should succeed.

Verify a sample of renamed definitions:
```
grep -n "function _construct_base_ham" src/hamiltonian.jl
grep -n "function _kron!" src/qi_tools.jl
grep -n "function _precompute_coherent_total_B" src/coherent.jl
```
  </verify>
  <done>
All internal function definitions in these 10 files have `_` prefix. All intra-file call sites are updated. Module loads without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename internal function definitions and intra-file call sites in worker/integration files</name>
  <files>src/jump_workers.jl, src/trajectories.jl, src/furnace_utensils.jl, src/misc_tools.jl</files>
  <action>
Rename function DEFINITIONS and INTRA-FILE call sites in the remaining source files. These files both DEFINE internal functions and CALL functions from other files -- only rename the definitions and intra-file calls here. Cross-file call site updates happen in Plan 10-03.

**src/jump_workers.jl:**
- `jump_contribution!` -> `_jump_contribution!` (ALL 6 dispatch variant definitions; cross-file calls in furnace.jl and trajectories.jl handled in Plan 10-03)
- `apply_coherent_unitary!` -> `_apply_coherent_unitary!` (definition + intra-file calls at lines 236, 329, 408)
- `finalize_kraus_step!` -> `_finalize_kraus_step!` (definition + intra-file calls at lines 308, 387, 450)

**src/trajectories.jl:**
- `precompute_R` -> `_precompute_R` (definition + intra-file call at line 96 -- both variants)

**src/furnace_utensils.jl:**
- `precompute_labels` -> `_precompute_labels` (definition + intra-file calls at lines 21, 36, 71, 86 -- both variants)
- `precompute_data` -> `_precompute_data` (ALL 4 variant definitions; cross-file calls in furnace.jl and trajectories.jl handled in Plan 10-03)
- `select_b_plus_calculator` -> `_select_b_plus_calculator` (definition + intra-file call at line 95)

**src/misc_tools.jl** (all calls are cross-file):
- `generate_filename` -> `_generate_filename` (both variant definitions; called from furnace.jl and trajectories.jl -- cross-file, handled in Plan 10-03)
- `riemann_sum` -> `_riemann_sum` (all 3 variant definitions; check if called from source -- if not, rename definitions only for convention consistency)
- `print_press` -> `_print_press` (both variant definitions; cross-file calls in furnace.jl and trajectories.jl handled in Plan 10-03)

**Approach:**
1. Rename definitions first
2. Then update intra-file calls (same file only)
3. Do NOT touch calls to functions defined in OTHER files -- those are Plan 10-03's responsibility
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to verify the module still loads. Cross-file calls to old names won't error at load time (late binding).

Verify a sample of renamed definitions:
```
grep -n "function _jump_contribution!" src/jump_workers.jl
grep -n "function _precompute_data" src/furnace_utensils.jl
grep -n "function _generate_filename" src/misc_tools.jl
```
  </verify>
  <done>
All internal function definitions in these 4 files have `_` prefix. All intra-file call sites are updated. Module loads without errors. Cross-file calls remain using old names (will be fixed in Plan 10-03).
  </done>
</task>

</tasks>

<verification>
1. Every non-exported function DEFINITION in `src/` starts with `_` (except constructors/type methods)
2. Every exported function definition retains its original name
3. All type names (structs) retain their original names
4. All INTRA-FILE call sites use the new `_`-prefixed names
5. `julia --project -e 'using QuantumFurnace'` loads without errors
6. Cross-file calls still use old names (expected -- Plan 10-03 will fix them)
</verification>

<success_criteria>
- All ~50 internal function definitions prefixed with `_` across 14 source files
- All intra-file call sites consistently updated
- All exported functions unchanged
- All types unchanged
- Module loads without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-surface-cleanup/10-02-SUMMARY.md`
</output>
