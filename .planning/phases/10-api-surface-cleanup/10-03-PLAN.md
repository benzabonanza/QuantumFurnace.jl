---
phase: 10-api-surface-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/furnace.jl
  - src/jump_workers.jl
  - src/trajectories.jl
  - src/furnace_utensils.jl
  - src/trotter_domain.jl
  - test/test_dm_scaling.jl
autonomous: true

must_haves:
  truths:
    - "All cross-file call sites use the new _-prefixed names -- no calls to old unprefixed internal function names remain anywhere in src/ or test/"
    - "Public API functions (trace_distance_h, create_f, trotterize, etc.) are accessible via `using QuantumFurnace` without qualification"
    - "Internal functions (precompute_data, generate_filename, prefactor_view, etc.) require qualified access via `QuantumFurnace._func_name`"
    - "All 224 existing tests pass with no regressions"
    - "Aqua.jl quality checks pass (no undefined exports)"
  artifacts:
    - path: "src/furnace.jl"
      provides: "All cross-file calls updated to _-prefixed names"
      contains: "_precompute_data"
    - path: "src/furnace_utensils.jl"
      provides: "All cross-file calls updated to _-prefixed names"
      contains: "_truncate_time_labels_for_oft"
    - path: "src/jump_workers.jl"
      provides: "All cross-file calls updated to _-prefixed names"
      contains: "_vectorize_liouv_diss_and_add!"
    - path: "test/test_dm_scaling.jl"
      provides: "Qualified access updated to _-prefixed names"
      contains: "QuantumFurnace._create_energy_labels"
  key_links:
    - from: "src/furnace.jl"
      to: "src/furnace_utensils.jl"
      via: "Calls _precompute_data instead of precompute_data"
      pattern: "_precompute_data"
    - from: "src/jump_workers.jl"
      to: "src/qi_tools.jl"
      via: "Calls _vectorize_liouv_diss_and_add! instead of vectorize_liouv_diss_and_add!"
      pattern: "_vectorize_liouv_diss_and_add!"
    - from: "src/furnace_utensils.jl"
      to: "src/coherent.jl"
      via: "Calls _compute_b_minus, _compute_b_plus, etc."
      pattern: "_compute_b_minus"
    - from: "test/test_dm_scaling.jl"
      to: "src/energy_domain.jl"
      via: "Qualified access updated to QuantumFurnace._create_energy_labels"
      pattern: "QuantumFurnace\\._create_energy_labels"
---

<objective>
Update all CROSS-FILE call sites to use the `_`-prefixed function names established by Plan 10-02, and update test qualified access patterns.

Purpose: Plan 10-02 renamed all function definitions and intra-file calls. This plan completes the rename by updating every place where file A calls a function defined in file B using the old unprefixed name. Also updates test files that use qualified access (`QuantumFurnace.func_name`) to the new `_`-prefixed names.

Output: All cross-file call sites updated, all tests pass, API surface cleanup is complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-api-surface-cleanup/10-CONTEXT.md
@.planning/phases/10-api-surface-cleanup/10-02-SUMMARY.md
@src/QuantumFurnace.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cross-file call sites in source files</name>
  <files>src/furnace.jl, src/jump_workers.jl, src/trajectories.jl, src/furnace_utensils.jl, src/trotter_domain.jl</files>
  <action>
Update all call sites where a function defined in one source file is called from a DIFFERENT source file. Plan 10-02 renamed the definitions; now we update the callers.

**src/furnace.jl** calls these functions from other files:
- `precompute_data(...)` -> `_precompute_data(...)` (defined in furnace_utensils.jl; called at lines ~58, ~123)
- `vectorize_liouvillian_coherent!(...)` -> `_vectorize_liouvillian_coherent!(...)` (defined in qi_tools.jl; called at line ~74)
- `precompute_coherent_total_B(...)` -> `_precompute_coherent_total_B(...)` (defined in coherent.jl; called at line ~72)
- `precompute_coherent_unitary_terms(...)` -> `_precompute_coherent_unitary_terms(...)` (defined in coherent.jl; called at line ~135)
- `jump_contribution!(...)` -> `_jump_contribution!(...)` (defined in jump_workers.jl; called at line ~88)
- `print_press(...)` -> `_print_press(...)` (defined in misc_tools.jl; called at lines ~9, ~111)
- `generate_filename(...)` -> `_generate_filename(...)` (defined in misc_tools.jl; find and update all calls)

**src/jump_workers.jl** calls these functions from other files:
- `vectorize_liouv_diss_and_add!(...)` -> `_vectorize_liouv_diss_and_add!(...)` (defined in qi_tools.jl; called at lines ~42, ~74, ~77, ~84, ~119, ~129)
- `vectorize_liouvillian_coherent!(...)` -> `_vectorize_liouvillian_coherent!(...)` (defined in qi_tools.jl; called at lines ~26, ~61, ~105)
- `prefactor_view(...)` -> `_prefactor_view(...)` (defined in nufft.jl; called at lines ~115, ~127)

**src/trajectories.jl** calls these functions from other files:
- `prefactor_view(...)` -> `_prefactor_view(...)` (defined in nufft.jl; called at lines ~542, ~575)
- `precompute_data(...)` -> `_precompute_data(...)` (defined in furnace_utensils.jl; called at line ~360)
- `print_press(...)` -> `_print_press(...)` (defined in misc_tools.jl; called at line ~345)
- `jump_contribution!(...)` -> `_jump_contribution!(...)` (defined in jump_workers.jl; find and update all calls)
- `generate_filename(...)` -> `_generate_filename(...)` (defined in misc_tools.jl; find and update all calls)

**src/furnace_utensils.jl** calls these functions from other files:
- `truncate_time_labels_for_oft(...)` -> `_truncate_time_labels_for_oft(...)` (defined in time_domain.jl; called at line ~87)
- `prepare_oft_nufft_prefactors(...)` -> `_prepare_oft_nufft_prefactors(...)` (defined in nufft.jl; called at line ~103)
- `create_energy_labels(...)` -> `_create_energy_labels(...)` (defined in energy_domain.jl; called at lines ~2, ~8)
- `truncate_energy_labels(...)` -> `_truncate_energy_labels(...)` (defined in energy_domain.jl; called at lines ~3, ~9)
- `pick_transition(...)` -> `_pick_transition(...)` (defined in energy_domain.jl; called at lines ~22, ~37, ~73, ~89)
- `pick_alpha(...)` -> `_pick_alpha(...)` (defined in bohr_domain.jl; called at lines ~19, ~34)
- `compute_b_minus(...)` -> `_compute_b_minus(...)` (defined in coherent.jl; called at line ~94)
- `compute_b_plus(...)` -> `_compute_b_plus(...)` (defined in coherent.jl; called at line ~126)
- `compute_b_plus_metro(...)` -> `_compute_b_plus_metro(...)` (defined in coherent.jl; called at line ~133)
- `compute_b_plus_smooth(...)` -> `_compute_b_plus_smooth(...)` (defined in coherent.jl; called at line ~130)
- `compute_truncated_func(...)` -> `_compute_truncated_func(...)` (defined in coherent.jl; called at lines ~94-96)

**src/trotter_domain.jl** calls this function from another file:
- `construct_disordering_terms(...)` -> `_construct_disordering_terms(...)` (defined in hamiltonian.jl; called at line ~243)

**Approach:**
1. For each file, search for all occurrences of the old function name
2. Replace with the `_`-prefixed version
3. After updating each file, verify no old names remain by grepping
4. After all files are done, run `julia --project -e 'using QuantumFurnace'` to verify module loads

**IMPORTANT:** Be careful NOT to rename:
- Exported function calls (these keep their original names)
- Type constructor calls (these keep their original names)
- String literals containing function names (e.g., in log messages)
  </action>
  <verify>
Run `julia --project -e 'using QuantumFurnace'` to verify the module loads.

Then verify no old unprefixed internal function names remain as call sites:
```
# These should return NO matches (definitions already renamed in Plan 10-02):
grep -rn "precompute_data\b" src/ --include="*.jl" | grep -v "^.*:.*function\|^.*:.*#\|_precompute_data"
grep -rn "vectorize_liouv_diss_and_add!\b" src/ --include="*.jl" | grep -v "^.*:.*function\|^.*:.*#\|_vectorize_liouv_diss_and_add!"
grep -rn "print_press\b" src/ --include="*.jl" | grep -v "^.*:.*function\|^.*:.*#\|_print_press"
```
  </verify>
  <done>
All cross-file call sites in source files use the new `_`-prefixed names. No old unprefixed internal function calls remain in `src/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test files for _-prefixed internal function names</name>
  <files>test/test_dm_scaling.jl</files>
  <action>
Update test files that use qualified access (`QuantumFurnace.func_name`) for functions that were renamed to `_func_name` in Plan 10-02.

**test/test_dm_scaling.jl** has these qualified-access call sites that need updating:
- `QuantumFurnace.create_energy_labels` -> `QuantumFurnace._create_energy_labels` (lines ~152, ~203)
- `QuantumFurnace.truncate_time_labels_for_oft` -> `QuantumFurnace._truncate_time_labels_for_oft` (lines ~154, ~205)
- `QuantumFurnace.time_oft!` -> `QuantumFurnace._time_oft!` (lines ~159, ~222)
- `QuantumFurnace.trotter_oft!` -> `QuantumFurnace._trotter_oft!` (lines ~165, ~241)
- `QuantumFurnace.prefactor_view` -> `QuantumFurnace._prefactor_view` (lines ~216, ~234)

**Note:** `test/test_dm_detailed_balance.jl` and trajectory validation files used `QuantumFurnace.trace_distance_h` which was already handled in Plan 10-01 Task 2 (unqualified, since it's now exported). No changes needed for those files.

Also verify no other test files reference renamed functions by searching for `QuantumFurnace\.` in the test directory and checking each hit against the renamed function list.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests must pass.

Verify the qualified access patterns are correct:
```
grep -n "QuantumFurnace\._" test/test_dm_scaling.jl
```
  </verify>
  <done>
All test files correctly reference `_`-prefixed internal functions via qualified access. All 224 tests pass including Aqua.jl quality checks.
  </done>
</task>

</tasks>

<verification>
1. No unprefixed internal function calls remain in `src/` (all cross-file calls updated)
2. No unprefixed internal function calls remain in `test/` (qualified access updated)
3. Every exported function retains its original name and is accessible via `using QuantumFurnace`
4. `julia --project -e 'using QuantumFurnace'` loads without errors
5. All 224 existing tests pass with `Pkg.test()`
6. Aqua.jl quality checks pass (no undefined exports)
</verification>

<success_criteria>
- All cross-file call sites use `_`-prefixed names
- All test qualified access uses `_`-prefixed names
- All 224 tests pass
- Phase 10 API surface cleanup is complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-api-surface-cleanup/10-03-SUMMARY.md`
</output>
