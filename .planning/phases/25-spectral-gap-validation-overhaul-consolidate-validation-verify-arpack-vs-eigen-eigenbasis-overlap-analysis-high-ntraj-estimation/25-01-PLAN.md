---
phase: 25-spectral-gap-validation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/convergence.jl
  - src/gap_estimation.jl
  - src/QuantumFurnace.jl
  - test/test_convergence.jl
  - test/test_gap_estimation.jl
  - experiments/validate_gap_estimation.jl
  - experiments/run_gap_validation.jl
  - experiments/run_sweep.jl
  - experiments/eigenmode_decomposition.jl
autonomous: true

must_haves:
  truths:
    - "Only one observable builder function exists: build_preset_trajectory_observables"
    - "CrossValidationResult and cross_validate_gap no longer exist in source or exports"
    - "All 4 old experiment scripts are deleted"
    - "All tests pass after the consolidation"
    - "estimate_spectral_gap internally calls the renamed function"
  artifacts:
    - path: "src/convergence.jl"
      provides: "Single observable builder: build_preset_trajectory_observables"
      contains: "build_preset_trajectory_observables"
    - path: "src/gap_estimation.jl"
      provides: "estimate_spectral_gap + SpectralGapResult only (no cross-validation)"
    - path: "src/QuantumFurnace.jl"
      provides: "Updated exports with new function name, old exports removed"
      contains: "build_preset_trajectory_observables"
    - path: "test/test_convergence.jl"
      provides: "All convergence tests using new builder function"
    - path: "test/test_gap_estimation.jl"
      provides: "Gap estimation tests without cross-validation testset"
  key_links:
    - from: "src/gap_estimation.jl"
      to: "src/convergence.jl"
      via: "build_preset_trajectory_observables call in estimate_spectral_gap"
      pattern: "build_preset_trajectory_observables"
    - from: "test/test_convergence.jl"
      to: "src/convergence.jl"
      via: "build_preset_trajectory_observables in test fixtures"
      pattern: "build_preset_trajectory_observables"
---

<objective>
Delete all old validation code and consolidate observable builders into a single function.

Purpose: Clean-slate removal of bloated validation infrastructure (4 experiment scripts, 4 observable builders, CrossValidationResult) per locked user decision. The core estimation pipeline stays untouched.

Output: Minimal, clean codebase with one observable builder, no cross-validation wrappers, no old experiment scripts, and all tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/convergence.jl
@src/gap_estimation.jl
@src/QuantumFurnace.jl
@test/test_convergence.jl
@test/test_gap_estimation.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old experiment scripts and remove cross-validation code from source</name>
  <files>
    experiments/validate_gap_estimation.jl
    experiments/run_gap_validation.jl
    experiments/run_sweep.jl
    experiments/eigenmode_decomposition.jl
    src/gap_estimation.jl
  </files>
  <action>
    1. DELETE these 4 experiment scripts entirely (git rm):
       - experiments/validate_gap_estimation.jl
       - experiments/run_gap_validation.jl
       - experiments/run_sweep.jl
       - experiments/eigenmode_decomposition.jl

    2. In src/gap_estimation.jl, DELETE everything from line 207 to end of file:
       - CrossValidationResult struct (lines 230-238)
       - cross_validate_gap(::SpectralGapResult, ::LindbladianResult) function (lines 259-261)
       - cross_validate_gap(::SpectralGapResult, ::Complex) function (lines 288-314)
       - All associated comments and docstrings in that section

    Keep EVERYTHING from lines 1-205 (SpectralGapResult, _select_best_observable, estimate_spectral_gap).
  </action>
  <verify>
    - `git status` shows 4 deleted files and gap_estimation.jl modified
    - `grep -c "CrossValidationResult" src/gap_estimation.jl` returns 0
    - `grep -c "cross_validate_gap" src/gap_estimation.jl` returns 0
  </verify>
  <done>
    CrossValidationResult, cross_validate_gap, and all 4 old experiment scripts are removed from the codebase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate observable builders and update all references</name>
  <files>
    src/convergence.jl
    src/gap_estimation.jl
    src/QuantumFurnace.jl
    test/test_convergence.jl
    test/test_gap_estimation.jl
  </files>
  <action>
    **A. In src/convergence.jl:**

    1. DELETE these 3 functions entirely:
       - `build_convergence_observables` (lines 13-46) -- per-bond ZZ + H in eigenbasis
       - `build_convergence_observables_trotter` (lines 48-77) -- per-bond ZZ + H in Trotter basis
       - `build_total_magnetization` (lines 79-111) -- standalone Mz builder

    2. RENAME `build_gap_estimation_observables` to `build_preset_trajectory_observables`:
       - Change function name on line 127
       - Update docstring to match new name
       - The internal logic stays EXACTLY the same (it already builds H, Mz, XX_avg, YY_avg, ZZ_avg with trotter keyword)
       - NOTE: `build_total_magnetization` is called INTERNALLY by this function (line 130). Since we're deleting it, inline the Mz construction:
         - Replace `mz_obs, mz_names = build_total_magnetization(hamiltonian, num_qubits; trotter=trotter)` with direct Mz construction (copy the 6 lines from the deleted function that build Mz_comp, transform it, return as array)

    **B. In src/gap_estimation.jl:**

    3. Update line 158 in `estimate_spectral_gap`:
       - Change `build_gap_estimation_observables(` to `build_preset_trajectory_observables(`

    **C. In src/QuantumFurnace.jl:**

    4. Update the "Convergence tracking" export line (line 48):
       - Remove: `build_convergence_observables`, `build_convergence_observables_trotter`, `build_total_magnetization`, `build_gap_estimation_observables`
       - Add: `build_preset_trajectory_observables`

    5. Update the "Gap estimation" export line (line 54):
       - Remove: `CrossValidationResult`, `cross_validate_gap`

    **D. In test/test_gap_estimation.jl:**

    6. DELETE the entire Cross-Validation section (lines 197-307):
       - The `_make_test_spectral_gap_result` helper function (lines 201-204)
       - The "Cross-Validation" testset (lines 206-305)

    7. Update remaining references to old function names:
       - Line 95: `build_gap_estimation_observables(SMALL_HAM, 3)` -> `build_preset_trajectory_observables(SMALL_HAM, 3)`
       - Line 110: same replacement

    **E. In test/test_convergence.jl:**

    8. DELETE testset 2 "build_convergence_observables (eigenbasis)" (lines 54-85)
    9. DELETE testset 3 "build_convergence_observables_trotter" (lines 90-108)
    10. DELETE testset 19 "build_total_magnetization (eigenbasis)" (lines 692-716)
    11. DELETE testset 20 "build_total_magnetization (Trotter basis)" (lines 719-748)

    12. RENAME testset 21 "build_gap_estimation_observables (eigenbasis)" to "build_preset_trajectory_observables (eigenbasis)":
        - Update function call: `build_gap_estimation_observables(TEST_HAM, NUM_QUBITS)` -> `build_preset_trajectory_observables(TEST_HAM, NUM_QUBITS)`
        - Lines 782-783: Replace `build_total_magnetization` cross-check with an inline Mz reference construction. The standalone function is deleted, but the correctness check for Mz must be preserved. Replace lines 782-783 with:
          ```julia
          # M_z observable (second) matches inline reference construction
          V = TEST_HAM.eigvecs
          Mz_comp = sum(Matrix{ComplexF64}(pad_term([Z], NUM_QUBITS, i)) for i in 1:NUM_QUBITS) / NUM_QUBITS
          Mz_expected = Matrix{ComplexF64}(V' * Mz_comp * V)
          @test isapprox(observables[2], Mz_expected; atol=1e-14)
          ```
          This preserves the Mz correctness assertion without depending on the deleted function.

    13. RENAME testset 22 "build_gap_estimation_observables (Trotter basis)":
        - Same function call rename pattern
        - Lines 835-836: Replace `build_total_magnetization` cross-check with an inline Mz reference construction:
          ```julia
          # M_z observable (second) matches inline reference construction
          V_T = TEST_TROTTER.eigvecs
          Mz_comp = sum(Matrix{ComplexF64}(pad_term([Z], NUM_QUBITS, i)) for i in 1:NUM_QUBITS) / NUM_QUBITS
          Mz_expected = Matrix{ComplexF64}(V_T' * Mz_comp * V_T)
          @test isapprox(observables[2], Mz_expected; atol=1e-14)
          ```

    14. In testsets 5, 8, 9, 10, 13, 14, 15, 18: Replace ALL calls to `build_convergence_observables(TEST_HAM, NUM_QUBITS)` with `build_preset_trajectory_observables(TEST_HAM, NUM_QUBITS)`.

    15. Update ALL assertions that depend on the old observable set:
        - Old set had `NUM_QUBITS + 1` observables (4 per-bond ZZ + H = 5). New set has exactly 5 (H, Mz, XX_avg, YY_avg, ZZ_avg). The COUNT is coincidentally the same for n=4!
        - But the NAMES differ: old was `["ZZ_12", "ZZ_23", "ZZ_34", "ZZ_41", "H"]`, new is `["H", "Mz", "XX_avg", "YY_avg", "ZZ_avg"]`.
        - And critically, the ORDERING differs: H was LAST (index 5) in the old builder, but is FIRST (index 1) in the new builder.
        - Update these specific assertions:

          **Testset 5** (_compute_gibbs_observable_values, line 131-141):
          a. Line 131: `build_convergence_observables(TEST_HAM, NUM_QUBITS)` -> `build_preset_trajectory_observables(TEST_HAM, NUM_QUBITS)`
          b. Line 138 comment: Update "Energy value (last entry)" -> "Energy value (first entry, H)"
          c. Line 141: `obs_gibbs[end]` -> `obs_gibbs[1]` (H is now the FIRST observable, not the last). This is critical — `obs_gibbs[end]` would silently compare ZZ_avg to the analytical energy, passing or failing without catching the real bug.

          **Testset 8** (integration test):
          a. Line 256: `size(conv_data.observable_values) == (NUM_QUBITS + 1, 5)` -> `(5, 5)` (same value but more explicit)
          b. Line 257: `conv_data.observable_names == names` -- this is fine (uses the variable)
          c. Line 258: `length(conv_data.observable_gibbs_values) == NUM_QUBITS + 1` -> `== 5`
          d. Lines 261-263: Energy convergence check. The old code used `energy_idx = length(names)` which was the last observable (H). With the new builder, H is the FIRST observable (index 1). Update: `energy_idx = findfirst(==("H"), names)`.

          **Testsets 9, 14, 15, 18** (determinism, hard cap, adaptive determinism, adaptive access):
          e. These testsets call `build_convergence_observables` as fixture input and pass the observables to convergence runners. The observables and names are passed through, so the runner does not assume ordering. No assertion updates needed beyond the function-call rename (step 14). Specifically verify:
             - Testset 9 (line 281): rename only, no observable-dependent assertions
             - Testset 14 (line 484): rename only, no observable-dependent assertions
             - Testset 15 (line 527): rename only, no observable-dependent assertions
             - Testset 18 (line 662): rename only, no observable-dependent assertions

          **Testset 10** (programmatic access):
          f. Line 344: `length(col_slice) == NUM_QUBITS + 1` -> `== 5`
          g. Line 347: `findfirst(==("H"), conv_data.observable_names)` -- this already uses findfirst, so it correctly finds H regardless of position. No change needed.

          **Testset 13** (adaptive convergence):
          h. Line 469: `size(conv_data.observable_values) == (NUM_QUBITS + 1, conv_data.total_batches)` -> `(5, conv_data.total_batches)`
  </action>
  <verify>
    Run the full test suite:
    ```bash
    cd /Users/bence/code/QuantumFurnace.jl && julia --project -e 'using Pkg; Pkg.test()'
    ```
    All tests must pass. Specifically verify:
    - `grep -c "build_convergence_observables" src/convergence.jl` returns 0
    - `grep -c "build_total_magnetization" src/convergence.jl` returns 0
    - `grep -c "build_gap_estimation_observables" src/` returns 0 (all replaced by new name)
    - `grep -c "CrossValidationResult" src/QuantumFurnace.jl` returns 0
    - `grep -c "cross_validate_gap" src/QuantumFurnace.jl` returns 0
  </verify>
  <done>
    Only `build_preset_trajectory_observables` exists as the observable builder. All tests pass. No references to deleted functions remain in source, exports, or tests.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "build_convergence_observables\b" src/ test/` returns nothing
2. `grep -r "build_total_magnetization" src/ test/` returns nothing
3. `grep -r "build_gap_estimation_observables" src/ test/` returns nothing
4. `grep -r "CrossValidationResult" src/ test/` returns nothing
5. `grep -r "cross_validate_gap" src/ test/` returns nothing
6. `ls experiments/validate_gap_estimation.jl experiments/run_gap_validation.jl experiments/run_sweep.jl experiments/eigenmode_decomposition.jl` all return "No such file"
7. Full test suite passes
</verification>

<success_criteria>
- Zero references to deleted functions in src/ and test/
- 4 experiment scripts deleted
- Single observable builder function exists: build_preset_trajectory_observables
- estimate_spectral_gap uses new function name internally
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/25-spectral-gap-validation-overhaul-consolidate-validation-verify-arpack-vs-eigen-eigenbasis-overlap-analysis-high-ntraj-estimation/25-01-SUMMARY.md`
</output>
