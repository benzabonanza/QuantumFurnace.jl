---
phase: 07-dry-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/qi_tools.jl
  - src/jump_workers.jl
  - src/trajectories.jl
  - src/furnace.jl
  - src/log_sobolev.jl
  - src/QuantumFurnace.jl
autonomous: true

must_haves:
  truths:
    - "A single hermitianize! function replaces all 12+ inline Hermitianization patterns across 4 source files"
    - "A single transform_jumps_to_basis helper replaces 3 identical Trotter basis transform comprehensions"
    - "All 224 tests pass with no regressions"
  artifacts:
    - path: "src/qi_tools.jl"
      provides: "hermitianize! in-place helper and transform_jumps_to_basis helper"
      contains: "function hermitianize!"
    - path: "src/jump_workers.jl"
      provides: "jump_contribution! functions calling hermitianize! instead of inline patterns"
    - path: "src/trajectories.jl"
      provides: "precompute_R and run_trajectories calling hermitianize! and transform_jumps_to_basis"
    - path: "src/furnace.jl"
      provides: "construct_lindbladian and run_thermalization calling transform_jumps_to_basis"
  key_links:
    - from: "src/jump_workers.jl"
      to: "src/qi_tools.jl"
      via: "hermitianize! call"
      pattern: "hermitianize!"
    - from: "src/furnace.jl"
      to: "src/qi_tools.jl"
      via: "transform_jumps_to_basis call"
      pattern: "transform_jumps_to_basis"
---

<objective>
Extract hermitianize! helper and Trotter basis transform helper to replace repeated inline patterns.

Purpose: Eliminate 12+ copy-pasted Hermitianization patterns and 3 identical Trotter basis transform comprehensions, establishing single-source helpers that reduce maintenance burden and error risk.

Output: Two new helper functions in qi_tools.jl, all call sites updated, all 224 tests passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/qi_tools.jl
@src/jump_workers.jl
@src/trajectories.jl
@src/furnace.jl
@src/log_sobolev.jl
@src/coherent.jl
@src/QuantumFurnace.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hermitianize! and transform_jumps_to_basis helpers, replace all call sites</name>
  <files>
    src/qi_tools.jl
    src/jump_workers.jl
    src/trajectories.jl
    src/furnace.jl
    src/log_sobolev.jl
    src/QuantumFurnace.jl
  </files>
  <action>
**Step 1: Add `hermitianize!` to `src/qi_tools.jl`**

Add at the top of qi_tools.jl (it is included before all consumers via the include order in QuantumFurnace.jl):

```julia
"""
    hermitianize!(A::AbstractMatrix) -> A

In-place Hermitianization: A .= (A + A') / 2.
Used to enforce Hermiticity after numerical accumulation.
"""
function hermitianize!(A::AbstractMatrix)
    A .= 0.5 .* (A .+ A')
    return A
end
```

**Step 2: Add `transform_jumps_to_basis` to `src/qi_tools.jl`**

```julia
"""
    transform_jumps_to_basis(jumps, eigvecs) -> Vector{JumpOp}

Transform jump operators from computational basis to a new eigenbasis.
Used for Trotter basis transforms in TrotterDomain simulations.
"""
function transform_jumps_to_basis(jumps::AbstractVector{<:JumpOp}, eigvecs::AbstractMatrix)
    return JumpOp[JumpOp(j.data, eigvecs' * j.data * eigvecs, j.orthogonal, j.hermitian) for j in jumps]
end
```

**Step 3: Replace all inline Hermitianization patterns**

Replace each instance with a call to `hermitianize!`. The exact replacements:

In `src/jump_workers.jl` (6 sites):
- Line ~238: `scratch.R .= 0.5 .* (scratch.R .+ scratch.R')` -> `hermitianize!(scratch.R)`
- Line ~270: `evolving_dm .= 0.5 .* (scratch.rho_next .+ scratch.rho_next')` -> `copyto!(evolving_dm, scratch.rho_next); hermitianize!(evolving_dm)` (NOTE: this is writing rho_next into evolving_dm AND hermitianizing -- the old code writes `0.5*(rho_next + rho_next')` into evolving_dm. Use hermitianize! on rho_next first, then copyto!. Or more directly: `hermitianize!(scratch.rho_next); copyto!(evolving_dm, scratch.rho_next)`)
- Line ~352: `scratch.R .= 0.5 .* (scratch.R .+ scratch.R')` -> `hermitianize!(scratch.R)`
- Line ~385: `evolving_dm .= 0.5 .* (scratch.rho_next .+ scratch.rho_next')` -> same pattern as line 270
- Line ~452: `scratch.R .= 0.5 .* (scratch.R .+ scratch.R')` -> `hermitianize!(scratch.R)`
- Line ~498: `evolving_dm .= 0.5 .* (scratch.rho_next .+ scratch.rho_next')` -> same pattern as line 270

In `src/trajectories.jl` (6 sites):
- Line ~83: `B_a .= 0.5 .* (B_a .+ B_a')` -> `hermitianize!(B_a)`
- Line ~113: `S_herm = Hermitian(0.5 .* (scratch.tmp2 .+ scratch.tmp2'))` -> `hermitianize!(scratch.tmp2); S_herm = Hermitian(scratch.tmp2)`
- Line ~203: `scratch.R .= 0.5 .* (scratch.R .+ scratch.R')` -> `hermitianize!(scratch.R)`
- Line ~258: `scratch.R .= 0.5 .* (scratch.R .+ scratch.R')` -> `hermitianize!(scratch.R)`
- Lines ~373, ~390, ~438: `rho_mean .= 0.5 .* (rho_mean .+ rho_mean')` -> `hermitianize!(rho_mean)`

In `src/furnace.jl` (1 site):
- Line ~22: `steady_state_dm = (steady_state_dm + steady_state_dm') / 2` -> `hermitianize!(steady_state_dm)`

In `src/log_sobolev.jl` (1 site):
- Line ~176: `v2_hermitian = Hermitian(v2_mat + v2_mat') / 2.0` -> `hermitianize!(v2_mat); v2_hermitian = Hermitian(v2_mat)`

IMPORTANT: For the 3 sites in jump_workers.jl where the pattern is `evolving_dm .= 0.5 .* (scratch.rho_next .+ scratch.rho_next')`, be careful: this both copies from rho_next and hermitianizes. The cleanest approach is:
```julia
hermitianize!(scratch.rho_next)
copyto!(evolving_dm, scratch.rho_next)
```
This is semantically identical since `0.5*(A+A')` is the same as hermitianize.

**Step 4: Replace Trotter basis transform comprehensions**

In `src/furnace.jl` (2 sites):
- Line ~74-76 (construct_lindbladian): Replace `JumpOp[JumpOp(j.data, trotter.eigvecs' * j.data * trotter.eigvecs, j.orthogonal, j.hermitian) for j in jumps]` with `transform_jumps_to_basis(jumps, trotter.eigvecs)`
- Line ~117-118 (run_thermalization): Same replacement

In `src/trajectories.jl` (1 site):
- Line ~69-71 (build_trajectoryframework): Replace `JumpOp[JumpOp(j.data, ham_or_trott.eigvecs' * j.data * ham_or_trott.eigvecs, j.orthogonal, j.hermitian) for j in jumps]` with `transform_jumps_to_basis(jumps, ham_or_trott.eigvecs)`
- Also update the else branch `JumpOp[j for j in jumps]` to `collect(JumpOp, jumps)` for consistency (or leave as-is if clearer).

**Step 5: Do NOT touch coherent.jl**

The Trotter transform in coherent.jl (B_trotter functions, lines 221 and 262) transforms a SINGLE jump's data matrix, not a vector of JumpOps. This is a different pattern (returns a plain Matrix, not a JumpOp). Leave it as-is -- it is not the same duplication as DRY-04.
  </action>
  <verify>
Run `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests must pass.

Grep to confirm no remaining inline patterns:
- `grep -n '0\.5 \.\* (' src/jump_workers.jl src/trajectories.jl src/furnace.jl` should return zero matches.
- `grep -n "eigvecs' \* j.data \* " src/furnace.jl src/trajectories.jl` should return zero matches.
- `grep -rn 'hermitianize!' src/` should show calls in jump_workers.jl, trajectories.jl, furnace.jl, log_sobolev.jl, and the definition in qi_tools.jl.
  </verify>
  <done>
hermitianize! helper defined in qi_tools.jl and called from all 12+ former inline sites. transform_jumps_to_basis helper defined in qi_tools.jl and called from all 3 former inline sites. All 224 tests pass.
  </done>
</task>

</tasks>

<verification>
1. `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests pass
2. No inline `0.5 .* (X .+ X')` or `(X + X') / 2` patterns remain in jump_workers.jl, trajectories.jl, furnace.jl
3. No inline `eigvecs' * j.data * eigvecs` comprehensions remain in furnace.jl or trajectories.jl
4. hermitianize! function exists in qi_tools.jl
5. transform_jumps_to_basis function exists in qi_tools.jl
</verification>

<success_criteria>
- DRY-01 satisfied: single hermitianize! helper replaces 12+ inline patterns
- DRY-04 satisfied: single transform_jumps_to_basis helper replaces 3 comprehensions
- All 224 tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-dry-refactoring/07-01-SUMMARY.md`
</output>
