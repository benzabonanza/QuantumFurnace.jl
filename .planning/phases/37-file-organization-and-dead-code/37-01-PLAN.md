---
phase: 37-file-organization-and-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/furnace.jl
  - src/structs.jl
  - src/convergence.jl
  - src/krylov_eigsolve.jl
  - src/errors.jl
  - src/kraus.jl
  - src/gap_estimation.jl
  - src/fitting.jl
  - src/log_sobolev.jl
  - test/test_gap_estimation.jl
  - test/test_fitting.jl
  - Project.toml
autonomous: true

must_haves:
  truths:
    - "Old entry points run_lindbladian() and run_thermalization() no longer exist in furnace.jl"
    - "Dead structs DMSimulationResult, LindbladianResult, and LSIFramework no longer exist in structs.jl"
    - "gap_estimation.jl, fitting.jl, and log_sobolev.jl are in src/staging/"
    - "test_gap_estimation.jl and test_fitting.jl are in test/staging/"
    - "errors.jl and kraus.jl are deleted from src/"
    - "Distributed is removed from Project.toml deps and compat"
    - "Stale comments referencing deleted functions are cleaned up"
  artifacts:
    - path: "src/staging/gap_estimation.jl"
      provides: "Staged gap estimation code"
    - path: "src/staging/fitting.jl"
      provides: "Staged exponential fitting code"
    - path: "src/staging/log_sobolev.jl"
      provides: "Staged LSI code (commented out)"
    - path: "test/staging/test_gap_estimation.jl"
      provides: "Staged gap estimation tests"
    - path: "test/staging/test_fitting.jl"
      provides: "Staged fitting tests"
  key_links:
    - from: "src/furnace.jl"
      to: "src/structs.jl"
      via: "run_lindblad still uses Config, LindbladResults; run_thermalize still uses ThermalizeResults"
      pattern: "LindbladResults|ThermalizeResults"
    - from: "src/convergence.jl"
      to: "src/trajectories.jl"
      via: "convergence functions still called by run_trajectory"
      pattern: "_run_trajectory_convergence|_run_trajectory_adaptive"
---

<objective>
Remove dead code, move trajectory gap estimation files to staging, and delete placeholder files.

Purpose: Clean the codebase by removing the old entry points and dead structs that have been replaced by Phase 36 API, isolate dormant gap estimation/fitting/LSI code in a staging area, and remove empty placeholder files. This prepares the module definition (Plan 02) to be updated cleanly.

Output: Cleaned source files with dead code removed, staging directories created with moved files, placeholder files deleted, Project.toml cleaned.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-file-organization-and-dead-code/37-RESEARCH.md

Key source files:
@src/furnace.jl
@src/structs.jl
@src/QuantumFurnace.jl
@Project.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code from furnace.jl and structs.jl, clean stale comments</name>
  <files>src/furnace.jl, src/structs.jl, src/convergence.jl, src/krylov_eigsolve.jl</files>
  <action>
1. **Delete run_lindbladian() from furnace.jl** (lines 1-36): Remove the entire function definition. The function starts at line 1 and ends at line 36 with `end`.

2. **Delete run_thermalization() from furnace.jl** (lines 91-161): Remove the entire function definition. Starts at line 91 and ends at line 161 with `end`.

3. **KEEP everything else in furnace.jl**: `construct_lindbladian` (used by `run_lindblad`), `run_lindblad` (new entry point), `run_thermalize` (new entry point).

4. **Clean stale comment in furnace.jl** (line 199): Change "same logic as existing run_lindbladian" to "Arpack shift-invert eigensolver" or similar.

5. **Delete DMSimulationResult from structs.jl** (lines ~125-142): Remove the docstring and the `@kwdef struct DMSimulationResult{T<:AbstractFloat}...end` block.

6. **Delete LindbladianResult from structs.jl** (lines ~144-163): Remove the docstring and the `@kwdef struct LindbladianResult{T<:AbstractFloat}...end` block.

7. **Delete LSIFramework from structs.jl** (lines ~288-318): Remove the `struct LSIFramework{T}...end` and its convenience constructor `function LSIFramework(dim::Int)...end`.

8. **Clean stale comment in structs.jl** (line ~360): Update "DM Kraus evolution (`run_thermalization`)" to "DM Kraus evolution (`run_thermalize`)" or remove the parenthetical.

9. **Clean stale comment in convergence.jl** (line ~96): Update "Follows the same transform as furnace.jl run_thermalization (line 116-118)" to remove the reference to the old function and line numbers, e.g., "Follows the same Trotter basis transform as run_thermalize".

10. **Clean stale comment in krylov_eigsolve.jl** (line ~438): Update "matching what `run_thermalization`" to "matching what `run_thermalize`".

**IMPORTANT:** Do NOT delete OFTCaches, ConvergenceData, TrajectoryResult, ObservableTrajectoryResult, or any other active struct. Do NOT delete construct_lindbladian, run_lindblad, run_thermalize, run_trajectories, or run_observable_trajectories.
  </action>
  <verify>
Run `grep -n "run_lindbladian\|run_thermalization\|DMSimulationResult\|LindbladianResult\|LSIFramework" src/furnace.jl src/structs.jl` and confirm no matches (except possibly in comment references to the new replacement functions). Verify construct_lindbladian, run_lindblad, run_thermalize still exist in furnace.jl.
  </verify>
  <done>run_lindbladian, run_thermalization, DMSimulationResult, LindbladianResult, and LSIFramework are deleted. Stale comments in furnace.jl, structs.jl, convergence.jl, krylov_eigsolve.jl are cleaned up. All active code (construct_lindbladian, run_lindblad, run_thermalize, OFTCaches, ConvergenceData, etc.) is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Move staging files, delete placeholders, clean Project.toml</name>
  <files>src/staging/gap_estimation.jl, src/staging/fitting.jl, src/staging/log_sobolev.jl, test/staging/test_gap_estimation.jl, test/staging/test_fitting.jl, src/errors.jl, src/kraus.jl, Project.toml</files>
  <action>
1. **Create staging directories:**
   ```bash
   mkdir -p src/staging
   mkdir -p test/staging
   ```

2. **Move gap estimation files to staging (use git mv to preserve history):**
   ```bash
   git mv src/gap_estimation.jl src/staging/gap_estimation.jl
   git mv src/fitting.jl src/staging/fitting.jl
   git mv src/log_sobolev.jl src/staging/log_sobolev.jl
   ```

3. **Move related tests to staging:**
   ```bash
   git mv test/test_gap_estimation.jl test/staging/test_gap_estimation.jl
   git mv test/test_fitting.jl test/staging/test_fitting.jl
   ```

4. **Delete placeholder files:**
   ```bash
   git rm src/errors.jl
   git rm src/kraus.jl
   ```

5. **Clean Project.toml — remove Distributed:**
   - Remove `Distributed = "8ba89e20-285c-5b6f-9357-94700520ee1b"` from [deps] section
   - Remove `Distributed = "1.11, 1.12"` from [compat] section

**IMPORTANT:** Do NOT move convergence.jl — it is actively called by run_trajectory in trajectories.jl. Do NOT move nufft.jl or ofts.jl — they are active. Do NOT touch test_convergence.jl — convergence.jl stays active.
  </action>
  <verify>
Verify staging directories exist with correct files:
```bash
ls src/staging/  # Should show gap_estimation.jl, fitting.jl, log_sobolev.jl
ls test/staging/ # Should show test_gap_estimation.jl, test_fitting.jl
ls src/errors.jl src/kraus.jl 2>&1  # Should show "No such file"
grep "Distributed" Project.toml     # Should show NO matches
```
Verify convergence.jl is still in src/:
```bash
ls src/convergence.jl  # Should exist
```
  </verify>
  <done>gap_estimation.jl, fitting.jl, log_sobolev.jl are in src/staging/. test_gap_estimation.jl and test_fitting.jl are in test/staging/. errors.jl and kraus.jl are deleted. Distributed is removed from Project.toml. convergence.jl remains in active src/.</done>
</task>

</tasks>

<verification>
1. Dead code is removed: `grep -rn "run_lindbladian\|run_thermalization\|DMSimulationResult\|LindbladianResult\b\|LSIFramework" src/` returns no matches in active code
2. Staging files exist: `ls src/staging/ test/staging/` shows all expected files
3. Placeholders deleted: `ls src/errors.jl src/kraus.jl` fails with "No such file"
4. Project.toml clean: `grep Distributed Project.toml` returns nothing
5. Active code preserved: construct_lindbladian, run_lindblad, run_thermalize, convergence.jl all intact
</verification>

<success_criteria>
- All dead code (old entry points, old result structs, LSIFramework) is removed from active source
- Staging files are correctly relocated with git history preserved
- Empty placeholder files are deleted
- Project.toml has no Distributed dependency
- No active code is accidentally deleted (convergence.jl stays, OFTCaches stays, construct_lindbladian stays)
</success_criteria>

<output>
After completion, create `.planning/phases/37-file-organization-and-dead-code/37-01-SUMMARY.md`
</output>
