---
phase: 15-data-architecture
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - test/test_results.jl
  - test/runtests.jl
autonomous: true

must_haves:
  truths:
    - "A KMS ThermalizeConfig ExperimentResult round-trips through BSON with all config fields, trajectory data, hamiltonian params, and metadata matching to machine precision"
    - "A GNS ThermalizeConfigGNS ExperimentResult round-trips through BSON with the config type preserved (GNS distinction survives serialization)"
    - "A LiouvConfig (non-thermalize) ExperimentResult round-trips correctly, including the case where mixing_time/delta are absent"
    - "Loading a BSON file with missing metadata or hamiltonian_params fields returns an ExperimentResult with empty Dicts (no error)"
    - "Companion .txt file is created alongside the BSON file with human-readable experiment summary"
    - "Filename generation produces the correct descriptive format (e.g., kms_n4_beta10_trotter_YYYYMMDD.bson)"
    - "An actual trajectory run result can be saved and loaded back with density matrix matching to machine precision"
  artifacts:
    - path: "test/test_results.jl"
      provides: "Round-trip serialization tests, edge case tests, integration test with real trajectory"
    - path: "test/runtests.jl"
      provides: "Updated to include test_results.jl"
  key_links:
    - from: "test/test_results.jl"
      to: "src/results.jl"
      via: "Tests call save_experiment, load_experiment, verify field equality"
      pattern: "save_experiment|load_experiment|ExperimentResult"
    - from: "test/test_results.jl"
      to: "test/test_helpers.jl"
      via: "Uses shared test fixtures (SMALL_HAM, SMALL_JUMPS, make_small_thermalize_config)"
      pattern: "SMALL_HAM|SMALL_JUMPS|make_small"
---

<objective>
Write comprehensive tests for ExperimentResult serialization round-trip, covering all config types, edge cases, and integration with real trajectory runs.

Purpose: Verify that DATA-01 and DATA-02 requirements are satisfied -- experiment results can be saved and loaded with all fields matching, including the three success criteria from the phase goal.

Output: `test/test_results.jl` with round-trip tests for all config types, forward-compatibility edge cases, and an integration test using actual trajectory data. Updated `test/runtests.jl` to include the new test file.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-data-architecture/15-CONTEXT.md
@.planning/phases/15-data-architecture/15-01-SUMMARY.md
@src/results.jl
@src/structs.jl
@src/trajectories.jl
@test/test_helpers.jl
@test/runtests.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round-trip serialization tests for all config types and edge cases</name>
  <files>test/test_results.jl</files>
  <action>
Create `test/test_results.jl` with `@testset "ExperimentResult serialization"` containing:

**Setup:** Use `mktempdir()` for all test files (auto-cleanup). Create test ExperimentResult objects using the existing test fixture configs from test_helpers.jl.

**Test group 1: "KMS ThermalizeConfig round-trip"**
- Create a ThermalizeConfig (KMS) using `make_small_thermalize_config(EnergyDomain())` or similar from test_helpers.jl.
- Create a fake TrajectoryResult with a small random density matrix (`rho = random_density_matrix(SMALL_DIM)`), n_trajectories=100, seed=42, times=nothing, measurements_mean=nothing.
- Create hamiltonian_params via `_extract_hamiltonian_params(SMALL_HAM)` (import from QuantumFurnace if needed, or use `QuantumFurnace._extract_hamiltonian_params`).
- Create metadata Dict with known values: seed=42, n_threads=4, julia_version="test", timestamp="2026-02-16", git_hash="abc123", wall_time_seconds=1.5.
- Construct ExperimentResult.
- Save to `joinpath(tmpdir, "test_kms.bson")`.
- Load back.
- Assert: loaded config fields match original (num_qubits, beta, sigma, domain type, with_coherent, etc.).
- Assert: loaded trajectory_result.rho_mean matches original to machine precision (`isapprox(loaded.trajectory_result.rho_mean, original.trajectory_result.rho_mean; atol=0)`).
- Assert: loaded trajectory_result.n_trajectories == original.
- Assert: loaded trajectory_result.seed == original.
- Assert: loaded metadata[:seed] == 42, etc.
- Assert: loaded hamiltonian_params has expected keys.

**Test group 2: "GNS ThermalizeConfigGNS round-trip"**
- Use `make_small_thermalize_config_gns(EnergyDomain())` or manually create a ThermalizeConfigGNS.
- Same pattern: create ExperimentResult, save, load, verify.
- Key assertion: the loaded config is a `ThermalizeConfigGNS` (GNS type preserved).
- Key assertion: `loaded.config.with_coherent == false`.

**Test group 3: "LiouvConfig round-trip"**
- Create a LiouvConfig (no mixing_time/delta).
- Same pattern: save, load, verify.
- Key assertion: loaded config is a `LiouvConfig` (not thermalize).
- Key assertion: config has no mixing_time field (or it's absent from the reconstructed config).

**Test group 4: "LiouvConfigGNS round-trip"**
- Create a LiouvConfigGNS.
- Save, load, verify GNS type preserved.

**Test group 5: "TrajectoryResult with observables"**
- Create a TrajectoryResult with non-nothing times and measurements_mean.
- Save/load ExperimentResult.
- Assert: times and measurements_mean round-trip correctly.

**Test group 6: "Forward compatibility - missing fields"**
- Manually create a BSON Dict that is missing `:metadata` and `:hamiltonian_params` keys.
- Write it with `BSON.bson(path, d)`.
- Load with `load_experiment(path)`.
- Assert: no error, metadata and hamiltonian_params are empty Dicts.

**Test group 7: "Companion .txt file"**
- Save an ExperimentResult.
- Assert: `.txt` file exists alongside `.bson` file.
- Read the `.txt` file contents.
- Assert: contains expected strings like "QuantumFurnace", the domain name, num_qubits value.

**Test group 8: "Filename generation"**
- Test `_generate_experiment_filename` (via `QuantumFurnace._generate_experiment_filename`) with a KMS TrotterDomain config.
- Assert: filename matches pattern `kms_n{N}_beta{B}_trotter_{date}.bson`.
- Test with a GNS config: assert starts with "gns_".

**Important:** Use `QuantumFurnace._extract_hamiltonian_params`, `QuantumFurnace._generate_experiment_filename`, etc. for internal functions (prefixed with `_`).
  </action>
  <verify>
`julia --project -e "using Pkg; Pkg.test()"` passes with the new test file included.
Count of test groups: at least 7 @testset blocks in test_results.jl.
  </verify>
  <done>All config types (LiouvConfig, LiouvConfigGNS, ThermalizeConfig, ThermalizeConfigGNS) round-trip correctly through BSON. Forward compatibility (missing fields) works. Companion .txt is verified. Filename generation is verified.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test with real trajectory run and runtests.jl update</name>
  <files>test/test_results.jl, test/runtests.jl</files>
  <action>
**Add to `test/test_results.jl`:**

**Test group 9: "Integration: save/load real trajectory result"**
- Use the SMALL test fixtures (SMALL_HAM, SMALL_JUMPS, SMALL_TROTTER from test_helpers.jl).
- Create a ThermalizeConfig for TrotterDomain (like the trajectory tests do -- see test_trajectory_fixes.jl or test_gns_trajectory.jl for patterns).
- Run `run_trajectories(SMALL_JUMPS, config, psi0, SMALL_HAM; trotter=SMALL_TROTTER, ntraj=10, seed=12345)` with a small number of trajectories.
- Extract hamiltonian_params via `QuantumFurnace._extract_hamiltonian_params(SMALL_HAM)`.
- Capture metadata via `QuantumFurnace._capture_metadata(wall_time_seconds=0.5)`.
- Construct ExperimentResult from the trajectory result, config, ham params, and metadata.
- Save to tempdir.
- Load back.
- Assert: `isapprox(loaded.trajectory_result.rho_mean, original.trajectory_result.rho_mean; atol=0)` (exact match since no floating point computation, just serialization).
- Assert: `loaded.trajectory_result.n_trajectories == 10`.
- Assert: `loaded.trajectory_result.seed == 12345`.
- Assert: loaded config matches (beta, sigma, domain type, num_qubits).
- Assert: loaded metadata contains :julia_version, :timestamp, :git_hash keys.
- Assert: loaded hamiltonian_params contains :base_coeffs, :periodic keys.

**Test group 10: "Metadata auto-capture"**
- Call `QuantumFurnace._capture_metadata(n_threads=2, wall_time_seconds=3.14)`.
- Assert: returned Dict has keys :julia_version, :timestamp, :git_hash, :n_threads, :wall_time_seconds.
- Assert: :julia_version == string(VERSION).
- Assert: :n_threads == 2.
- Assert: :wall_time_seconds == 3.14.
- Assert: :git_hash is a String (either a hex hash or "unknown").
- Assert: :timestamp is a non-empty String.

**Update `test/runtests.jl`:**
- Add `include("test_results.jl")` in the appropriate location (after the existing test includes, near the end before any closing statements). Follow the existing pattern of how other test files are included.

**Run full test suite** to verify no regressions:
`julia --project -e "using Pkg; Pkg.test()"`
All tests must pass (existing ~284 tests + new results tests).
  </action>
  <verify>
1. `julia --project -e "using Pkg; Pkg.test()"` passes with all tests (existing + new).
2. `grep "test_results" test/runtests.jl` confirms inclusion.
3. New test file has at least 10 @testset blocks total.
  </verify>
  <done>Full integration test proves end-to-end: real trajectory run -> save -> load -> fields match. Metadata auto-capture verified. All existing tests continue to pass. New test file is included in runtests.jl.</done>
</task>

</tasks>

<verification>
1. `julia --project -e "using Pkg; Pkg.test()"` -- ALL tests pass (existing + new results tests).
2. Phase success criterion 1: "An experiment result containing config, convergence history, observable time series, and final density matrix can be saved to a BSON file" -- verified by integration test saving a real trajectory ExperimentResult.
3. Phase success criterion 2: "A saved experiment result can be loaded back and all fields match the original" -- verified by round-trip tests for all 4 config types with exact field comparison.
4. Phase success criterion 3: "Saved files include sufficient metadata (seed, thread count, Julia version, timestamp) to reproduce or contextualize the result" -- verified by metadata auto-capture test and integration test.
</verification>

<success_criteria>
- All round-trip tests pass for LiouvConfig, LiouvConfigGNS, ThermalizeConfig, ThermalizeConfigGNS
- Forward compatibility test passes (missing fields don't cause errors)
- Companion .txt file is created and contains expected content
- Filename generation produces correct format
- Integration test with real trajectory data passes with exact matrix match
- Metadata auto-capture includes all required fields
- Full test suite passes with no regressions (existing ~284 tests + new results tests)
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-architecture/15-02-SUMMARY.md`
</output>
