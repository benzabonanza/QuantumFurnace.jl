---
phase: 09-type-parameterization
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/furnace.jl
  - src/furnace_utensils.jl
  - src/jump_workers.jl
  - src/trajectories.jl
  - src/coherent.jl
  - src/qi_tools.jl
  - src/ofts.jl
  - src/energy_domain.jl
  - src/bohr_domain.jl
  - src/misc_tools.jl
autonomous: true

must_haves:
  truths:
    - "Simulation functions (run_lindbladian, run_thermalization, run_trajectories) accept HamHam{T} and produce results with matching T"
    - "All function signatures that previously hardcoded ComplexF64/Float64 now accept Complex{T}/T generically"
    - "The full simulation pipeline works end-to-end with Float64 (verified by all 224 tests passing)"
    - "No type instabilities introduced -- functions infer T from their HamHam/Config inputs"
    - "Cross-struct T mismatch between Config{D,T1} and HamHam{T2} where T1!=T2 produces a clear error"
  artifacts:
    - path: "src/furnace.jl"
      provides: "run_lindbladian, run_thermalization, construct_lindbladian with generic T signatures"
    - path: "src/jump_workers.jl"
      provides: "jump_contribution! functions accepting generic Complex{T} matrices and workspaces"
    - path: "src/trajectories.jl"
      provides: "build_trajectoryframework, step_along_trajectory!, run_trajectories with generic T"
    - path: "src/furnace_utensils.jl"
      provides: "precompute_data functions returning data consistent with input T"
    - path: "src/coherent.jl"
      provides: "coherent term computation functions with generic T"
  key_links:
    - from: "src/furnace.jl"
      to: "src/jump_workers.jl"
      via: "run_thermalization calls jump_contribution! with evolving_dm matching T"
      pattern: "jump_contribution!"
    - from: "src/furnace.jl"
      to: "src/furnace_utensils.jl"
      via: "precompute_data called with config and ham_or_trott"
      pattern: "precompute_data"
    - from: "src/trajectories.jl"
      to: "src/jump_workers.jl"
      via: "build_trajectoryframework uses KrausScratch and precompute_R"
      pattern: "precompute_R|KrausScratch"
---

<objective>
Propagate type parameter T through all simulation function signatures, ensuring the full pipeline from HamHam{T} through computation to results is type-consistent.

Purpose: Plans 01-02 parameterized the structs. This plan updates all functions that construct or consume those structs to work generically with T, completing the type parameterization phase.

Output: All simulation code works generically with T<:AbstractFloat. Full test suite passes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-type-parameterization/09-CONTEXT.md
@.planning/phases/09-type-parameterization/09-01-SUMMARY.md
@.planning/phases/09-type-parameterization/09-02-SUMMARY.md
@src/furnace.jl
@src/furnace_utensils.jl
@src/jump_workers.jl
@src/trajectories.jl
@src/coherent.jl
@src/qi_tools.jl
@src/ofts.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update jump_workers.jl, qi_tools.jl, and coherent.jl for generic T</name>
  <files>src/jump_workers.jl, src/qi_tools.jl, src/coherent.jl, src/ofts.jl, src/energy_domain.jl, src/bohr_domain.jl</files>
  <action>
**jump_workers.jl:**

Update all `jump_contribution!` function signatures. Currently they hardcode `ComplexF64`:
- `L_target::AbstractMatrix{ComplexF64}` -> `L_target::AbstractMatrix{<:Complex}`
- `evolving_dm::Matrix{ComplexF64}` -> `evolving_dm::Matrix{<:Complex}` or `Matrix{Complex{T}}`
- `ws::LindbladianWorkspace` -> `ws::LindbladianWorkspace` (unchanged -- Julia dispatch on the parameterized type)
- `scratch::KrausScratch{ComplexF64}` -> `scratch::KrausScratch{<:Complex}` or `scratch::KrausScratch`
- `coherent_term::Union{Nothing, AbstractMatrix{ComplexF64}}` -> `Union{Nothing, AbstractMatrix{<:Complex}}`
- `coherent_unitary_cache::Union{Nothing,Matrix{ComplexF64}}` -> `Union{Nothing,Matrix{<:Complex}}`

Update `apply_coherent_unitary!` signature from hardcoded ComplexF64 to generic.
Update `finalize_kraus_step!` signature from hardcoded ComplexF64 to generic.

In `finalize_kraus_step!`, the line `U_residual = Matrix{ComplexF64}(...)` should become `Matrix{CT}(...)` where CT is inferred from the evolving_dm element type.

Strategy: Use `eltype(evolving_dm)` to infer CT where needed for allocations inside functions. For dispatch, use `<:Complex` in type annotations to accept any complex type.

**qi_tools.jl:**

Update `vectorize_liouv_diss_and_add!` signatures:
- `jump::AbstractMatrix{ComplexF64}` -> `jump::AbstractMatrix{<:Complex}`
- `L_target::AbstractMatrix{ComplexF64}` -> `L_target::AbstractMatrix{<:Complex}`
- `ws::LindbladianWorkspace` stays as-is (no type annotation on the parameter)

Update `vectorize_liouvillian_coherent!` similarly.

The `kron!` function is already generic (uses `AbstractMatrix` and `Number`).

`hermitianize!` is already generic (`AbstractMatrix`).

`transform_jumps_to_basis` is already generic.

`trace_distance_h`, `trace_distance_nh`, etc. are already generic (use `Union{Hermitian{<:Real}, Hermitian{<:Complex}}`).

`gibbs_state` and `gibbs_state_in_eigen`: update `Matrix{ComplexF64}` return type to be generic. These functions take `HamHam` (now `HamHam{T}`) -- infer T from hamiltonian.

**coherent.jl:**

Update `precompute_coherent_total_B` return type from `Union{Nothing, Matrix{ComplexF64}}` to `Union{Nothing, Matrix{<:Complex}}`.

Update `precompute_coherent_unitary_terms` similarly.

Update internal functions (`B_time`, `B_trotter`, `coherent_bohr`) to work with generic T. These functions access `hamiltonian.eigvals` (now `Vector{T}`) and `hamiltonian.bohr_freqs` (now `Matrix{T}`). Most computations use `exp`, `sqrt`, etc. which Julia dispatches correctly for any AbstractFloat.

**ofts.jl:**

Update `oft!` function to accept generic types instead of hardcoded ComplexF64/Float64 in its matrix arguments.

**energy_domain.jl and bohr_domain.jl:**

Update function signatures that reference `Float64` or `ComplexF64` explicitly. Key functions:
- `create_energy_labels` -- likely takes Int and Float64; update to accept T
- `create_alpha` / `create_alpha_gns` -- update Float64 type annotations
- `pick_transition` -- returns a function; should work generically
- `pick_alpha` -- returns a function; should work generically

For `create_bohr_dict` (wherever it lives): accept `Matrix{T}` instead of `Matrix{Float64}`.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests must pass.
Spot-check type stability: `julia --project -e 'using QuantumFurnace; @code_warntype hermitianize!(zeros(ComplexF64, 4, 4))'` should show no type instabilities.
  </verify>
  <done>
All jump_workers, qi_tools, coherent, OFT, and domain functions accept generic T types. No hardcoded ComplexF64/Float64 in function signatures (except where semantically correct, e.g., Float64 constants like pi). Type inference works correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update furnace.jl, furnace_utensils.jl, trajectories.jl, and verify full pipeline</name>
  <files>src/furnace.jl, src/furnace_utensils.jl, src/trajectories.jl, src/misc_tools.jl</files>
  <action>
**furnace.jl:**

`run_lindbladian`: Update signature to accept `HamHam{T}` generically (no explicit type annotation needed -- just remove `ComplexF64` from internal allocations):
- `total_lindbladian = zeros(ComplexF64, dim^2, dim^2)` -> infer CT from hamiltonian: `CT = Complex{eltype(hamiltonian.eigvals)}; zeros(CT, dim^2, dim^2)`
- `ws = LindbladianWorkspace(dim)` -> `ws = LindbladianWorkspace{eltype(hamiltonian.eigvals)}(dim)` or keep the default (Float64) since the Liouvillian construction is typically Float64.

`construct_lindbladian`: Same pattern -- infer element type from hamiltonian.

`run_thermalization`:
- `evolving_dm::Matrix{ComplexF64}` -> `evolving_dm::Matrix{<:Complex}` in signature
- `scratch = KrausScratch(ComplexF64, dim)` -> infer from evolving_dm: `KrausScratch(eltype(evolving_dm), dim)`
- `gibbs = Hermitian(...)` computations should work generically

**furnace_utensils.jl:**

`precompute_data` functions: These compute Float64 quantities from config fields. Since config fields are now `T`, the computations naturally produce T. Most internal math uses `sqrt`, `pi`, etc. which Julia handles for any AbstractFloat.

`select_b_plus_calculator`: Already generic (returns tuples of config fields).

`validate_config!`: Float comparisons like `isapprox(config.beta, rhs)` work for any T.

`print_press`: Just printing, works for any type.

**trajectories.jl:**

`TrajectoryWorkspace` and `PerOperatorKraus` are already parameterized on T.

`TrajectoryFramework{T,D}`: Currently T=ComplexF64. The T parameter here is the complex element type. After this change, T should still represent the complex type (Complex{Float}), matching the existing pattern. No change to the struct definition needed.

`build_trajectoryframework`:
- `scratch::KrausScratch{ComplexF64}` -> `scratch::KrausScratch{<:Complex}`
- `delta::Float64` -> `delta` (let Julia infer)
- Internal allocations: `Vector{Union{Nothing, Matrix{ComplexF64}}}` -> infer from ham_or_trott
- `ws = TrajectoryWorkspace(ComplexF64, dim)` -> infer CT

`step_along_trajectory!`:
- `psi::Vector{ComplexF64}` -> `psi::Vector{<:Complex}`
- `fw::TrajectoryFramework{ComplexF64,D}` -> `fw::TrajectoryFramework{<:Complex,D}` or just `fw::TrajectoryFramework`

`run_trajectories`:
- `psi0::Vector{ComplexF64}` -> `psi0::Vector{<:Complex}`
- Internal allocations: use `eltype(psi0)` to infer CT
- `rho_mean = zeros(ComplexF64, dim, dim)` -> `zeros(eltype(psi0), dim, dim)`

`_evolve_along_trajectory!`, `_accumulate_density_matrix!`, `_accumulate_measurements!`, `_norm2`:
- Update type annotations from `ComplexF64` to `<:Complex`

**misc_tools.jl:**

`validate_config!` and `_collect_config_errors!`: Already use `AbstractConfig` dispatch, should work.

`print_press`: Already generic.

`expm_pauli_padded`, `pad_term`: These use `ComplexF64` from the Pauli constants. Leave as-is -- Pauli matrices are always ComplexF64 (X, Y, Z constants). Trotterization is a one-time precomputation.

**Cross-struct T mismatch check:**

Add a validation in `run_lindbladian` and `run_thermalization`: if the HamHam's T doesn't match the Config's T, throw an error:
```julia
# At the top of run_lindbladian / run_thermalization:
if eltype(hamiltonian.eigvals) !== typeof(config.beta)
    error("Type mismatch: HamHam uses $(eltype(hamiltonian.eigvals)) but config uses $(typeof(config.beta)). All structs must share the same precision.")
end
```
Per locked decision: "Cross-struct must match: All structs in a simulation must share the same T".

**Final verification:**

After all changes, run `Pkg.test()` to confirm all 224 tests pass. The tests all use Float64 (the default), so they exercise the backward-compatibility path.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all 224 tests MUST pass.
Verify cross-struct mismatch detection works (manual test or add an assertion in tests).
Verify the full simulation pipeline: `run_lindbladian`, `run_thermalization`, `run_trajectories` all accept the new parameterized types and produce correct results.
  </verify>
  <done>
All simulation functions work generically with T<:AbstractFloat. Cross-struct T mismatch produces clear error. Full simulation pipeline tested end-to-end via all 224 tests. Type parameterization is complete across the entire codebase.
  </done>
</task>

</tasks>

<verification>
1. All 224 existing tests pass with `Pkg.test()`
2. `run_lindbladian` accepts `HamHam{Float64}` and produces `HotSpectralResults{D,Float64}`
3. `run_thermalization` accepts `HamHam{Float64}` and produces `HotAlgorithmResults{D,Float64}`
4. `run_trajectories` accepts `HamHam{Float64}` and returns NamedTuple with Float64-typed arrays
5. Mismatched T between HamHam and Config throws a clear error
6. No hardcoded ComplexF64/Float64 remain in function signatures (except Pauli constants and BSON-specific code)
</verification>

<success_criteria>
- All simulation functions accept generic T types
- Cross-struct T mismatch detection works
- Full backward compatibility: all existing Float64 code works unchanged
- All 224 tests pass
- Phase 9 success criteria met: HamHam{T}, LindbladianWorkspace{T}, Config{D,T} all parameterized
</success_criteria>

<output>
After completion, create `.planning/phases/09-type-parameterization/09-03-SUMMARY.md`
</output>
