---
phase: 17-adaptive-sampling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/convergence.jl, src/results.jl, src/QuantumFurnace.jl]
autonomous: true

must_haves:
  truths:
    - "run_trajectories_adaptive stops early when trace distance relative change is below threshold for patience consecutive batches"
    - "run_trajectories_adaptive runs up to n_max trajectories and returns converged=false when convergence is not reached"
    - "run_trajectories_adaptive returns the same (TrajectoryResult, ConvergenceData) tuple as run_trajectories_convergence"
    - "Existing run_trajectories_convergence and all Phase 16 tests pass unchanged (backward-compatible ConvergenceData extension)"
    - "ConvergenceData serialization round-trips correctly for both old (6-field) and new (10-field) data"
  artifacts:
    - path: "src/convergence.jl"
      provides: "Extended ConvergenceData struct (4 new fields), _windowed_relative_change helper, run_trajectories_adaptive function"
      contains: "run_trajectories_adaptive"
    - path: "src/results.jl"
      provides: "Updated _convergence_to_dict and _dict_to_convergence with new adaptive diagnostic fields"
      contains: "converged"
    - path: "src/QuantumFurnace.jl"
      provides: "Export of run_trajectories_adaptive"
      contains: "run_trajectories_adaptive"
  key_links:
    - from: "src/convergence.jl"
      to: "src/trajectories.jl"
      via: "run_trajectories called per batch in adaptive loop"
      pattern: "run_trajectories\\("
    - from: "src/convergence.jl"
      to: "src/results.jl"
      via: "ConvergenceData struct used in _convergence_to_dict/_dict_to_convergence"
      pattern: "ConvergenceData"
    - from: "src/convergence.jl"
      to: "src/qi_tools.jl"
      via: "trace_distance_h and hermitianize! called per batch checkpoint"
      pattern: "trace_distance_h|hermitianize!"
---

<objective>
Implement the adaptive sampling layer: extend ConvergenceData with adaptive diagnostic fields, create the _windowed_relative_change helper for convergence detection, implement run_trajectories_adaptive that wraps the Phase 16 batch-loop pattern with automatic stopping, and update Dict serialization for backward compatibility.

Purpose: Enables users to run trajectory sampling without specifying a fixed trajectory count -- the system monitors trace distance convergence and stops automatically (CONV-04, CONV-05).

Output: Extended ConvergenceData struct, _windowed_relative_change helper, run_trajectories_adaptive function, updated serialization functions, module export.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-adaptive-sampling/17-RESEARCH.md
@.planning/phases/16-convergence-tracking/16-01-SUMMARY.md
@src/convergence.jl
@src/results.jl
@src/QuantumFurnace.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ConvergenceData, implement _windowed_relative_change and run_trajectories_adaptive</name>
  <files>src/convergence.jl</files>
  <action>
  Extend the ConvergenceData struct and add the adaptive sampling function. All changes in convergence.jl only.

  **1. Extend ConvergenceData struct (add 4 new fields at the end):**

  Add after the existing 6 fields:
  - `converged::Bool` -- did adaptive stopping trigger?
  - `final_relative_change::Float64` -- relative change at termination
  - `consecutive_stable_batches::Int` -- how many consecutive stable checks achieved
  - `total_batches::Int` -- number of batches actually run

  Update the docstring to document the new fields, noting they are Phase 17 adaptive diagnostics.

  **Add a backward-compatible 6-argument outer constructor** so existing code (run_trajectories_convergence) that constructs ConvergenceData with 6 positional args still works. The outer constructor fills defaults: `converged=false`, `final_relative_change=NaN`, `consecutive_stable_batches=0`, `total_batches=length(batch_sizes)`.

  **2. Add `_windowed_relative_change` helper function** (private, not exported):

  ```julia
  function _windowed_relative_change(trace_dists::Vector{Float64}, window_size::Int)
      n = length(trace_dists)
      n < 2 * window_size && return Inf
      mean_recent = sum(@view trace_dists[n - window_size + 1 : n]) / window_size
      mean_previous = sum(@view trace_dists[n - 2*window_size + 1 : n - window_size]) / window_size
      return abs(mean_recent - mean_previous) / max(abs(mean_previous), eps(Float64))
  end
  ```

  Place this helper between the Gibbs helpers section and the main convergence runner section. Add a brief docstring.

  **3. Add `run_trajectories_adaptive` function:**

  The function signature (per CONTEXT.md locked decisions):
  ```julia
  function run_trajectories_adaptive(
      jumps::Vector{JumpOp},
      config::AbstractThermalizeConfig,
      psi0::Vector{<:Complex},
      hamiltonian::HamHam;
      gibbs::Hermitian,
      observables::Vector{<:Matrix{<:Complex}},
      observable_names::Vector{String},
      batch_size::Int = 200,
      n_max::Int = 20_000,
      convergence_threshold::Float64 = 0.01,
      patience::Int = 3,
      min_batches::Int = 5,
      window_size::Int = 3,
      seed::Union{Int,Nothing} = nothing,
      trotter::Union{TrottTrott,Nothing} = nothing,
      total_time::Real = config.mixing_time,
      delta::Real = config.delta,
  )
  ```

  Implementation details (follow research patterns exactly):
  - Compute `max_batches = cld(n_max, batch_size)` (ceiling division, accept slight n_max overshoot per research recommendation)
  - Compute `effective_min = max(min_batches, 2 * window_size)` to ensure enough data for windowed comparison
  - Generate `actual_seed` from system entropy if seed is nothing, same as run_trajectories_convergence
  - Use **push!-based dynamic storage** (not pre-allocated): `trace_dists = Float64[]`, `obs_values_list = Vector{Float64}[]`, `cum_n_traj = Int[]`, `batch_sizes_vec = Int[]`
  - Accumulate `rho_acc` with same pattern as Phase 16: `rho_acc .+= result.rho_mean .* batch_size`
  - Seed management: `batch_seed = actual_seed + n_total` (identical to Phase 16 pattern)
  - Per-batch: call `run_trajectories(jumps, config, psi0, hamiltonian; trotter, total_time, delta, ntraj=batch_size, seed=batch_seed)`, accumulate, hermitianize running average, measure trace distance, measure observable values as `[real(tr(rho_running * observables[i])) for i in 1:n_obs]`, push to dynamic arrays
  - Convergence check: only after `batch_idx >= effective_min`, compute `_windowed_relative_change(trace_dists, window_size)`, increment `consecutive_stable` if below threshold (else reset to 0), break if `consecutive_stable >= patience`
  - After loop: compute final `rho_final`, convert `obs_values_list` to matrix via `reduce(hcat, obs_values_list)`, construct full 10-field ConvergenceData with adaptive diagnostics populated
  - Return `(TrajectoryResult, ConvergenceData)` -- same signature as run_trajectories_convergence

  Place this function after `run_trajectories_convergence`. Add a comprehensive docstring documenting all keyword arguments and the convergence criterion.

  **Key locked decisions to honor:**
  - Stop trigger: trace distance only (observables tracked but do NOT gate stopping)
  - Windowed average: compare mean of last K batches vs mean of previous K batches
  - 3 consecutive stable checks required (configurable via patience kwarg)
  - Minimum 5 batches before stopping (configurable via min_batches kwarg)
  - Return immediately when converged (no extra confirmation batches)
  - Fixed batch size throughout
  - Default batch_size=200, n_max=20_000, threshold=0.01, patience=3, min_batches=5, window_size=3
  </action>
  <verify>
  Run `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e "using QuantumFurnace; println(\"Module loads OK\")"` -- should load without error.

  Run existing test suite: `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e "using Pkg; Pkg.test()"` -- all 470 existing tests must pass (backward-compatible ConvergenceData constructor).
  </verify>
  <done>
  - ConvergenceData struct has 10 fields (6 original + 4 new adaptive diagnostics)
  - 6-argument backward-compatible outer constructor exists and is used by existing run_trajectories_convergence
  - `_windowed_relative_change` returns Inf when < 2*window_size data points, else correct relative change
  - `run_trajectories_adaptive` function exists with all kwargs matching locked decisions
  - All 470 existing tests pass unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update serialization and module exports for adaptive sampling</name>
  <files>src/results.jl, src/QuantumFurnace.jl</files>
  <action>
  **1. Update `_convergence_to_dict` in results.jl** to include the 4 new fields:
  - Add `:converged => conv.converged`
  - Add `:final_relative_change => conv.final_relative_change`
  - Add `:consecutive_stable_batches => conv.consecutive_stable_batches`
  - Add `:total_batches => conv.total_batches`

  **2. Update `_dict_to_convergence` in results.jl** to handle new fields with backward-compatible defaults:
  - Use `get(d, :converged, false)` for the converged field
  - Use `get(d, :final_relative_change, NaN)` for final_relative_change
  - Use `get(d, :consecutive_stable_batches, 0)` for consecutive_stable_batches
  - Use `get(d, :total_batches, length(d[:batch_sizes]))` for total_batches
  - Call the full 10-argument ConvergenceData constructor (inner constructor)

  This ensures:
  - New files saved with adaptive data serialize completely (no lost diagnostics)
  - Old BSON files (pre-Phase 17, no adaptive fields) load correctly with sensible defaults

  **3. Update export in QuantumFurnace.jl:**
  - Add `run_trajectories_adaptive` to the convergence tracking export line
  - The existing export line is: `export ConvergenceData, run_trajectories_convergence, build_convergence_observables, build_convergence_observables_trotter`
  - Change to: `export ConvergenceData, run_trajectories_convergence, run_trajectories_adaptive, build_convergence_observables, build_convergence_observables_trotter`
  </action>
  <verify>
  Run `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e "using QuantumFurnace; @assert :run_trajectories_adaptive in names(QuantumFurnace); println(\"Export OK\")"` -- confirms the new function is exported.

  Run full test suite: `cd /Users/bence/code/QuantumFurnace.jl && julia --project -e "using Pkg; Pkg.test()"` -- all 470 existing tests pass (serialization backward compatibility).
  </verify>
  <done>
  - `_convergence_to_dict` serializes all 10 ConvergenceData fields including adaptive diagnostics
  - `_dict_to_convergence` loads both old (6 keys) and new (10 keys) Dicts correctly using `get` with defaults
  - `run_trajectories_adaptive` is exported from QuantumFurnace module
  - All 470 existing tests pass unchanged
  </done>
</task>

</tasks>

<verification>
1. `julia --project -e "using QuantumFurnace"` loads without error
2. `julia --project -e "using Pkg; Pkg.test()"` -- all 470 existing tests pass
3. `run_trajectories_adaptive` is in `names(QuantumFurnace)` (exported)
4. ConvergenceData can be constructed with both 6 args (backward compat) and 10 args (full)
5. `_windowed_relative_change([0.5, 0.4, 0.35, 0.33, 0.32, 0.315], 3)` returns a finite value less than 1
6. `_convergence_to_dict` / `_dict_to_convergence` round-trips with adaptive fields
</verification>

<success_criteria>
- ConvergenceData extended with converged, final_relative_change, consecutive_stable_batches, total_batches
- Backward-compatible 6-argument constructor keeps all existing code working
- _windowed_relative_change helper correctly computes windowed relative change
- run_trajectories_adaptive implements adaptive batch loop with all locked-decision parameters
- Serialization handles both old and new ConvergenceData formats
- run_trajectories_adaptive is exported
- All 470 existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/17-adaptive-sampling/17-01-SUMMARY.md`
</output>
