---
status: testing
phase: v2.1 (39-42)
source: 39-01-SUMMARY.md, 39-02-SUMMARY.md, 40-01-SUMMARY.md, 41-01-SUMMARY.md, 41-02-SUMMARY.md, 42-01-SUMMARY.md, 42-02-SUMMARY.md
started: 2026-03-01T13:30:00Z
updated: 2026-03-01T13:30:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 2
name: save_every keyword works
expected: |
  `run_thermalize` accepts `save_every=10` and returns a ThermalizeResults with shorter trace_distances array (roughly num_steps/10 entries instead of num_steps). Default save_every=1 produces same results as before.
awaiting: user response

## Tests

### 1. Full test suite passes
expected: `julia --project -e 'using Pkg; Pkg.test()'` completes with 1246 tests passing, 0 failures.
result: issue
reported: "TVAL-02 EnergyDomain single-step cross-validation fails: 0.0104 < 0.01. Also duplicate test_helpers.jl include causes method redefinition warning."
severity: major

### 2. save_every keyword works
expected: `run_thermalize` accepts `save_every=10` and returns a ThermalizeResults with shorter trace_distances array (roughly num_steps/10 entries instead of num_steps). Default save_every=1 produces same results as before.
result: [pending]

### 3. estimate_mixing_time returns MixingTimeEstimate
expected: `estimate_mixing_time(result)` on a ThermalizeResults returns a MixingTimeEstimate with fields: fitted_gap, mixing_time, R_squared, offset, amplitude, fit_result, target_epsilon, extrapolated, steps_to_target, confidence_intervals, converged, warnings.
result: [pending]

### 4. Mixing time extrapolation mode
expected: `estimate_mixing_time(result; extrapolate=true)` returns a MixingTimeEstimate where `extrapolated == true` and `mixing_time` reflects the extrapolated crossing point rather than actual simulation length.
result: [pending]

### 5. LsqFit.jl is an active dependency
expected: `using QuantumFurnace` loads without error and `fit_exponential_decay` and `FitResult` are exported symbols accessible from the module.
result: [pending]

### 6. BLAS thread restoration after run_thermalize
expected: BLAS thread count before and after calling run_thermalize is identical -- no thread state leak. Verified by checking `BLAS.get_num_threads()` before and after.
result: [pending]

### 7. Precomputation produces identical physics
expected: run_thermalize trace distances for all 4 domains (Energy, Time, Trotter, Bohr) decrease monotonically from maximally mixed state toward Gibbs state. Results match pre-v2.1 behavior within floating-point tolerance.
result: [pending]

## Summary

total: 7
passed: 0
issues: 1
pending: 6
skipped: 0

## Gaps

- truth: "Full test suite passes with 0 failures"
  status: failed
  reason: "User reported: TVAL-02 EnergyDomain single-step cross-validation fails: 0.0104 < 0.01. Also duplicate test_helpers.jl include causes method redefinition warning."
  severity: major
  test: 1
  artifacts: []
  missing: []
  debug_session: ""
