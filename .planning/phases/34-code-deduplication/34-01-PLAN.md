---
phase: 34-code-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/furnace_utensils.jl
  - src/ofts.jl
  - src/jump_workers.jl
  - src/krylov_workspace.jl
  - src/krylov_matvec.jl
  - src/krylov_eigsolve.jl
  - src/trajectories.jl
autonomous: true

must_haves:
  truths:
    - "domain_prefactor(::EnergyDomain, w0, sigma) returns w0/(sigma*sqrt(2*pi)) and is called from all 5 simulation files instead of inline formula"
    - "domain_prefactor(::TimeDomain, w0, sigma, t0) and domain_prefactor(::TrotterDomain, w0, sigma, t0) return identical time-domain formula"
    - "No domain_prefactor method exists for BohrDomain -- BohrDomain callers use gamma_norm_factor directly"
    - "A single oft!(out, eigenbasis, bohr_freqs, energy, inv_4sigma2) replaces both the old JumpOp-based oft! (deleted) and _krylov_oft! (deleted)"
    - "time_oft! and trotter_oft! remain unchanged as test/debug utilities in ofts.jl"
    - "All regression, allocation, and Krylov cross-validation tests pass with identical numerical results"
  artifacts:
    - path: "src/furnace_utensils.jl"
      provides: "domain_prefactor function definitions and precomputed_data integration"
      contains: "domain_prefactor"
    - path: "src/ofts.jl"
      provides: "Unified oft! with concrete-typed signature"
      contains: "function oft!"
  key_links:
    - from: "src/furnace_utensils.jl"
      to: "src/jump_workers.jl, src/krylov_matvec.jl, src/krylov_eigsolve.jl, src/krylov_workspace.jl, src/trajectories.jl"
      via: "precomputed_data.domain_prefactor field"
      pattern: "precomputed_data\\.domain_prefactor"
    - from: "src/ofts.jl"
      to: "src/jump_workers.jl, src/trajectories.jl, src/krylov_workspace.jl, src/krylov_matvec.jl, src/krylov_eigsolve.jl"
      via: "unified oft! call replacing both old oft! and _krylov_oft!"
      pattern: "oft!"
---

<objective>
Extract `domain_prefactor()` as a single-source function and unify `oft!` with `_krylov_oft!` into one optimized function, replacing 16 inline prefactor formulas and 2 duplicate OFT implementations.

Purpose: Eliminate the two most pervasive copy-paste patterns in the codebase -- the domain-dependent prefactor formula (16 copies across 5 files) and the duplicate OFT functions (functionally identical `oft!` and `_krylov_oft!`).
Output: `domain_prefactor` function in furnace_utensils.jl, unified `oft!` in ofts.jl, all callers updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-code-deduplication/34-RESEARCH.md
@.planning/phases/34-code-deduplication/34-CONTEXT.md
@src/furnace_utensils.jl
@src/ofts.jl
@src/jump_workers.jl
@src/krylov_workspace.jl
@src/krylov_matvec.jl
@src/krylov_eigsolve.jl
@src/trajectories.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract domain_prefactor and store in precomputed_data</name>
  <files>src/furnace_utensils.jl, src/jump_workers.jl, src/krylov_workspace.jl, src/krylov_matvec.jl, src/krylov_eigsolve.jl, src/trajectories.jl</files>
  <action>
1. **Add `domain_prefactor` functions to furnace_utensils.jl** (top of file, before `_precompute_labels`):
   ```julia
   domain_prefactor(::EnergyDomain, w0::Real, sigma::Real) = w0 / (sigma * sqrt(2 * pi))
   domain_prefactor(::TimeDomain, w0::Real, sigma::Real, t0::Real) = w0 * t0^2 * (sigma * sqrt(2 / pi)) / (2 * pi)
   domain_prefactor(::TrotterDomain, w0::Real, sigma::Real, t0::Real) = w0 * t0^2 * (sigma * sqrt(2 / pi)) / (2 * pi)
   ```
   No method for BohrDomain -- BohrDomain callers use `gamma_norm_factor` directly per user decision.

2. **Store `domain_prefactor` in `precomputed_data` NamedTuple** for EnergyDomain and Time/TrotterDomain `_precompute_data` functions:
   - In `_precompute_data(config::Config{<:Any, EnergyDomain}, ...)`: compute `dp = domain_prefactor(config.domain, config.w0, config.sigma)` and add `domain_prefactor = dp` to the returned NamedTuple.
   - In `_precompute_data(config::Config{<:Any, D}, ...) where {D<:Union{TimeDomain, TrotterDomain}}`: compute `dp = domain_prefactor(config.domain, config.w0, config.sigma, config.t0)` and add `domain_prefactor = dp` to the returned NamedTuple.
   - Do NOT add to BohrDomain `_precompute_data` functions.

3. **Update all 16 call sites** to use `precomputed_data.domain_prefactor` instead of inline formula:

   **jump_workers.jl** (4 sites):
   - Lindbladian EnergyDomain `_jump_contribution!` (~line 65): Replace `(config.w0 / (config.sigma * sqrt(2 * pi))) * gamma_norm_factor` with `precomputed_data.domain_prefactor * gamma_norm_factor`. Note: need to destructure `domain_prefactor` from precomputed_data or access via dot.
   - Lindbladian Time/TrotterDomain `_jump_contribution!` (~line 109): Replace `config.w0 * config.t0^2 * (config.sigma * sqrt(2 / pi)) / (2 * pi) * gamma_norm_factor` with `precomputed_data.domain_prefactor * gamma_norm_factor`.
   - Thermalize EnergyDomain `_jump_contribution!` (~line 334): Replace `config.w0 / (config.sigma * sqrt(2 * pi)) * jump_weight_scaling` with `precomputed_data.domain_prefactor * jump_weight_scaling`. Note: Thermalize uses `jump_weight_scaling` not `gamma_norm_factor` -- preserve this distinction.
   - Thermalize Time/TrotterDomain `_jump_contribution!` (~line 410): Replace `config.w0 * config.t0^2 * (config.sigma * sqrt(2 / pi)) / (2 * pi) * jump_weight_scaling` with `precomputed_data.domain_prefactor * jump_weight_scaling`.

   **krylov_workspace.jl** (2 sites):
   - EnergyDomain `_accumulate_R_total!` (~line 189): Replace `(config.w0 / (config.sigma * sqrt(2 * pi))) * gamma_norm_factor` with `precomputed_data.domain_prefactor * gamma_norm_factor`. Need to destructure or access `domain_prefactor` from precomputed_data. Since this function receives `precomputed_data` as a parameter, add `domain_prefactor` to the destructuring or use `precomputed_data.domain_prefactor`.
   - Time/TrotterDomain `_accumulate_R_total!` (~line 234): Replace `config.w0 * config.t0^2 * (config.sigma * sqrt(2 / pi)) / (2 * pi) * gamma_norm_factor` with `precomputed_data.domain_prefactor * gamma_norm_factor`.

   **krylov_matvec.jl** (4 sites):
   - EnergyDomain `apply_lindbladian!` (~line 166): Replace inline formula with `ws.precomputed_data.domain_prefactor * gamma_norm_factor`.
   - EnergyDomain `apply_adjoint_lindbladian!` (~line 234): Same replacement.
   - Time/TrotterDomain `apply_lindbladian!` (~line 477): Replace inline formula with `ws.precomputed_data.domain_prefactor * gamma_norm_factor` (destructure from precomputed_data if already destructured).
   - Time/TrotterDomain `apply_adjoint_lindbladian!` (~line 547): Same replacement.

   **krylov_eigsolve.jl** (2 sites):
   - EnergyDomain `_accumulate_jump_sandwich!` (~line 231): Replace inline formula with `ws.precomputed_data.domain_prefactor * gamma_norm_factor`.
   - Time/TrotterDomain `_accumulate_jump_sandwich!` (~line 277): Same replacement.

   **trajectories.jl** (4 sites):
   - `build_trajectoryframework` EnergyDomain scaled_prefactor (~line 186): Replace `config.w0 / (config.sigma * sqrt(2 * pi)) * gamma_norm_factor / (1.0 / n_jumps)` with `precomputed_data.domain_prefactor * gamma_norm_factor / (1.0 / n_jumps)`. Note: `precomputed_data` is available at this point.
   - `build_trajectoryframework` Time/TrotterDomain scaled_prefactor (~line 189): Replace inline formula with `precomputed_data.domain_prefactor * gamma_norm_factor / (1.0 / n_jumps)`.
   - `_precompute_R` EnergyDomain base_prefactor (~line 245): Replace `config.w0 / (config.sigma * sqrt(2 * pi)) * gamma_norm_factor` with `precomputed_data.domain_prefactor * gamma_norm_factor`.
   - `_precompute_R` Time/TrotterDomain base_prefactor (~line 301): Replace inline formula with `precomputed_data.domain_prefactor * gamma_norm_factor`.

   **IMPORTANT pitfalls:**
   - Thermalize `_jump_contribution!` uses `jump_weight_scaling` not `gamma_norm_factor`. `domain_prefactor` returns only the domain-dependent scalar; the caller multiplies by its own scaling factor.
   - `trajectories.jl` `build_trajectoryframework` also divides by `(1.0 / n_jumps)` i.e. multiplies by `n_jumps`. Preserve this.
   - Do NOT change BohrDomain code at all.
   - Some destructuring patterns `(; transition, gamma_norm_factor, energy_labels) = precomputed_data` may need to add `domain_prefactor` to the destructuring or use separate access.

4. **Run tests** to verify identical numerical results.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests must pass with zero numerical regressions.
Grep for the old inline formulas to confirm they are gone:
- `grep -rn "w0 / (config.sigma \* sqrt(2" src/` should return 0 results outside furnace_utensils.jl
- `grep -rn "w0 \* config.t0\^2 \* (config.sigma" src/` should return 0 results outside furnace_utensils.jl
- `grep -rn "domain_prefactor" src/` should show furnace_utensils.jl (definition) + all 5 caller files
  </verify>
  <done>
`domain_prefactor` is defined in furnace_utensils.jl with 3 methods (Energy, Time, Trotter), stored in `precomputed_data` NamedTuple for Energy/Time/Trotter domains, and used by all 16 former inline formula sites. No inline prefactor formula remains outside furnace_utensils.jl. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify oft! and _krylov_oft! into single function</name>
  <files>src/ofts.jl, src/krylov_matvec.jl, src/krylov_eigsolve.jl, src/krylov_workspace.jl, src/jump_workers.jl, src/trajectories.jl</files>
  <action>
1. **Add new primary `oft!` signature to ofts.jl** (before the existing `oft!`):
   ```julia
   """
       oft!(out, eigenbasis, bohr_freqs, energy, inv_4sigma2) -> nothing

   Compute the Operator Fourier Transform A(omega) in-place:
       out[i,j] = eigenbasis[i,j] * exp(-(energy - bohr_freqs[i,j])^2 * inv_4sigma2)

   This is the optimized concrete-typed signature for all simulation paths.
   EnergyDomain only (Time/TrotterDomain uses NUFFT prefactors).
   """
   @inline function oft!(
       out::Matrix{T},
       eigenbasis::Matrix{T},
       bohr_freqs::Matrix{<:Real},
       energy::Real,
       inv_4sigma2::Real,
   ) where {T<:Complex}
       @. out = eigenbasis * exp(-(energy - bohr_freqs)^2 * inv_4sigma2)
       return nothing
   end
   ```

2. **Delete the old JumpOp-based `oft!` method from ofts.jl** after all callers are migrated in steps 7-9. The old signature `oft!(out_matrix, jump::JumpOp, energy, hamiltonian::HamHam, sigma)` is removed per user decision (CONTEXT.md: "Old JumpOp-based `oft!` signature: Removed"). All callers must use the new concrete-typed signature.

3. **Delete `_krylov_oft!` from krylov_matvec.jl** (lines ~1-20, the function and its docstring).

4. **Update all krylov_matvec.jl callers** (6 sites) -- replace `_krylov_oft!` with `oft!`:
   - EnergyDomain `apply_lindbladian!`: `_krylov_oft!(ws.jump_oft, eigenbasis, bohr_freqs, w, inv_4sigma2)` -> `oft!(ws.jump_oft, eigenbasis, bohr_freqs, w, inv_4sigma2)`
   - EnergyDomain `apply_adjoint_lindbladian!`: same replacement
   - (Time/TrotterDomain uses NUFFT prefactors, not OFT -- no changes needed there)

5. **Update krylov_eigsolve.jl callers** (2-3 sites) -- replace `_krylov_oft!` with `oft!`:
   - EnergyDomain `_accumulate_jump_sandwich!`: replace `_krylov_oft!` with `oft!`

6. **Update krylov_workspace.jl callers** -- the EnergyDomain `_accumulate_R_total!` currently uses inline `@. jump_oft = eigenbasis * exp(-(w - bohr_freqs)^2 * inv_4sigma2)`. Replace with `oft!(jump_oft, eigenbasis, bohr_freqs, w, inv_4sigma2)`.

7. **Update jump_workers.jl Lindbladian EnergyDomain callers** -- currently calls `oft!(jump_oft, jump, w, hamiltonian, config.sigma)`. Update to new signature: compute `inv_4sigma2 = 1.0 / (4 * config.sigma^2)` once before the loop, then call `oft!(jump_oft, jump.in_eigenbasis, hamiltonian.bohr_freqs, w, inv_4sigma2)`.

8. **Update jump_workers.jl Thermalize EnergyDomain callers** -- same pattern: compute `inv_4sigma2` once, replace `oft!(scratch.jump_oft, jump, w, hamiltonian, config.sigma)` with `oft!(scratch.jump_oft, jump.in_eigenbasis, hamiltonian.bohr_freqs, w, inv_4sigma2)`.

9. **Update trajectories.jl EnergyDomain callers** -- `_precompute_R` uses `oft!(scratch.jump_oft, jump, w, hamiltonian, config.sigma)`. Replace with new signature using `inv_4sigma2`.

10. **Preserve `time_oft!` and `trotter_oft!` unchanged** in ofts.jl -- they are clearly-marked test/debug utilities using `OFTCaches`. Do NOT modify them.

11. **Run tests** to verify identical numerical results.

**NOTE:** The old JumpOp-based `oft!` must be deleted (not kept) per user decision. After migrating all callers in steps 7-9, delete the old `function oft!(out_matrix::Matrix{<:Complex}, jump::JumpOp, ...)` method entirely from ofts.jl.
  </action>
  <verify>
Run `julia --project -e 'using Pkg; Pkg.test()'` -- all tests must pass.
Grep to verify:
- `grep -rn "_krylov_oft!" src/` should return 0 results (function deleted, all callers updated)
- `grep -rn "oft!" src/ofts.jl` should show the unified `oft!` plus `time_oft!` and `trotter_oft!` (no JumpOp-based `oft!` signature remaining)
- `grep -n "JumpOp" src/ofts.jl` should return 0 results (old JumpOp-based method deleted)
  </verify>
  <done>
A single `oft!(out, eigenbasis, bohr_freqs, energy, inv_4sigma2)` in ofts.jl replaces `_krylov_oft!`. Old JumpOp-based `oft!` is deleted from ofts.jl. All Krylov, Lindbladian, Thermalize, and Trajectory EnergyDomain callers use the new signature. `_krylov_oft!` is deleted from krylov_matvec.jl. `time_oft!` and `trotter_oft!` remain as test utilities. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. Run full test suite: `julia --project -e 'using Pkg; Pkg.test()'` -- all tests pass
2. Verify no inline prefactor formulas remain: grep for `w0 / (config.sigma * sqrt(2` and `w0 * config.t0^2 * (config.sigma` across src/ (should return 0 outside furnace_utensils.jl)
3. Verify `_krylov_oft!` is fully removed: grep returns 0 hits
4. Verify `domain_prefactor` is used in all 5 files: jump_workers.jl, krylov_workspace.jl, krylov_matvec.jl, krylov_eigsolve.jl, trajectories.jl
</verification>

<success_criteria>
- `domain_prefactor` is the single source for the 16 formerly copy-pasted prefactor formulas
- A single `oft!` function with optimized signature replaces both old `oft!` (deleted) and `_krylov_oft!` (deleted)
- `time_oft!` and `trotter_oft!` remain unchanged as test/debug utilities
- All regression, allocation, and Krylov cross-validation tests pass with identical numerical results
</success_criteria>

<output>
After completion, create `.planning/phases/34-code-deduplication/34-01-SUMMARY.md`
</output>
