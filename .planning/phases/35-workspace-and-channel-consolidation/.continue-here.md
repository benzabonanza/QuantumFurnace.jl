---
phase: 35-workspace-and-channel-consolidation
task: plan-02
total_tasks: 2 plans
status: in_progress
last_updated: 2026-02-26T16:46:18.784Z
---

<current_state>
Executing phase 35 via `/gsd:execute-phase 35`. Wave 1 (plan 35-01) completed successfully. Wave 2 (plan 35-02) has NOT been spawned yet — paused between waves.
</current_state>

<completed_work>

- Plan 35-01 (Wave 1): Unified Workspace struct — COMPLETE
  - Defined `Workspace{S,D,C,T,SC}` parameterized struct replacing KrylovWorkspace, LindbladianWorkspace, KrausScratch
  - 5th type parameter `SC` ensures concrete-typed scratch on hot paths (zero-allocation preserved)
  - `_TransitionWrap{F}` function barrier pattern reduces matvec allocations from ~200KB to ~300 bytes
  - Single-jump B function variants (B_bohr, B_time, B_trotter) eliminated
  - Dead fields removed (KrausScratch.K0, LindbladianWorkspace.Id)
  - 3 Scratch sub-structs: KrylovScratch, LiouvillianScratch, ThermalizeScratch
  - 25 files modified across source and tests
  - All 1156 tests pass
  - Commits: 33670f6, 0031f9c, aea1c57
  - SUMMARY: .planning/phases/35-workspace-and-channel-consolidation/35-01-SUMMARY.md
</completed_work>

<remaining_work>

- Plan 35-02 (Wave 2): Trajectory workspace consolidation — NOT STARTED
  - Consolidate TrajectoryWorkspace + TrajectoryFramework + PerOperatorKraus into Workspace{Trajectory,D,C,T}
  - Define TrajectoryScratch sub-struct (jump_oft, psi_tmp, Rpsi, rho_acc)
  - Replace build_trajectoryframework with _build_trajectory_workspace factory function
  - Flatten per-operator Kraus data into vectors (Rs, K0s, U_residuals, U_Bs)
  - Update step_along_trajectory! from 4-arg to 3-arg form
  - Update all _run_chunk_* and run_trajectories functions
  - Per-thread workspace copy pattern for threading safety
  - Update 7 test files
- After 35-02: Phase verification (gsd-verifier), roadmap update
</remaining_work>

<decisions_made>

- [35-01] Workspace{S,D,C,T,SC} uses 5th type parameter SC for concrete scratch typing on hot paths
- [35-01] Union{Nothing, Function} boxing accepted at ~300 bytes/call (MATVEC_ALLOC_BUDGET=512)
- [35-01] _TransitionWrap{F} pattern for function barrier dispatch on transition closures
- [35-01] KrylovWorkspace kept as const alias for backward compatibility
- [35-01] JumpOp[jump] typed vector literal for Julia invariant parameterization
</decisions_made>

<blockers>
None
</blockers>

<context>
Phase 35 is a 2-wave sequential execution. Wave 1 unified the non-trajectory workspace types (Krylov, Lindbladian, Thermalize). Wave 2 brings the trajectory path into the same Workspace{S,D,C,T,SC} pattern. The key constraint for 35-02 is the dispatch conflict: Workspace(config::Config{Thermalize}) already exists in krylov_workspace.jl returning Workspace{Krylov}, so the trajectory constructor MUST use a factory function (_build_trajectory_workspace) instead of a Workspace constructor to avoid ambiguity.
</context>

<next_action>
Resume with `/gsd:execute-phase 35` — it will detect 35-01 SUMMARY exists, skip to Wave 2, and spawn the 35-02 executor agent. Alternatively, can manually spawn the 35-02 executor.
</next_action>
