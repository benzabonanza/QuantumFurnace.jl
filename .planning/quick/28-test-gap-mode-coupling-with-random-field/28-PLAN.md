---
phase: quick-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - experiments/validate_gap_disordered.jl
autonomous: true
must_haves:
  truths:
    - "Script runs to completion for n=4 and n=6 with disordered Heisenberg Hamiltonians"
    - "Eigenbasis overlap analysis shows whether |c_gap| > 0 for any observable with disordered Hamiltonian"
    - "Trajectory gap estimation runs with 20k trajectories and reports relative error for both system sizes"
    - "Results are printed with clear comparison between disordered and pure-Heisenberg baselines"
  artifacts:
    - path: "experiments/validate_gap_disordered.jl"
      provides: "Disordered Heisenberg gap validation experiment"
      min_lines: 150
  key_links:
    - from: "experiments/validate_gap_disordered.jl"
      to: "src/misc_tools.jl:load_hamiltonian"
      via: "load_hamiltonian(\"heis\", n; beta=10.0)"
      pattern: "load_hamiltonian"
    - from: "experiments/validate_gap_disordered.jl"
      to: "src/convergence.jl:build_preset_trajectory_observables"
      via: "function call"
      pattern: "build_preset_trajectory_observables"
    - from: "experiments/validate_gap_disordered.jl"
      to: "src/gap_estimation.jl:eigenbasis_overlap_analysis"
      via: "function call"
      pattern: "eigenbasis_overlap_analysis"
    - from: "experiments/validate_gap_disordered.jl"
      to: "src/gap_estimation.jl:estimate_spectral_gap"
      via: "function call"
      pattern: "estimate_spectral_gap"
---

<objective>
Create and run an experiment script that tests whether disordered Heisenberg Hamiltonians (random Z-field per site) break the symmetry protection on the Lindbladian gap mode for n=6.

Purpose: Quick tasks 25-27 established that all 8 observables have |c_gap|=0 for n=6 with pure Heisenberg chains due to symmetry protection (translational + SU(2) + discrete symmetries). Random Z-fields should break all these symmetries. This experiment determines if disorder enables trajectory-based gap estimation for n=6.

Output: `experiments/validate_gap_disordered.jl` script with execution results showing overlap analysis and gap estimation for disordered Hamiltonians.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@experiments/validate_spectral_gap.jl
@src/misc_tools.jl
@src/convergence.jl
@src/gap_estimation.jl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create disordered Heisenberg gap validation script</name>
  <files>experiments/validate_gap_disordered.jl</files>
  <action>
Create `experiments/validate_gap_disordered.jl` modeled closely on the existing `experiments/validate_spectral_gap.jl` but using pre-generated disordered Hamiltonians instead of pure Heisenberg chains.

Key differences from validate_spectral_gap.jl:

1. **System construction**: Replace `HamHam([[X,X],[Y,Y],[Z,Z]], ...)` with `load_hamiltonian("heis", n; beta=BETA)` which loads pre-generated disordered Hamiltonians from `hamiltonians/heis_disordered_periodic_n{4,6}.bson`. These have random Z-field per site (generated by `find_ideal_heisenberg`).

2. **make_system function**: Should call `load_hamiltonian("heis", n; beta=BETA)` to get the HamHam. Then build jump operators exactly like the pure version: single-site Paulis (X,Y,Z) on each site, normalized by `sqrt(3*n)`, transformed to eigenbasis via `ham.eigvecs`. Return `(ham, jumps, dim)`.

3. **No ARPACK vs eigen section** (Section 1): That's already validated. Go straight to the per-system-size analysis.

4. **Keep the same structure for each system size** (n=4, n=6):
   - Build system via `load_hamiltonian`
   - Build Lindbladian via `run_lindbladian`
   - Compute exact gap
   - Run `eigenbasis_overlap_analysis` -- THIS IS THE KEY DIAGNOSTIC
   - Run `estimate_spectral_gap` with 20k trajectories
   - Report per-observable fits and relative error

5. **Same parameters**: BETA=10.0, DELTA=0.01, NTRAJ=20_000, SAVE_EVERY=10, SEED=42, skip_initial=0.1, TimeDomain, with_coherent=false, excited initial state (psi0[end]=1.0).

6. **Same config helpers**: `make_liouv_config(n)` and `make_thermalize_config(n; mixing_time)` identical to validate_spectral_gap.jl (same grid parameters: NUM_ENERGY_BITS=12, W0=0.05, T0=2pi/(2^12*0.05), NUM_TROTTER_STEPS_PER_T0=10, SIGMA=1/BETA).

7. **Enhanced reporting**: After the overlap analysis for each n, print a clear summary line like:
   ```
   KEY RESULT: Disorder {DID/DID NOT} break symmetry protection for n={n}
   Max |c_gap| = {value} (observable: {name})
   ```
   Use threshold |c_gap| > 0.001 to determine if symmetry is broken.

8. **Final summary**: Print pass/fail for gap estimation AND a clear answer to the key question: "Did disorder break the n=6 symmetry protection?"

9. **No threshold targets** for pass/fail on n=6: We don't know what to expect yet. Just report the relative error. For n=4, keep TARGET_REL_ERROR_N4 = 1e-2 (1%). For n=6, use a generous 20% target (TARGET_REL_ERROR_N6 = 0.20) since the disordered Hamiltonian is a different system.

10. **Script header**: Document that this tests disordered Heisenberg chains, referencing quick tasks 25-27 as motivation.

Usage line: `cd QuantumFurnace.jl && julia --project experiments/validate_gap_disordered.jl`
  </action>
  <verify>File exists and has correct structure: `head -30 experiments/validate_gap_disordered.jl` shows proper header and imports. `grep -c "load_hamiltonian" experiments/validate_gap_disordered.jl` returns at least 1. `grep -c "eigenbasis_overlap_analysis" experiments/validate_gap_disordered.jl` returns at least 1.</verify>
  <done>Script exists with load_hamiltonian-based system construction, eigenbasis overlap analysis, and trajectory gap estimation for n=4 and n=6.</done>
</task>

<task type="auto">
  <name>Task 2: Run the disordered validation script and capture results</name>
  <files>experiments/validate_gap_disordered.jl</files>
  <action>
Run the experiment script:

```bash
cd /Users/bence/code/QuantumFurnace.jl && julia --project experiments/validate_gap_disordered.jl
```

This will take several minutes (n=6 Lindbladian is 4096x4096, plus 20k trajectories for each system size).

Capture the full output. The key things to look for in the results:

1. **n=4 overlap**: Should have non-zero |c_gap| (disordered n=4 breaks translational symmetry; pure n=4 already had non-zero overlap since gap was in k=0)
2. **n=6 overlap**: THE KEY QUESTION -- are any |c_gap| values now non-zero? If yes, disorder broke the symmetry protection.
3. **n=4 gap estimation**: Should pass with small relative error (expect similar to pure Heisenberg ~0.7%)
4. **n=6 gap estimation**: If overlap is non-zero, gap estimation should now work (relative error should be much better than the 10.7% seen with pure Heisenberg)

If the script fails to run, debug and fix the script. Common issues:
- Import errors: ensure `using QuantumFurnace, LinearAlgebra, Printf` are present
- load_hamiltonian path issues: the function uses `Pkg.project().path` internally, so `--project` flag is essential
- Type mismatches: ensure ComplexF64 consistency

Use timeout of 600000ms (10 minutes) for the run.
  </action>
  <verify>Script runs to completion. Output contains "FINAL SUMMARY" section. Output contains overlap analysis for both n=4 and n=6. Output contains "KEY RESULT" lines answering whether disorder broke symmetry.</verify>
  <done>Experiment completed, results captured showing whether disordered Hamiltonians break the n=6 gap-mode symmetry protection. Key findings documented in summary.</done>
</task>

</tasks>

<verification>
- [ ] `experiments/validate_gap_disordered.jl` exists and uses `load_hamiltonian` (not `HamHam` constructor)
- [ ] Script runs to completion for n=4 and n=6
- [ ] Eigenbasis overlap analysis printed for both system sizes
- [ ] Gap estimation results printed with relative errors
- [ ] Clear answer to: "Did disorder break n=6 symmetry protection?"
</verification>

<success_criteria>
The experiment runs and produces a definitive answer about whether disordered Heisenberg Hamiltonians break the symmetry protection that causes |c_gap|=0 for all observables at n=6. The script is committed to git for reproducibility.
</success_criteria>

<output>
After completion, create `.planning/quick/28-test-gap-mode-coupling-with-random-field/28-SUMMARY.md`
</output>
